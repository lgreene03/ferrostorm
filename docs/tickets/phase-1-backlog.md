# Phase 1 Backlog: Deterministic Core Prototype
Gate criteria (design doc 05): 500 units pathing at budget; double-run hash identical across Windows and Linux; 20 LAN test games with zero desyncs. Kill criterion: determinism cannot be stabilised -> halt and rethink, do not patch around.

## Blocking prerequisites (before any code)
- [x] TICKET-P0-01 | status: DONE (ratified via owner go-ahead, see ADR-001) | owner: Luke | Ratify ADR-001 (engine split). AC: status flipped to Ratified.
- [x] TICKET-P0-02 | status: DONE (see ADR-002-fixed-point.md: custom Fix64 Q32.32, benchmarked) | owner: architect | ADR-002 fixed-point library: benchmark >=2 candidates (throughput, sqrt/trig support, licence), write ADR. AC: ratified by Luke.
- TICKET-P0-03 | owner: architect | Publish /sim module map, command/tick API, and snapshot API spec. AC: sim-engineer and netcode sign off in plan comments.
- TICKET-P0-04 | owner: luke+legal-review | Stage B title checks per docs/design/10-stage-a-report.md s4. AC: registry/domain checklist completed and recorded in doc 09.

## Engineering (order matters)
- [x] TICKET-P1-01 | status: DONE for current scope (workflows/determinism.yml: purity grep + cross-platform golden hash, Windows+Linux; extends as systems land) | owner: qa | phase:1 | gdd:TDD-s9 | persona:P2 | Determinism CI: repo pipeline running double-run seeded headless matches on Windows+Linux, hash compare, merge-blocking. AC: red on injected float, green on clean build.
- [x] TICKET-P1-02 | status: DONE (World tick loop, command queue, splitmix64 RNG, FNV-1a state hash; movement system; selftests green) | owner: sim-engineer | Fixed-timestep sim core: tick loop, command queue, seeded PRNG, state hash. AC: US1.1 double-run hashes match; unit tests.
- [x] TICKET-P1-03 | status: DONE (runner CLI: selftest/determinism/match/bench; 0.87ms/tick at 500 units vs 8ms budget) | owner: sim-engineer | Headless match runner CLI (US1.3). AC: runs scripted command file, emits stats + final hash.
- [x] TICKET-P1-04 | status: DONE (Map + Dijkstra flow fields, tie-broken heap, no corner cutting, cache; 500 units arrive by tick 294 at 0.161ms/tick) | owner: sim-engineer | Grid world + hierarchical flow-field pathfinding, deterministic. AC: 500 units cross the benchmark map within tick budget (TDD s6) on CI hardware.
- [x] TICKET-P1-05 | status: DONE (harvest state machine, refinery processing at GDD numbers, retargeting; 4000/4000 credits exact in test) | owner: sim-engineer | Harvest loop: Ferrite field, harvester behaviour (US2.2), refinery processing, credits. AC: economy stats match GDD s4 numbers from /data.
- [x] TICKET-P1-06 | status: DONE (warhead x armour matrix, auto-acquire, stop-to-engage, deaths after scan; fog visible+explored bitgrids in hash) | owner: sim-engineer | One weapon type + damage matrix application + fog/shroud grid. AC: engagement results reproducible; fog updates incremental.
- [~] TICKET-P1-07 | status: SCAFFOLDED. /game contains the full Godot 4.2 project (scene, input, 15Hz accumulator, ugly-box _Draw) consuming SnapshotInterpolator; the interpolation contract is proven headless (runner mode: spectate, in CI). Compile/run needs a dev machine with the Godot .NET editor - bring-up steps in game/README-GODOT.md. Honest status: code UNTESTED until then. | owner: client-engineer | Godot ugly-box renderer consuming snapshots with interpolation; click-select and move orders. AC: visually tracks a headless-verified match.
- [x] TICKET-P1-08 | status: DONE for loopback (relay + 2 TCP lockstep clients, delay 3, hash exchange, 20 games zero desyncs); P1-08b netem harness OPEN | owner: netcode | 2-client LAN lockstep with command delay + hash exchange. AC: 20 LAN games, zero desyncs; desync injection test surfaces within 5s (US1.2).
- [x] TICKET-P1-09 | status: DONE (nightly-soak.yml: 5 seeds x 2 OS x determinism suite + 20 LAN games; first green week pending GitHub) | owner: qa | Cross-platform determinism soak: nightly 20-match Windows/Linux run, dashboard of hash results. AC: 7 consecutive green nights before gate review.

## Parallel non-engineering
- [x] TICKET-P1-10 | status: DONE (Q1 no cap, Q2 strict adjacency, Q3 squads via ADR-003; GDD s13 updated) | owner: game-designer | Close GDD Q1 (unit cap), Q2 (build radius), Q3 (squads, pairs with ADR-003). AC: GDD updated with rationale.
- [x] TICKET-P1-11 | status: DONE (Ferrite identity adopted, player-facing Cinder references swept from GDD/overview) | owner: game-designer | Ferrite resource identity write-up + rename sweep spec (doc 10 s2). AC: GDD s4 updated; legal-review sign-off.
- [x] TICKET-P1-12 | status: DONE (docs/design/11-control-scheme-spec.md accepted) | owner: ux | Control scheme spec v1 (classic + modern), Phase 2 sidebar wireframe. AC: client-engineer accepts as implementable.
- [ ] TICKET-P1-13 | status: OPEN, needs human contractor contact | owner: art-pipeline | Style bible v0 + vertical-slice asset spec with costed quotes (doc 08 validation). AC: 2-3 real quotes per category recorded.
- [x] TICKET-P1-14 | status: DONE (docs/design/12-balance-simulator-spec.md accepted; implementation queued as TICKET-P2-BAL-01) | owner: balance | Balance simulator design doc: engagement runner spec against /sim headless API. AC: architect sign-off; implementable as TICKET-P2-xx.
- [x] TICKET-P1-15 | status: DONE (docs/design/13-phase1-gate-review.md: conditional pass recommended, honest ledger included) | owner: producer | Gate review pack: criteria evidence, risk register update, go/no-go recommendation. AC: signed by producer+qa+legal-review, decision by Luke.


## Added during Phase 1
- [x] TICKET-P1-08b | owner: netcode | status: DONE at transport level (ChaosProxy: 60ms±30ms and 150ms±30ms + 5% stalls of 500ms, zero desyncs; identical seed produced identical hash at both latencies, proving network timing cannot affect sim state). Kernel netem unavailable in the build container; real-hardware netem remains a beta checklist item. |
- [ ] TICKET-P2-BAL-01 | owner: balance | Implement simulator per docs/design/12.
- [x] TICKET-P2-SIM-01 | owner: sim-engineer | status: DONE. (a) Sqrt bit-length guess, 5.5x, bit-identical (goldens unchanged pre-regen). (b) Separation with spatial buckets, deterministic pair order, arrival contagion, and leaky stall-arrival gated on stationary contact - debugged through three failure modes (rim orbit, corridor give-up, capacity lock) via the pathdebug diagnostic mode. Crowd compaction/formations remain P2 polish. |
- [x] TICKET-P2-SIM-02 | owner: sim-engineer | status: DONE. Power supply/draw totals; production rate floors at 50% when undersupplied (GDD s5), verified exactly (75 vs 150 ticks). |
- [x] TICKET-P2-SIM-03 | owner: sim-engineer | status: DONE. Factory queues, upfront cost deduction, deterministic spawn offsets, attack-pursuit, dead-target clearing. Pay-as-you-build deduction is a P2 refinement ticket. |
- [x] TICKET-P2-DATA-01 | owner: tools | status: DONE. Strict YAML-subset loader (flat maps, inline lists, folded blocks), zero dependencies, round-trips data/units/com_harvester.yaml in selftest. |
- [x] TICKET-P2-DATA-02 | owner: tools | status: DONE. Instance unit catalogue with RegisterUnitType (locked after tick 0); /data files for both producible units committed; selftest proves the loaded YAML converts to exactly the compiled reference defs. Weapon /data files remain compiled (name map) pending Phase 2 weapon schema.
- [x] TICKET-P2-SIM-03b | owner: sim-engineer | status: DONE. Pay-as-you-build: credits drain with progress via integer cumulative formula, production stalls while broke and resumes on refill; drain audited to the exact credit at tick 40 in the production scenario. Queue-cancel refund is a future ticket.
- [x] TICKET-P2-BAL-01 | owner: balance | status: DONE. tools/Ferrostorm.Balance implements doc 12: per-cost engagement matrix (3 seeds), TTK sanity bounds, counter audit vs design intent; markdown report; nonzero exit gates CI on every push. Baseline report committed at docs/balance/2026-07-08-baseline.md - first run PASSED, and notably the simulation contradicted the naive dps spreadsheet (range 4 vs 3 kiting + Lanchester concentration give cannons a 100% survivor-value win over rifles per-cost), which is precisely the failure mode of spreadsheet balancing the tool exists to prevent. Harvester tempo test awaits the second faction's opening layout.
- [x] TICKET-P2-BAL-02 | owner: balance | status: DONE. Standard-opening tempo test in the balance gate: 8400 credits per 3000 ticks committed as the baseline tripwire; reproducibility and collapse floors are hard failures. Faction-divergent layouts slot in when Sodality economy data lands.
- [x] TICKET-P2-UX-01 | owner: sim-engineer | status: DONE (sim side; client bindings when Godot lands). (a) Attack-move: sight-range acquisition with hunt-approach, stop-to-engage, resume to the ordered point when clear, own final-arrival rule; ordered destination preserved in dedicated fields so pursuit cannot clobber it. Verified by the gauntlet scenario, which also proves fog-honesty: pickets beyond sight of the marching line are correctly bypassed, not omnisciently hunted. Contagion/stall arrival gated off during hunts (statue bug found and fixed). (b) CancelProduce with exact refund: pay-as-you-build makes the pro-rata refund free - refund equals BuildPaid; verified together with stall-on-broke credit conservation (credits + BuildPaid invariant).
- [x] TICKET-P2-SIM-04 | owner: sim-engineer | status: DONE. Structures are real map objects: 2x2 blocked footprints, anchored spawns, PlaceStructure command with GDD Q2 strict adjacency (BuildRadius 5 Chebyshev from any own structure), footprint/occupancy/bounds/treasury validation, upfront cost deduction, and flow-cache invalidation on every passability change. Construction scenario proves accept/reject/chained-adjacency exactly and reroutes a unit mid-march when its corridor is sealed. Harvesters dock adjacent to blocked refinery footprints.
- [x] TICKET-P2-SIM-05 | owner: sim-engineer | status: DONE. Classic sidebar flow: BuildStructure queues at a Construction Yard, pay-as-you-build drains during construction, the finished structure holds in a ready slot (production line pauses), and PlaceStructure consumes readiness at zero further cost. Rejected placements retain readiness; CancelProduce serves CY queues with the same exact refund. Found and fixed a carrier-aliasing bug where the ApplyCommand epilogue writeback resurrected consumed readiness - the diagnostic symptom was an AI placing 260 free power plants.
- [x] TICKET-P2-SIM-06 | owner: sim-engineer | status: DONE (except damage states). Construction Yard with radius-7 projection (others 5) per GDD Q2; turret (armed structure, fires through the standard CombatSystem); SellStructure refunds half cost and frees the footprint; destroyed structures leave passable rubble (footprint unblocks + flow cache cleared). Damage-state visuals are presentation work.
- [x] TICKET-P2-SIM-07 | owner: sim-engineer | status: DONE. Replays: seed + setup + command stream in a text format; playback re-simulates and must land on the identical final hash. A 3000-tick AI-vs-AI match round-trips bit-exactly in 4.4 KB. Doubles as the preservation-promise feature (doc 07) and the strongest determinism regression artefact; gated in CI per push.
- [x] TICKET-AI-01 | owner: ai-engineer | status: DONE (first doctrine). Deterministic rule-based skirmish commander playing through the public command interface only: build order (power/refinery/factory/turret), harvester production and tasking, alternating army production under pay-as-you-build, and 6-unit attack waves at the nearest enemy production structure. AI-vs-AI runs the complete classic loop with zero human input - the skirmish scenario gates every push. Personality tiers, threat response, and expansion logic are follow-up tickets.
- [x] TICKET-P2-BAL-03 | owner: balance | status: DONE. Rocket squad (first Sodality roster entry) completes the counter triangle; the balance gate now audits all three edges per-cost and PASSED on first data attempt (cannon>rifle 100% sv, rocket>cannon 90% sv, rifle>rocket 100% sv, TTKs 7-31s). Report: docs/balance/2026-07-08-triangle.md.
- [x] TICKET-AI-02 | owner: ai-engineer | status: DONE (threat response + difficulty knobs). Enemy units within 14 cells of any own structure pull the whole standing army onto them, preempting offence that beat; act-frequency and wave-size are constructor parameters for difficulty tiers. Expansion (second refinery/base) remains open as AI-03.
- [ ] TICKET-AI-03 | owner: ai-engineer | AI expansion logic + personality tiers using the difficulty knobs.
- [x] TICKET-P2-SIM-08 | owner: sim-engineer | status: DONE (sim side). Repair: 2 hp per tick for 1 credit per tick, halts while broke, auto-off when whole, charge verified to the exact credit. Harvester flee: shot while LOADING abandons the crystal face and runs the part-load home; damage merely en route does not deter (classic stubbornness) - first cut fled on any damage and starved the economy, refined after the test exposed it. Damage-state visuals are presentation work.

## Continuation session 5
- [x] TICKET-P2-SIM-09 | owner: sim-engineer | status: DONE. Stealth and detection: per-player DetectedMask recomputed each tick from detector coverage; firing breaks stealth for 3 seconds (RevealTicks); auto-acquire, sight-hunt, and explicit targeting all respect targetability; stale explicit targets on re-cloaked units clear to auto-acquire. Shade Raider (Sodality stealth identity) and Sentinel Scout (Directorate detector) in /data and the catalogue. All three rules pinned by the stealth scenario.
- [x] TICKET-P2-SIM-10 | owner: sim-engineer | status: DONE. Veterancy: kill credit to the first deterministic attacker on the killing tick; promotion at 3 and 6 kills; damage scales 4/4, 5/4, 6/4 (verified to the hit point: 12/15/18); elites self-repair 1 hp per 15 ticks. veterancy_enabled respected (harvesters and MCVs never rank up).
- [x] TICKET-P2-DATA-03 | owner: tools | status: DONE. Text map format (ferrostorm-map v1: size, starts, character grid with terrain and ferrite fields) with a strict fail-loud parser; the committed skirmish-01 map is now the single source of terrain, fields, and start positions for the skirmish scenario and replays; round-trip pinned in selftest.
- [x] TICKET-P2-SIM-11 | owner: sim-engineer | status: DONE. MCV: producible from the factory (type 7), Deploy consumes the vehicle and founds a Construction Yard on its own footprint (ValidFoundation: physical clearance, no adjacency - expansion from nothing), projecting the full CY build radius. Deploy pinned in the construction scenario. AI expansion USE of MCVs remains AI-03.
- [x] BUGFIX | Movement orders now clear standing attack orders (classic order priority). Found by the veterancy regen test: a bully ordered to retreat stood its ground shooting because Move never cancelled its explicit target - the retreat was silently hijacked. Also fixed: production spawn read the queue after dequeue (index shift/overrun caught by the production scenario the moment unit type ids were attached to spawns).

## Continuation session 6
- [x] TICKET-P2-SIM-12 | owner: sim-engineer | status: DONE. Victory conditions, classic short-game rule: no structures and no MCV = eliminated; units alone cannot rebuild. Winner latches the exact tick the last hope dies (verified: destroying the final structure declared nothing while an MCV lived; the MCV's death and the win landed the same tick; a surviving rifle changed nothing). Winner is hashed; PlayerEliminated events fire once per player.
- [x] TICKET-P2-SIM-13 | owner: sim-engineer | status: DONE. Deterministic gameplay event stream (Fired, Died, StructurePlaced, ProductionComplete, Promoted, Deployed, PlayerEliminated) cleared and emitted per Step - the presentation layer's sound/effects feed, identical on every client by construction, deliberately outside the hash. Consistency pinned in spectate (death events == corpses).
- [x] TICKET-AI-03 | owner: ai-engineer | status: DONE. AI expansion: correct local-depletion trigger (no meaningful deposit within 20 cells of home - the naive nearest-field test is wrong once home runs dry, because the nearest field IS the rich distant one), save-for-MCV with army production standing aside, drive-and-deploy with arrival-slack tolerance, newest-first placement search so the expansion base receives its own refinery, and the infrastructure-before-army budget doctrine. Personality presets: Standard/Rusher/Turtle via the difficulty knobs. The expansion scenario proves the full arc: second base founded at the rich field, refinery added, deposit mined from 12000 down to 230.

## Continuation session 7
- [x] TICKET-P2-SIM-14 | owner: sim-engineer | status: DONE. Area damage: weapons carry MinRange and SplashRadius; splash deals half matrix damage (per victim armour, veterancy-scaled) to everything near the target regardless of allegiance, sparing only the shooter; the dead zone is absolute - inside minimum range a gun neither fires nor pursues inward, even under a standing order. Howitzer (dir_howitzer, range 9, dead zone 3, indiscriminate splash) joins the roster from /data. Pinned exact: 18 direct / 9 splash / 9 splash on a clump, dead zone held for 100 ticks under an explicit attack order. Balance gate extended: siege loses every knife fight per-cost (all three expectations held first try, 7-12s TTKs).
- [x] TICKET-P2-SIM-15 | owner: sim-engineer | status: DONE. Superweapon: 4000-credit structure, 100 power draw, charge counts down only at full power (classic pause-when-brown-out), premature launches refused, 75-tick incoming warning, Omni detonation (full within 1.5 cells, half within 3) with standard death rules and no veterancy credit, automatic recharge. Events: SuperweaponReady/Launched/Impact carry the ground-zero position for the presentation layer. Pinned exact: ground zero annihilated (720), outer-ring factory damaged to precisely 360, bystander untouched, recharge restarted. AI use of superweapons is ticketed as AI-04.
- [ ] TICKET-AI-04 | owner: ai-engineer | AI builds and fires superweapons at economy clusters.

## Continuation session 8
- [x] TICKET-P2-SIM-16 | owner: sim-engineer | status: DONE. Crush: heavy vehicles flatten enemy foot infantry on deep contact; crush-eligible squads exert no separation push on the vehicle (treads do not yield - the first cut had separation politely steering the tank AROUND its victims), kill credit follows standard veterancy rules, stealth is no defence against contact. Pinned: an MCV flattened three squads without a shot fired, took no damage, earned no rank (veterancy-disabled), and completed its drive. Balance gate re-run with crush live: all matchup expectations still hold.
- [x] TICKET-AI-04 | owner: ai-engineer | status: DONE. Superweapon doctrine: the yard reaches for the sky once a 4500-credit war chest is banked (infrastructure-first covers the saving), and a charged weapon fires immediately at the enemy refinery, falling back to any production structure. Pinned end to end: the AI placed its superweapon at tick 766, waited out the full 1500-tick charge, and destroyed the enemy refinery - decision, patience, and strike all through the public command interface.

## Continuation session 9
- [x] TICKET-P2-SIM-17 | owner: sim-engineer | status: DONE. Mid-game save/load: versioned binary format capturing every field of world state (packed terrain, per-player fog/credits/elimination flags, RNG state, all ~50 entity fields, production queues serialized in sorted-key order so dictionary iteration can never leak into an artefact), with a trailing magic against truncation. The saveload gate is self-verifying end to end: a mid-replay save must hash identically on load AND the resumed run must reach the uninterrupted final hash - so even fields outside the hash (e.g. Sight) are protected, since their corruption diverges the continuation. 7.2 KB for a 1500-tick two-AI match. Gated in CI per push.
- [x] TICKET-P2-SIM-18 | owner: sim-engineer | status: DONE. Veil projector (Sodality's second stealth pillar): powered projectors field-cloak friendly mobile units within radius 6; the veil drops the instant supply falls below draw (classic brown-out decloak, verified by selling the power plant mid-scenario); detectors strip the veil; firing breaks it for 3 seconds. FieldCloaked recomputed per tick and hashed.
- [x] TICKET-P2-QA-01 | owner: qa | status: DONE. The perf report became a hard gate: match mode exits nonzero if movement exceeds the 8 ms/tick budget (TDD s6), so a perf regression now blocks merge instead of scrolling past in a log.

## Continuation session 10
- [x] TICKET-P2-SIM-19 | owner: sim-engineer | status: DONE. Shift-queued waypoint orders end to end: Command carries a Queued flag through the wire format (Ferrostorm.Net), the replay format (bumped to v2), the state hash (sorted-key iteration), and the save format; an OrderDispatchSystem pops the next order for every idle entity in sorted-id order (dispatched commands can touch shared state such as the treasury, so between-entity order is pinned); a fresh direct order wipes the entity's remaining plan; dead entities' queues are reaped deterministically. Pinned: three legs visited strictly in order; a mid-plan direct order wiped the remainder (the unit never approached the abandoned waypoint); the LAN soak now sends one queued order in three across real TCP - 20 games, zero desyncs. The replay and save/load gates independently converge on the same final hash for the same match.

## Continuation session 11
- [x] TICKET-P2-SIM-20 | owner: sim-engineer | status: DONE. Campaign trigger pipeline: map format v2 adds tagged `unit`/`structure` placement lines and single-line `trigger WHEN... -> DO...` scripting (conditions: elapsed, destroyed TAG, credits, entered-zone; actions: grant, spawn, win, message); MissionRunner is a deterministic sim-adjacent driver (like SkirmishAI) firing each trigger once through legitimate scripting entry points; World.DeclareWinner lets scripted objectives decide a match ahead of the short-game rule. Mission-01 ships as pure data and is proven end to end by the 19th scenario: the skirmish AI (which knows nothing about missions) bootstraps, springs the ambush zone, and destroys the tagged camp - scripted victory at tick 2157. The first failure was mission ECONOMICS, not code: starting funds plus the timed grant could not cover the 5700-credit bootstrap chain, so the strike force was the only wave that would ever exist - fixed as mission design.
- [x] TICKET-P2-DOC-01 | owner: docs | status: DONE. docs/design/14-sim-handbook.md: the ground-truth reference for the deterministic core - contractual system order, full command/catalogue/weapon tables, mechanics quick truths, all four file formats, runner modes and gates, golden-hash policy, and what is deliberately NOT in the sim.
- [ ] TICKET-P2-SIM-21 | owner: sim-engineer | Campaign save: pair the world save with MissionRunner state (fired flags, messages).

## Continuation session 12
- [x] TICKET-P2-SIM-21 | owner: sim-engineer | status: DONE. Campaign save: MissionRunner serializes its fired flags and message log ("MSN1" section appended after the world save); on load, the mission rebuilds from the same map file (tags and entity ids are deterministic from map order) and restores state on top. The campaignsave gate proves it the hard way: record a full mission, replay-drive to the midpoint, save world + mission, load both, resume - identical scripted victory, identical message log, identical final hash. 5,062 bytes for mid-mission world + mission state. Gated in CI per push.
- [x] TICKET-P2-UX-02 | owner: sim-engineer | status: DONE. Cancelling a READY structure in the sidebar slot refunds it in full (it was fully paid) and clears the slot, letting the paused production line resume; in-progress cancels continue to refund exactly what was paid so far. Pinned in construction phase H: net cost of a build-then-cancel is exactly zero.

## Continuation session 13 (the faction session)
- [x] TICKET-P3-FAC-01 | Faction registry on the world (Directorate/Sodality, hashed) with production locks: faction hardware is refused to the wrong side, enforcement not convention.
- [x] TICKET-P3-FAC-02 | Signature units: sod_phantom_tank (rockets from nowhere), dir_bulwark_tank (the wall that walks, new BulwarkCannon weapon), com_engineer. Rifle AND rocket squads reclassified common - see design law below.
- [x] TICKET-P3-FAC-03 | Engineer capture: CaptureSystem converts enemy structures on contact via the existing Attack command (zero wire churn); engineer consumed, queues cleared, Captured event. Pinned by scenario 20 including production under the new flag.
- [x] TICKET-P3-FAC-04/06/07 | Faction AI doctrine: symmetric cheap opening book (nobody opens with their Mammoth), Directorate cycle with bulwark/howitzer/capped sentinels plus harvester-escort, Sodality shadow war (phantoms strangle enemy harvesting instead of joining the line), waves conscript fighters only.
- [x] TICKET-P3-FAC-05 | Faction-war gate built (6 seeded AI matches with tick-limit adjudication by surviving value) - REPORTING ONLY, deliberately: eleven forge iterations oscillated between 6-0 shutouts, proving outcomes are currently doctrine-dominated. Design law extracted: THE COUNTER-TRIANGLE IS COMMON, identity lives in the specials. The final diagnostic reproduced the classic 1995 harass-bait exploit (economy-wide threat response reverted with the post-mortem in the code). Full journal in docs/design/15-factions.md and docs/balance/2026-07-11-factions.md.
- [ ] TICKET-AI-05 | Defence squads: dedicated guard force so the main army ignores harass bait; gates faction-war promotion to blocking.
- [x] TICKET-P3-LORE-01 | docs/design/15-factions.md: Directorate/Sodality identities and moral framing (two self-righteous narrators, the C&C model), plus the Reclaimers defined as the third faction - lore seeded now (salvage crews from mission 3), mechanics post-launch, engineer capture as their future doctrine.

## Continuation session 14 (the loop session)
- [x] TICKET-AI-05 | Defence squads: waveSize/2 lowest-id fighters form the garrison; only they answer threats (now including harvester distress anywhere - safe, because the field army no longer chases), idle garrison drifts home, waves conscript the field army only and target the enemy refinery first. The puppet exploit is dead: armies now survive full matches with structures intact. Faction-war score remains reporting-only pending human playtesting (adjudication-margin territory).
- [x] TICKET-P3-CAM-01 | Campaign trigger verbs: `owned TAG P` condition (capture objectives) and `assault P CX CY` action - implemented as command issuance through MissionRunner.Tick's output list, because the mission is a player, not a god. Map headers: `faction P F` and `rules noshortgame`.
- [x] TICKET-P3-CAM-02 | DESIGN LAW, found the hard way: the short-game rule is a SKIRMISH rule. A commando force with no base was eliminated on tick one, and a defence mission's baseless attackers handed the defender victory on tick zero - both before a single order ran. Maps now declare `rules noshortgame`; the flag is hashed and saved. The hunt also disproved three wrong theories (stale binaries, static caches, sequence contamination) via mechanical body-diff and bisection before the real one stood.
- [x] TICKET-P3-CAM-03 | Missions 02 "Silent Salvage" (Sodality commando: escort the engineer, alarm assault, capture-the-depot objective - scripted victory tick 153) and 03 "Hold the Line" (three scripted assault waves break on a Spine gap; Turtle survival victory at exactly 4200), plus the campaign manifest and three briefings in full story voice (Luke to rewrite as author). Both missions pinned by scenarios.
- [x] TICKET-P3-MAP-01 | The Spine: skirmish-01 rebuilt with real geography - a ridge wall pierced by three gaps, the contested field sitting in the Ferrite Gap itself. AI verified fighting through chokepoints.
- [x] TICKET-P3-SIM-01 | Service depot (structure 8): powered field repairs, 2 hp and 1 credit per tick per unit in radius 4; exact-cost, no-charity, enemies-untreated all pinned by scenario.
- [x] TICKET-P3-OPS-01 | Godot headless bring-up attempted from the container: blocked at the egress proxy (release-assets.githubusercontent.com 403s despite appearing in the session allowlist). Exact remedy and the one-command validation documented in game/README-GODOT.md.

## Continuation session 15 (visuals)
- [x] TICKET-P3-VIS-01 | Runner `export` mode: a faction war sampled every 2 ticks into JSON (positions, hp, cloak state, credits, events with superweapon ground-zero coordinates). 1.8 MB for six minutes of battle.
- [x] TICKET-P3-VIS-02 | The match viewer (tools/viewer/): a standalone HTML war-room replaying exports on canvas - Directorate uplink aesthetic grounded in the world's materials (cinder/ferrite-gold/faction chrome), telemetry rail (clock, treasuries, force bar, wire-traffic feed), tape-deck transport with scrub and keyboard control, drag-drop record loading. Signature: ferrite seams glow and DIM as they are mined - the economy drains out of the terrain itself. Tracers per shot, death rings, capture flashes, superweapon reticle countdown and impact bloom. JS syntax machine-checked; composition verified by rendering a real mid-battle frame (art/battle-preview.png) and inspecting it.
- [x] TICKET-P3-VIS-03 | Sprite set: 19 top-down SVGs in the two faction languages + commons (rendered PNGs + contact sheet, visually inspected: silhouette test passes, faction languages read at RTS scale). Style guide at docs/design/16-visual-style.md - palette from the world's materials, the one-place team-colour law, silhouette-first doctrine; the bible for later Blender/contractor work.

## Continuation session 16 (3D)
- [x] TICKET-P3-3D-01 | Blender asset pipeline: headless Blender 4.0 installed in the container; art/3d/builder.py procedurally models all 20 units and structures in the faction language (gunmetal slabs vs rust wedges vs olive commons, one-place team-colour law, emissive ferrite), Cycles-rendered and visually inspected (art/3d/asset-lineup.png).
- [x] TICKET-P3-3D-02 | All 20 models exported as .glb (620 KB total) into game/assets/models/ - Godot-native import, zero conversion steps.
- [x] TICKET-P3-3D-03 | The first 3D battle render (art/3d/battle-first-light.png): a real exported match frame placed as instanced 3D models on 3D terrain (Spine ridges merged to strips), classic RTS camera, Cycles. Ferrostorm as a 3D game, generated from live sim data.
- [x] TICKET-P3-3D-04 | Godot 3D client shell: ModelLibrary (kind/unitType -> .glb), RtsCamera (three-quarter rig, WASD/edge pan, wheel zoom), ReplayTheater (plays sim exports as a smoothed 3D battle; same shapes the live SnapshotInterpolator binding will feed later), scenes/Battle3D.tscn, and first-run instructions. Written blind - the one part of the project pending first-run validation in the editor, honestly flagged in README-GODOT.md.

## Continuation session 17 (rename)
- [x] TICKET-P0-NAME-01 | The former codename and the word it contained are UK-offensive slang and are purged project-wide at Luke's direction: directories, csproj files, namespaces (Ferrostorm.Sim et al), the map format header (ferrostorm-map, extension .fmap), the save magic (FER1), the palette token (--cinder), all prose, and the two historical title-study docs. Full battery green after the rename; golden hashes byte-identical, confirming pure rename with zero behavioural change. The project is Ferrostorm, everywhere, and the old term does not appear again.

## Continuation session 18 (Godot bring-up + GitHub)
- [x] TICKET-P1-07 | status: DONE - first-run validation complete. Handed off from a sandboxed container with no editor/nuget.org access to a Mac with full tooling. Environment stood up from scratch: .NET 8 SDK (user-local install to ~/.dotnet, bypassing the Homebrew cask's blocking sudo prompt) and Godot 4.7 .NET (official godotengine.org release zip, bypassing the same sudo problem in the Godot Homebrew cask). Two real bugs found and fixed in the previously-untested client code: a `Fix64.FromFraction` argument needed `int` not `long` in Main.cs; the repo-root NuGet.config (which intentionally clears all package sources for the offline sim) also blocked /game from reaching nuget.org for Godot.NET.Sdk, fixed with a game/NuGet.config override. Verified end to end without an interactive display (Screen Recording permission unavailable in-session): headless asset import (20/20 .glb), full C# build, and a real windowed Godot run with a temporary capture script confirmed terrain and multiple distinct unit/structure models rendering at their correct positions, cross-checked directly against game/replay.json frame data. Battle3D.tscn is now the main scene. Two macOS-specific gotchas documented for next time: Godot's CoreAudio init can hang forever with no visible dialog (fix: `--audio-driver Dummy`), and macOS App Nap can freeze an unfocused windowed GUI app's render loop entirely (fix: launch via `open -a`, not a backgrounded shell). Full details in game/README-GODOT.md.
- [x] TICKET-P0-GITHUB-01 | owner: Luke | Repo pushed public to https://github.com/lgreene03/ferrostorm. Windows CI failed on first push (golden hash check reported every scenario "diverged" despite identical values) - root cause was `Console.WriteLine` in the Runner's `Golden()` emitting `Environment.NewLine` (\r\n on Windows) regardless of the LF-committed golden-hashes.txt; a `.gitattributes` normalization pass alone was insufficient because got.txt is generated fresh on the runner, not checked out from git. Fixed at the source: `Golden()` now forces `Console.Out.NewLine = "\n"`. Not a determinism bug - build, selftest, and double-run determinism all passed on windows-latest before the byte-diff step failed. CI is green on both ubuntu-latest and windows-latest as of this session.
- [~] TICKET-P3-VIS-04 | owner: art-pipeline | Visual upgrade to the C&C-Remaster stylized-realistic band. All four pipeline parts DONE this session: (a) hero.py and materials2.py written from scratch (the originals were container-only artifacts, never committed), hero cannon tank renders convincingly ahead of the blockout in art/3d/hero.png; (b) roster-wide weathering via builder.USE_WEATHERED, verified across all 20 models in art/3d/lineup.png (lineup.py is new); (c) bake.py bakes diffuse+roughness to 1024px textures and re-exported all 20 .glbs, verified by Blender re-import render, clean Godot headless import, AND an offscreen Godot capture showing baked models in the actual client; (d) battle-scene.py path-fixed and extended with mist + Fog Glow glare (art/3d/battle-atmosphere.png). The session's real find: three Blender 5.x incompatibilities in the committed builder.py (stale matrix_world at join, transform_apply parameter defaults resetting part origins, and primitive size semantics halving every box) - under Blender 5 every roster model rendered exploded until fixed; the committed Blender-4 .glbs predated the break. Four further Blender 5 compositor API changes documented in doc 17. Ticket stays open for the per-unit detail-geometry pass (hero.py is the pattern) and taste iteration; the pipeline itself is complete and verified.
- [x] TICKET-P3-VIS-05 | owner: art-pipeline | Roster detail-geometry pass, all 20 models: shared track/hatch/antenna helpers in builder.py, then per-unit detail in the faction languages - Directorate tanks with road wheels, skirts, glacis plates, mantlets and muzzle brakes; the howitzer with cradle, sleeve and stabiliser spades; the scout as a 6-wheeler with sensor mast; Sodality wedges with welded salvage plates, fins and exhausts; the harvester with intake teeth and hopper rim; the MCV with a full deployment crane; structures with louvres, sawtooth roofs, pipes, gantry trolleys, coils, guy-wires and cargo clutter. Silhouette law held throughout (team colour in exactly one place). All 20 re-baked and re-exported; verified loading 20/20 in Godot and rendering in the actual client via offscreen capture. Two more findings for the pattern book: Blender's Rx(+t) tips a cylinder's +Z end toward MINUS Y, so elevated barrels need the negative angle with sleeve/muzzle placed along the computed axis (probe world extents per PART, not just the join, to catch it); and Godot's glTF importer default (embedded_image_handling=1, Extract Textures) writes loose texture copies into the source tree on every import - deleting those out from under the import cache leaves stale .scn dependencies that silently null PackedScene loads for a subset of models. Fixed properly: all 20 .glb.import files pin mode 3 (Embed as Uncompressed), the strays are gitignored as a backstop, and the recovery for a corrupted cache is wiping .godot/imported and reimporting - verified with a load-all script (20/20) rather than trusting a clean import log.
## Continuation session 19 (the game session)
- [x] TICKET-P5-MAP-01 | owner: client-engineer | Doc 22 Wave A map scale, MINUS the golden-regenerating bundle: MAP-01 (batch the terrain draw calls), MAP-03 (scale scatter with map area) and MAP-04 (a real 192x128 map with its generator). MAP-02 and C-13 are NOT here and were not attempted: they regenerate the `skirmish` golden and are blocked on an ADR plus Luke's sign-off, exactly as doc 22 section 4 says. MAP-01: every per-cell dressing piece in BuildTerrain moved from its own MeshInstance3D to a per-Material MultiMesh, batching by Material reference because a MultiMesh carries one Mesh and only the material differs. MEASURED on skirmish-04 (192x128, 9.0% blocked): 3974 top-level MeshInstance3D became 20 children, and in a live frame 3479 draw calls became 143. BuildTerrain wall time 160.5 ms to 97.0 ms. The shipped 96x64 maps: skirmish-01 693 to 8 children, skirmish-02 1003 to 8, skirmish-03 1420 to 22. TWO DEFECTS IN THE DOC 22 SPEC, both found by measurement and both fixed here rather than copied. (a) STEP 1's helper says `Basis.FromEuler(rot).Scaled(size)`. Basis.Scaled applies scale in the GLOBAL frame (diag(S)*R) but a Node3D with a Rotation and a BoxMesh of Size S is R*diag(S). Probed against GodotSharp 4.7: for the shore ramp (size 0.36,0.02,1.04, Z rot -0.33) the spec's form gives a y axis of length 0.118 instead of 0.02, i.e. a 6x-too-thick wedge at the wrong tilt. Uses `Basis.FromEuler(rot) * Basis.FromScale(size)` instead. (b) STEP 2's hill clause says to squash a unit sphere per-instance by (hr, hh/2, hr). Godot builds a MultiMesh instance's normal matrix as mat3(instance_transform), NOT its inverse-transpose, so a non-uniform per-instance scale skews the normals. Harmless on a box (face normals lie along the scale axes and renormalise back, which is why the bridge station A/B diffs at worst 6/255 with zero pixels over 8), but on a sphere it turned matte Roughness-0.95 mounds into glossy plastic. Confirmed by experiment: forcing the scale uniform removed the gloss. Fixed by bucketing hills into 8 bins by squash ratio k=(hh/2)/hr, giving each bin a sphere BUILT at that ratio so the per-instance scale stays uniform. Costs up to 8 batches instead of 1 and quantises hill height by under 4%; the A/B's pixels-over-8 at the ridge station fell from 2099 to 180. This is why total children is 20-22 and not the doc's "at most 15": that number assumed one hill batch, and one hill batch is wrong. MAP-03: densityScale = (w*h)/6144 with all three 2200 sites moved together (count, loop bound, and the talus fold's base index) plus an explicit InstanceCount assert, because that fold is the one place a naive edit runs off the end. VERIFIED exactly 1.0 at 96x64, so the three shipped maps keep 2200/900/350/250 to the instance; at 192x128 they are 8800/3600/1400/1000. MAP-04: data/maps/skirmish-04.fmap, "Tarnwater Crossing", generated by tools/gen_skirmish_04.py rather than hand-typed because its fairness invariant is mechanical. 192x128, starts (12,20) and (179,107), 180-degree rotation symmetry (x,y) to (191-x,127-y) since the true midpoint is (95.5,63.5), 9x9 open aprons, 60 ferrite cells in 12 clusters of 5, 2224 blocked = 9.05% (the doc's own reference generator was 4.5%, below its 8-10% target, and is superseded by this one). The generator asserts every constraint itself, INCLUDING that closing the three bridges disconnects the two starts, so the crossings are proven load-bearing rather than decorative. Validated through the real MapData.Load, not the generator's own view: Width/Height/Starts/Fields.Count exact, blocked and field symmetry cell by cell, Chebyshev distance multisets from the two starts identical (sum 5272 each), flood fill reaching all 60 fields and the far start from both. A 5000-tick AI-vs-AI match completes with both sides building and fighting: 5 structures each, 89 live entities, and it double-runs to the same hash. HONEST PERF FINDING, and it contradicts the doc. Doc 22 calls MAP-01 "the single change that makes large maps renderable". MEASURED back to back on one machine, that is not true here: the PRE-MAP-01 renderer already ran the 192x128 map at 9.09 ms mean, and after MAP-01 plus MAP-03 it is 9.51 ms, i.e. about 5% SLOWER, with p99 flat (9.5 to 9.75). A MultiMesh has ONE AABB, so batching trades away the per-piece frustum culling the individual nodes had and the whole map's terrain is submitted every frame (primitives 919k to 1.73M); MAP-03 then deliberately spends more on 4x scatter, which is its entire point. The map HOLDS: about 105 fps at 192x128, p99 9.76 ms, no stutter attributable to this work. The ~80 ms worst frame is PRE-EXISTING, present in the pristine baseline at ~77 ms, and is not introduced here. So the draw-call collapse pays in draw calls (24x), in load time (1.65x) and in scaling headroom, NOT in frame time on an M4 - but the TDD's target is GTX 1060-class with a 4-core CPU, where 3479 submissions per frame cost far more than 143, so the ticket is still right for the reason the doc gives badly. Sim tick on skirmish-04 measures mean 0.079 ms, p99 1.53, worst 6.2-6.5 ms over four runs against the 8 ms budget: inside it, but roughly double doc 22's predicted 3.332 ms max, because this map is 9.05% blocked where the doc's reference was 4.5% and flow-field cost is linear in cells. PROOF: all 24 golden hashes byte-identical (`golden 2026` diffed clean), `git diff --stat sim/` empty, full battery green (match/determinism/selftest/replay/saveload all exit 0). Verified offscreen against a real Godot 4.7 run, not by inspection, with a before/after A/B at two fixed camera stations on skirmish-03. CAVEATS, stated rather than buried: the client verification ran in an isolated git worktree because a concurrent session was live-editing sim/World.cs in the shared tree, which would have made any golden result meaningless; MAP-01 landed BEFORE C-02, inverting doc 22 integration rule 1, so C-02 will touch these Emit call sites a second time when it lands (the rule warns of churn, not breakage, and MAP-01 arguably makes C-02 easier by giving it one Material argument per site); the map is only reachable as SKIRMISH-04 in the picker because MainMenu uppercases the filename, so "Tarnwater Crossing" lives in a file comment; and skirmish-04 uses only typed terrain (w/h/r/f), so it has no '#' cells and never exercises the ridge/strata/mesa/talus path - skirmish-03 covers that and is the map the A/B was shot on. NEEDS A HUMAN: whether Tarnwater Crossing plays well (the three-crossing tempo lever is a taste call, cheapModelSafe=false), and whether the 8-bucket hill quantisation is acceptable against the artist's intent.
- [x] TICKET-P5-BD-01 | owner: sim-engineer + client-engineer | Wave D's hash-neutral subset: the data catalogue, the power bar and the harvester courtesy (doc 22 section D). SIX TICKETS, chosen because they are the ones that can honestly ship without an ADR; the rest of Wave D is blocked and listed at the end of this line. (1) BD-06, THE STRUCTURE CATALOGUE INTO /data: data/buildings was an empty directory while the catalogue lived in a hardcoded switch and the per-structure hp, supply, draw and sight lived as literals scattered across eight Spawn methods, which CLAUDE.md forbids. Now nine files (the ticket said eight; the ADR-005 barrier landed after doc 22 was written and leaving it compiled-only would have left exactly the second source of truth this ticket exists to delete) plus data/schema.structure.json, a DataLoader.ParseStructure mirroring ParseUnit, and a StructureCatalogue bridge whose type-id map is the same one BD-17 needs. StructureTypeDef gained Hp/PowerSupply/PowerDraw/SightCells plus DEF-11's Footprint and WeaponId, GetStructureType went static to instance behind RegisterStructureType (tick-0 guard, mirroring RegisterUnitType), and FootprintOf/AnchorOf followed it so footprint has ONE source: DEF-12's warning that a footprint mismatch is silent and fatal is only true while there are two places to edit. ADR-005's reserved gate (type 10, no def, no file) is preserved as a named constant rather than quietly losing its 1x1. The supply/hp/draw spawn overrides became nullable rather than literal defaults, so the scenarios that pass `hp: 1500` are bit-identical and the ones that pass nothing read the catalogue. The Balance tool's own `const int WallHp = 500, TurretHp = 400` copy is deleted and reads the def: a balance gate quoting stale numbers passes while the game is wrong. THE CLAIM WAS VERIFIED, NOT TRUSTED: all 24 golden hashes byte-identical, `git diff sim/golden-hashes.txt` empty, and a new selftest round-trip proves all 9 /data files reproduce the compiled defs exactly and that no compiled type lacks a file. (2) BD-14, RALLY ATTRIBUTION: GameEvent gained a trailing `C` (the producing structure) set at both ProductionComplete emissions; GameEvents are not hashed, and the goldens prove it. The client's old proximity guess (any rallied structure within four cells, found by walking a Dictionary in nondeterministic order) is deleted for an exact read of ev.C. One marker per rallied structure, drawn only while that structure is selected, freed with it. Rally widened to the service depot, and deliberately NOT to the Construction Yard: structures are placed, not rallied. (3) BD-10, THE POWER BAR: GDD s5 asked for supply against draw as a bar; what stood there was a label named _power rendering the credits total a second time. Now POWER n / n over a 174x8 bar, supply as the fill and draw as a tick, gold while covered, amber to 75 per cent, red and pulsing below it, tween killed on recovery. Credits now appear exactly once. (4) BD-02, BUILD TIMES: no build time was shown anywhere in the game; every buildable now reads its cost and its seconds, and the queue head counts down. The wall shows none, because 0 ticks means unqueueable rather than instant. (5) P5-ECON-06, THE RECORDED FIND: right-clicking a field with no refinery was accepted, mutated FieldId, and did nothing, so the player concluded harvesting was broken. The client now refuses to send it, says NO REFINERY - BUILD ONE FIRST, and shows no order marker. DEVIATION FROM THE TICKET, deliberate: its clause 4 suppresses only the gold harvest marker, which leaves a denied harvest drawing the MOVE ring and playing the move sound - the same lie in a different colour. A click that queues nothing now gets no acknowledgement at all. (6) P5-ECON-07, THE COURTESY THE AI ALREADY HAD: an Idle harvester stayed Idle forever while the AI re-issued Harvest every tick. The client now re-issues through the ordinary command path, picking the field by RetargetField's exact rule (squared distance, ties to the lower id, in the sim's own fixed-point maths, so the client cannot fight the sim's reassignment), rate-limited to one attempt per harvester per second. It is quality of life and not strategy automation (GDD s1): an explicit Stop, a bare move or a rally parks a harvester permanently, and only an explicit Harvest re-enrols it. Doc 22 records that BD-15 read that rule the other way; P5-ECON-07's reading is the adopted one and the Game Designer may overrule it. PROOF: golden 24/24 byte-identical on every commit-time check; full battery (match, determinism 2026, selftest, replay, saveload) exit 0; Balance gate exit 0; purity and ADR-004 greps clean. CLIENT: an offscreen harness drove the shipped SkirmishLive and Sidebar through their real input paths - 47 checks, all passing, exit 0, capture read by eye. Two of those checks earned their keep: the rate-limit assertion passed VACUOUSLY at first (RunOneTick drains _pending into the tick and clears it, so a count taken after the step counts nothing), and rewriting it to count at the source proved the limiter genuinely allows 20 re-issues per 300 ticks where an unflagged flood would send 300. HONEST CAVEATS: the draw tick is a 2px seam-coloured rect per BD-10 clause 2, which against a gold fill reads as a notch cut out of the bar rather than a marker drawn on it - legible, but the colour was chosen for a cinder background; worth a designer's eye. The auto-resume lives in the client where the honest home is the sim (HarvestSystem already retries FindNearestRefinery and already reassigns dry fields), so it does not survive save/load and would not replicate in a networked game; the same is true of _rally, which BD-14 clause 6 already records. Both are filed in docs/questions/. SKIPPED AS SIM-BLOCKED, reported rather than attempted: BD-07 (honest power draw), DEF-10 (turrets offline below 75 per cent), BD-09 (radar), BD-17 (tech tree), DEF-12 (emplacement), DEF-13b (bastion), DEF-16 (CY queues hashed), BD-05 (sell refunds the ready slot), BD-20 (queue cap), P5-ECON-04/05/11/12/13/14/15 and DEF-14 - every one regenerates goldens or changes sim behaviour, which needs an ADR and Luke's sign-off that this session does not have.
- [~] TICKET-P5-SET-01 | owner: client-engineer | Settings, hotkeys and the LAN front door (doc 18 Phase D; closes register item M30, half of the LAN item, and the A/S hotkeys Phase A specified and never got). THREE PARTS, and the third is deliberately incomplete - read the LAN section before believing the word LAN. (1) THE SETTINGS SCENE, reachable from the main menu and persisted to user://settings.cfg via ConfigFile: four audio sliders, four video options, and every key in the game rebindable with bind-time conflict detection. The audio half needed a bus layout FIRST, because there was not one: every voice AudioDirector owns played on Master, so there was nothing for a slider to hold. AudioBuses now creates Sfx, Ui and Ambient sending to Master, and the director's three pools name their own bus (4 UI voices, 8 positional, 1 ambient bed), so a slider moves one layer of the mix instead of all of it. Video options APPLY rather than decorate: display mode and vsync through DisplayServer, MSAA and render scale through the root viewport. (2) THE INPUTMAP MIGRATION: Key.P, Key.R, Key.X, Key.Escape and Key.1-9 were matched literally in SkirmishLive._UnhandledInput and WASD literally in RtsCamera._Process; project.godot now carries an [input] block of 19 actions and NOTHING in the client matches a literal keycode any more except the bare modifiers (shift to queue, ctrl to assign a group), which are not actions because a modifier is not a binding. Plus the two hotkeys doc 18 flagged and the client never had: A attack-move (two-step, arm then pick the destination, because a one-step attack-move-to-the-cursor is a different order every time the mouse moves) and S stop. (3) THE LAN FRONT DOOR: host/join screens, an in-process two-client smoke path, and the HUD desync notice - see the LAN section for what is real and what is not. PROOF: ALL 24 GOLDEN HASHES BYTE-IDENTICAL against a fresh `golden 2026` diff; `git diff --stat sim/` EMPTY and `git status --porcelain sim/` empty, which is what a client-side ticket should look like; full battery green (match, determinism, selftest, replay, saveload all exit 0, plus lan 5/5 zero desyncs and campaignsave); client builds clean (the two AudioDirector warnings are pre-existing and untouched). VERIFIED LIVE OFFSCREEN, not by inspection: a temporary autoload drove the REAL scenes through REAL widgets and REAL button presses across five phases, 97 assertions, exit 0 on every one. ACCEPTANCE 1 (doc 18: "a rebound key persists across a restart"), and it is a GENUINE restart rather than a re-read in the same process: phase 1 rebinds attack-move to G through the settings scene's own row and quits the process; phase 2 is a SECOND godot launch that finds attack_move on G, finds G in the live InputMap because EnsureLoaded applied it at startup, and finds the master volume still at 0.42 and already on the Master bus. ACCEPTANCE 2 (doc 18: "a conflict is refused"): binding STOP to G is refused with stop still on S and the notice naming the binding it would have broken, "G is already ATTACK-MOVE - STOP unchanged". ACCEPTANCE 3 (doc 18: "two in-process clients complete a scripted 1v1 with zero desyncs"): a relay and two lockstep clients inside the client binary, over a real TCP socket, on skirmish-01 through the shipped BuildStartingWorld, both worlds ending on 0x4EC4DA95C8D7F31A with the relay silent. The sliders are measured against the mixer, not against themselves: dragging MASTER to 42% puts the Master bus at -7.54 dB, which is LinearToDb(0.42) to two decimals, and dragging EFFECTS to zero MUTES the Sfx bus rather than sending it -inf. THE GATE WAS MUTATION-TESTED, not trusted, on the P5-SAVE-01 precedent that a test passing first try proves nothing: (1) making ConflictFor always return null is caught by 4 assertions, one of them emergent and better than the three written on purpose - "S emitted 0 Stop commands", because with the conflict gone STOP took G and S stopped stopping, which proves the battle-side assertion is genuinely coupled to the binding rather than to a static field; (2) making Load never read the stored bindings back is caught by the 2 restart assertions and by nothing else, which is the argument for spending a second process on phase 2. Both mutants reverted and the clean run re-confirmed at 74 + 7. THE DEVIATION THAT MATTERS, and it is a control-scheme change every player will feel: WASD NO LONGER PANS THE CAMERA. It could not survive the ticket. Doc 18 Phase A specifies A for attack-move and S for stop; RtsCamera bound A and S to pan left and down; a key that both pans the camera and orders the army does neither. Pan is now four rebindable actions defaulting to the ARROW KEYS, which with the shipped graded edge band and the minimap is the classic scheme, and the settings scene carries a note telling the player exactly this and inviting them to rebind it back. A player who does will meet the conflict detector and have to move attack-move and stop out of the way first, which is the honest order to do it in. THE HOLD HOTKEY IS NOT SHIPPED, and this is a finding rather than an omission. Doc 18 asks for "A attack-move, S stop, H hold". THE SIM HAS NO HOLD STANCE AND DOES NOT NEED ONE: auto-acquisition never pursues (World.cs:1116-1137 scans for targets already inside weapon range and fires, and sets Moving nowhere; only an explicit Attack order pursues, World.cs:1109-1114, and only AMove hunts within sight), so a unit that is not moving already stands its ground and shoots what comes to it. CommandType.Stop clears Moving, HState, ExplicitTarget and AMove (World.cs:638-640), which IS hold-position. Binding H would therefore have shipped a second key doing exactly what S does while implying a stance system that does not exist, and giving it real behaviour means adding a stance to the sim: a golden-hash move needing an ADR and Luke's sign-off, which this ticket has neither of and was told not to take. Filed for the Architect as docs/questions/Q003-no-hold-stance-in-the-sim.md instead, which also flags the one genuinely absent behaviour of the three (there is no way to tell a unit NOT to shoot, and an engineer walking past an enemy it must not wake is a real scenario the sim cannot express). THE LAN SECTION, and the headline is that MULTIPLAYER DOES NOT WORK AND THIS TICKET DID NOT MAKE IT WORK. What is real: the host/join screen, the desync notice in the HUD, and a two-client smoke path that drives the shipped Relay and LockstepClient over a real socket from inside the client binary. What is NOT real: any game between two machines. THE BLOCKER IS ONE LINE AND IT IS IN THE SIM, NOT THE CLIENT - Relay binds IPAddress.Loopback (Lockstep.cs:93) and LockstepClient connects to IPAddress.Loopback taking a port and NO ADDRESS (Lockstep.cs:239-241), so nothing off this machine can reach a hosted game and JOIN BY IP has nowhere to dial. The runner's `lan` mode has always been a LOOPBACK soak, not a LAN soak; it proves the protocol, and doc 18's "a LAN lockstep layer soak-tested to zero desyncs" reads stronger than what the code can do. Adding an address parameter is additive and small and cannot move a golden hash, but it is a netcode ticket in sim/Ferrostorm.Net and it is filed as docs/questions/Q002-lan-relay-is-loopback-only.md rather than quietly taken here; the second half, driving SkirmishLive's 15 Hz accumulator from an AdvanceTick that BLOCKS on a Monitor.Wait for up to 10 seconds (Lockstep.cs:312-335), is the real work, needs a non-blocking poll or a buffering scheme with a feel cost, and is honestly beyond this session. Q002 also asks docs to correct doc 18 s1's "a LAN lockstep layer soak-tested to zero desyncs", which reads to anyone who has not opened Lockstep.cs as though LAN works and only the UI is missing; the UI is now there and LAN still does not work, because the distance was never the UI. So HOST and JOIN are DISABLED BUTTONS THAT NAME THEIR OWN BLOCKER on the screen ("needs the loopback bind lifted", "needs an address parameter on the client") rather than live buttons that fail into a socket timeout the player has to interpret. The smoke path earns its place rather than decorating: the runner's soak runs against LanWorldFactory's synthetic 20-unit world, and this runs against SkirmishLive.BuildStartingWorld on a committed .fmap, which is the world a player actually gets, so it asserts something the sim's own gate cannot. Each client loads its OWN MapData inside its own factory: sharing one across two client threads would have them reading the same object concurrently inside BuildWorld, and a lockstep smoke that races its own fixture proves nothing about lockstep. THE DESYNC NOTICE IS WIRED AND PROVEN TO RAISE AND NOTHING SHIPPED CAN SET IT: NetSession.NoteDesync(451) raises a latched red banner reading "DESYNC AT TICK 451 / this match's players no longer share a world; the result is void", legible in the capture, but no single-player mode drives it because there is no networked mode. It is built now rather than later because it is the half of LAN the PLAYER experiences and retrofitting it onto a real session later is the expensive path. The smoke deliberately does NOT write NetSession: one is a menu-side test, the other is a battle's state, and letting the first write the second is how a smoke run that desynced in the menu raises a desync notice over the next single-player skirmish. DESIGN CALLS, stated rather than buried. (a) SETTINGS IS A STATIC CLASS CALLED FROM THE SCENE ROOTS, not the GameFlow autoload doc 18 sketches: a scene launched directly never runs an autoload's peer, and Skirmish.tscn IS launched directly both by this project's offscreen verification and by the scene-direct pattern, so an autoload would leave the settings unapplied in exactly the configuration the tests use; keeping [autoload] empty also keeps the verification harness's own temporary autoload honest. (b) ev.IsActionPressed, NOT Input.IsActionJustPressed as the ticket's wording suggested: IsActionJustPressed answers for the whole FRAME, so two key events arriving in one frame would both see it true and both fire, whereas asking the event whether IT is the action is the engine's idiom for an input callback and is exactly what the literal keycode matches used to do. Input.IsActionPressed IS used in RtsCamera, correctly, because a pan is held and that is a frame poll. (c) ACTIONS ARE DEFINED ON keycode, NOT physical_keycode (the editor's default): the client's own PressKey verification hook and every rebind capture speak keycode, and one scheme end to end beats two that disagree. The cost is real and is stated: keycode bindings are keyboard-layout dependent, so an AZERTY player's defaults land on the wrong physical keys - which matters less than it would have, because every key is now rebindable, but it is a real trade and physical_keycode is the better default the day someone teaches the rebind capture to store both. (d) DEFAULTS ARE CAPTURED FROM THE ENGINE, not restated: the bind defaults come from project.godot's [input] block and the video defaults from the live viewport, so RESET DEFAULTS restores what the project actually ships rather than a second opinion that drifts. (e) Only REBOUND keys are written to settings.cfg; a file that restates every default silently pins them, and changing a default in project.godot would then never reach an existing player. Asserted, not assumed. (f) The HUD hint reads the LIVE bindings, so a player who rebinds repair to F is told "F repair"; it is built once at _Ready, which is correct in practice only because settings are reachable from the main menu alone and a battle is therefore always built after any rebind. Latent the day settings open from the pause menu. CAPTURES DROVE ONE REAL DEFECT OUT THAT EVERY ASSERTION WAS HAPPY WITH, which is the argument for looking: the settings scroller's vertical scrollbar draws OVER its content and was sitting on top of the volume readouts, clipping them, while every assertion about those readouts passed. Fixed with a right margin; re-captured; legible. A SECOND DEFECT CAME OUT OF RE-READING MY OWN DIFF after the first commit and is fixed in the second: every slider called Settings.Save() from its ValueChanged handler, and ValueChanged fires once per frame across a drag, so dragging a volume slider rewrote the config file about sixty times a second. Volume still APPLIES on every change, because the whole point of an audio slider is hearing it move, but the WRITE is now coalesced by SaveSoon into one save 0.4s after the last change, with _ExitTree flushing anything still pending so leaving the scene can never drop a write. The discrete controls (the video dropdowns, a rebind) still save immediately and correctly: a dropdown pick is one event, not a drag. Pinned by three assertions that would have passed against the bad version only by accident: nothing is on disk one frame after the drag, the file appears on its own shortly after, and what it carries is the dragged value rather than a stale one. HONEST CAVEATS. (a) No accessibility page: doc 18's M30 asks for colourblind-safe markers and this ships audio, video and controls only. The markers are a rendering change with no setting to hang on until they exist. (b) No control-scheme picker (doc 11's schemes): the rebind list is the whole surface, and one scheme with 19 rebindable rows beats two presets that disagree about what the rows mean. (c) The settings scene is reachable from the MAIN MENU only, not from the pause menu, so changing a binding mid-battle means leaving the battle. (d) Escape aborts a rebind capture rather than binding escape; CANCEL is still rebindable to any other key, but the abort had to live somewhere and every other key is a legal bind. (e) The verification autoload is deliberately NOT committed, per the DEF-04 and P5-SAVE-01 precedent; project.godot was restored with its [rendering] and [gui] sections intact and its new [input] block, the temp script and its .uid are deleted, and ~/capture.png is removed. NEEDS A HUMAN: whether arrow-key panning is acceptable as the default or whether A and S should have lost instead (this is a taste call about a scheme every player touches, and it is Luke's, not mine); whether the settings page's information hierarchy reads at a glance; the default volume mix across four buses, which no assertion can judge; and real two-machine LAN play, which cannot be judged because it cannot be run.
- [x] TICKET-P5-SAVE-01 | owner: client-engineer | Save, load and replays reach the player (doc 18 Phase D). The sim had all of this and no UI reached any of it. Now: P raises a pause overlay (OPERATIONS) in the MainMenu FullOverlay/OverlayBox idiom carrying RESUME, SAVE GAME, LOAD GAME and ABANDON OPERATION over four slots in user://saves, each a World.Save (plus MissionRunner.Save appended on one stream, the exact shape the campaignsave gate proves) with a sidecar JSON carrying map name, map path, mission index, tick, timestamp, credits and the setup fields needed to rebuild the world; the main menu gained LOAD GAME and REPLAYS; every live match and mission records through ReplayWriter to user://replays with a setup sidecar (doc 18's own prescription, so recordings survive new setup options without touching the .frep format); and playback re-simulates the stream through the live battle pipeline rather than the baked-JSON theatre, ending on an in-game verdict banner that states whether the replayed hash matched the recording. The overlay chrome MainMenu open-coded is now UplinkUi, shared by both menus, and the campaign manifest parser is now Campaign, shared by the menu and by loading a campaign save, because two parsers for one file is how the two drift apart. PROOF: all 24 golden hashes BYTE-IDENTICAL against a fresh `golden 2026` diff; `git diff --stat sim/` EMPTY and `git status --porcelain sim/` empty, which is what a client-side ticket should look like; full battery green (match, determinism, selftest, replay, saveload, campaignsave all exit 0); client builds clean (the two AudioDirector warnings are pre-existing and untouched). VERIFIED LIVE OFFSCREEN, not by inspection: a temporary autoload drove the REAL menu, the REAL pause overlay and REAL button presses (FindButton by text, EmitSignal Pressed) menu-first through nine phases, 68 assertions, exit 0. ACCEPTANCE 1: saved at tick 200, the bytes on disk load to 0xF5CF8E467A2283FC against a live 0xF5CF8E467A2283FC, and loading through the actual LOAD GAME button lands the battle at tick 200 on that same hash. ACCEPTANCE 2: a 600-tick recorded session re-simulated to 0xFD44289E9B105B79 against the live 0xFD44289E9B105B79, verdict VERIFIED, through the browser. THE FINDING, and it is a real sim defect this ticket was not looking for, filed as docs/questions/Q001-save-drops-playerfaction.md for sim-engineer rather than patched here: WORLD.SAVE DROPS _playerFaction, WHICH ComputeStateHash HASHES (World.cs:1776), so World.Load returns a world where everyone is Directorate again (World.cs:216) and its hash misses its own save. It is NOT cosmetic: faction gates what a player may build (World.cs:745 Veil Projector, :789 "not your side's hardware"), so a loaded campaign save would let a side build the wrong faction's hardware. BOTH SIM GATES MISS IT BY LUCK: skirmish-01.fmap and mission-01.fmap declare no factions at all, so their worlds are all-Directorate and the dropped byte round-trips as 0; mission-03.fmap declares `faction 1 1`, which is why driving the client over it caught what neither gate can see. Measured, not argued: live 0x820B9B21E28CAE21 against a bare World.Load 0x6DDA34B730F02E78, with entity count, all hashed entity fields, _rng.State, Tick, Winner and ShortGameEnabled compared field by field and identical, and re-applying the map's factions restoring 0x820B9B21E28CAE21 exactly. The client repairs it honestly rather than silently: ResumeFromSave re-applies the faction from the map, sound ONLY because a faction is map content (SetFaction is called from exactly one place in the sim, MapLoader.BuildWorld) and it is the same move the sim already endorses for mission tags; the workaround is commented as needing deletion when the format is fixed, and a probe assertion pins the defect so it fails loudly when it is. The gate hole is the more important half: a field no scenario can see is a field that can be dropped again. THE GATE WAS MUTATION-TESTED, not trusted, because a test that passes first try proves nothing: (1) restamping the recording off by one tick is caught by 5 assertions including ACCEPTANCE 2; (2) making ApplyFrom ignore the sidecar's starting credits diverges the replay, which proves the sidecar metadata is load-bearing rather than decorative; (3) recording the MissionRunner's commands is caught by the structural assertion but NOT by the hash, because double-issuing an identical AttackMove on the same entity in the same tick is idempotent - so that decision rests on the structural assertion, and it is LATENT: the day the sim gains a non-idempotent mission action, recording them would silently desync. All three mutants were reverted and the clean run re-confirmed at 68 pass. DESIGN, and the two subtle calls are stated rather than buried. (a) COMMAND RESTAMPING: every player order is built with Tick 0 (IssueOrder and friends all do), so RunOneTick restamps each command onto the tick it is applied on, which is the world's tick BEFORE the step - the same convention the runner's replay gate uses. This is invisible to the sim (nothing in World.cs reads Command.Tick, and ComputeStateHash does not hash it, both checked rather than assumed) and is exactly what the replay buckets on. Proven live: the 4 PathMoves issued at tick 220, the power plant at 260 and the attack-moves at 400 all land on those ticks in the .frep, no player order keeps its tick 0, and the AI's genuine tick-0 opening move is correctly left at 0. (b) MISSION COMMANDS ARE NEVER RECORDED: the same MissionRunner re-derives them from the same world during playback, so recording them would double-issue every scripted assault. Verified on mission 3, whose `elapsed 300 -> assault 1 26 24` is the only trigger in the game that makes the MissionRunner emit commands: its recording contains 0 player-1 commands and still re-simulates to the live hash. DEVIATIONS. (1) THE LIVE COMMAND ORDER CHANGED from AI, mission, player to AI, player, mission, so live order and playback order are the same order. It moves no golden hash (those are runner-driven, and sim/ is untouched) and is inert in practice today (mission commands drive player 1 and player commands drive player 0, disjoint sets), but it is a real change to the client's tick assembly and is not silent. (2) A RESUMED GAME RECORDS NOTHING: a .frep is a command stream from tick 0 and a session beginning at tick T cannot honestly be expressed in it without shipping the save alongside; the HUD says "RESUMED (not recording)" rather than leaving the player to discover it. (3) SkirmishLive.AutoStep, a static verification hook, exists so an offscreen run can measure a hash at an exact tick instead of racing the 15 Hz accumulator; the race was real and cost four false failures before it was removed. The tick body was extracted into ONE RunOneTick that _Process and verification both call, deliberately, because a test driving a parallel copy of the loop proves nothing about the one that ships. HONEST CAVEATS. (a) The skirmish AI's own wave timers (_produced, _lastWaveTick, _lastDefendTick) are private with no accessor and are NOT in the save, so a loaded skirmish resumes with the AI's cadence reset; the world hash is exact regardless, which is what the acceptance asks, but the AI will not behave as the uninterrupted run would. Stated in the save overlay itself, not just here. Fixing it needs a sim accessor and is not a client ticket's to add. (b) Replay playback shows the LOCAL player 0 fog, so a replay is spectated from one side rather than with full vision; an observer fog toggle is Phase E. (c) The captures drove three real UI defects out that the assertions were all happy with, which is the argument for looking: the sidebar stayed lit and clickable during playback offering a "PLACE >>" prompt that RunOneTick silently swallowed (sidebar now hidden in playback, hint rewritten, the call to action dropped from the toast), and a resumed mission re-toasted its whole restored message log as though it were news (restored messages are history; _seenMessages is now set past them, and a persistent objective readout on resume is a real gap left to the HUD's own ticket). (d) The load and replay boxes are fixed-size and a short list leaves dead space; the block is now vertically centred so it reads as intentional, but sizing an overlay to its content is a UplinkUi change that would touch the shipped campaign and briefing overlays and was left alone. (e) The replay browser lists the 10 most recent and says so; orphan .frep files with no sidecar are listed but disabled, because without the sidecar there is no way to know which map or which starting credits built the world they need and guessing would produce a confident silent desync. (f) A recording under 45 ticks is discarded as a mis-click rather than littering the browser. (g) The verification autoload is deliberately NOT committed, per the DEF-04 precedent; project.godot was restored with its [rendering] and [gui] sections intact and the temp script and its .uid deleted. NEEDS A HUMAN: whether the pause overlay interrupts at the right weight, whether a replay is watchable without a transport (play, pause, scrub, speed are Phase E and the recording plays start to finish at 1x today), and the Q001 ruling.
- [~] TICKET-P5-DEF-17 | owner: balance | The turtle gate: artillery beats static defence, proven per push. tools/Ferrostorm.Balance gains a STATIC DEFENCE section after the counter-triangle matrix: a fortified position (Construction Yard, 1 power plant at supply 200 >= draw 140 so DEF-10's coming 75% rule cannot silently disarm it, 2 turrets, 12 wall segments) besieged by 3000 credits of one unit type under attack-move, across three wall shapes and the committed seeds {11, 22, 33}. Hard-fails: artillery must raze the walled base within the cap with both turrets dead and >30% army retained, must breach at least one segment against a sealed wall (keeping the DEF-05 path alive), a sealed wall may buy the turtle at most 1200 ticks measured against an unwalled control, and a wall may not exceed 8.0 hp per credit. GATE PROVED TO BITE IN BOTH DIRECTIONS, not trusted: wall hp 500->5000 in a scratch build trips the delay bound (235 -> 1936 ticks) and exits 1; wall cost 100->10 trips the cost bound (50.00 hp/credit) and exits 1; both regressions reverted. PROOF: `git diff --stat sim/` empty, all 24 golden hashes byte-identical, full battery (match/determinism/selftest/replay/saveload) exit 0, two consecutive reports byte-identical apart from the date line, tool runs in 2.3s. Report + full iteration journal at docs/balance/2026-07-15-turtle-gate.md. DEVIATIONS, all needing Balance + Game Designer co-sign per CLAUDE.md A11 and none of them silent: (a) expectedBreachers is NOT the specified three-verdict hard-fail table - only the howitzer is asserted, the rifle and cannon rows are reporting-only on the faction-war precedent already in this tool, because their "MUST NOT breach" pass is manufactured by the attack-move stall-cancel rather than by balance; (b) the segments-destroyed verdicts are dropped as unmeasurable; (c) SiegeTicks is 6000, not the matrix's 3000, because the shipped siege completes at 3124-3359 so a 3000 cap fails the BASELINE. THREE REAL FINDINGS, reported rather than tuned away: (1) against artillery a GAPPED wall - the shape ADR-005 clause 6 intends - is worth EXACTLY ZERO ticks (yard falls at t=3124 with it and t=3124 without it) because the howitzer's range 9 beats the turret's range 5 and it shells over the top; a sealed wall buys 235 ticks, 7% of the siege. A gate written only on "artillery razes the walled base" would pass unchanged if barriers were deleted from the game, which is why the differenced delay bound exists. (2) The ticket's two clause-2 verdicts are mutually exclusive: segments-destroyed is set by geometry not warhead, so unsealed it is 0 for everyone and sealed the rifles out-chew the howitzer 8 segments to 3; the ticket's own arithmetic (89s per segment against a 200s cap) already contradicted its "zero segments destroyed" assertion. (3) Splash can never take a wall flush against a 2x2 turret - nearest legal wall centre is distSq 2.5 against splash 2.25 - so DEF-05's breach is the only path to a dead segment. NEEDS A HUMAN: whether a barrier that only delays the attacker who was already going to lose is the feature that was wanted; the turret, not the wall, is the number that looks wrong (2 turrets lose to equal-credit rifles while killing 2 of 15 squads); and attack-move does not prosecute a base to the finish (World.cs:1488 drops AMove permanently, leaving an intact yard 7 cells from a full-strength army), which DEF-14's acceptance test leans on.
- [x] TICKET-P5-DEF-01 | owner: client-engineer | Placement range-preview rings and correctly sized ghost/selection rings. BattlefieldView.MakeRangeRing(radius, colour) returns an unshaded alpha torus built at radius 1, so Scale IS the radius and one mesh serves every range. The ghost's own ring is a CHILD of the ghost (the cursor-follow carries it for free, no per-frame work) in ferrite gold, and every own armed structure wears a dimmer coverage ring while placing, which is the coverage-gap read that turns turret siting into a decision. WeaponOfStruct mirrors the WeaponId hardcoded in World.SpawnTurret and is commented as needing to move with it; RangeOf converts Fix64.Raw at FracBits 32, the same conversion SnapshotInterpolator.ToDouble uses. The ghost box and its centre offset now derive from World.FootprintOf instead of a hardcoded 2 and +1, so a 1x1 barrier previews at its own size on its own cell centre while every 2x2 keeps the exact position it had. The ghost tint's ValidPlacement call now passes the struct type, which for types 1-8 is what the defaulted call already meant. Selection rings derive from FootprintOf * 0.75, reproducing 1.5 for every 2x2 exactly. Clause 9: the per-frame StandardMaterial3D allocation while placing is gone, hoisted to two static readonly materials assigned by reference. Clause 6: both teardown sites (Escape, and PlaceAtCell's commit) now route through one ExitPlacement. PROOF: all 23 existing golden hashes plus DEF-06's walls line BYTE-IDENTICAL (all 24 lines of a fresh `golden 2026` diff clean); `git diff --stat sim/` empty. Verified live offscreen, not by inspection: EnterPlacement(5) gives a visible ring at Scale.X exactly 5.0 (Weapons.TurretGun.Range is Fix64.FromInt(5)) over a 2x2 ghost; EnterPlacement(1) hides it; EnterPlacement(9) gives a 1x1 ghost and no ring; after ExitPlacement the scene holds zero coverage-ring nodes.
- [x] TICKET-P5-DEF-08 | owner: client-engineer | Drag-a-line wall placement and neighbour-mask mesh selection. Press starts the line, release commits it; the run is strictly orthogonal on the dominant axis (no diagonals, no L-bends, two lines for a corner) and each cell emits an independent PlaceStructure, so no new sim command was needed. The commit is deliberately NOT filtered on ValidPlacement: the sim rejects illegal segments silently and per-segment, and a client-side filter would be a second opinion that desyncs under lockstep; ValidPlacement drives the ghost TINT only. Barriers stay in placement mode (draw, draw, draw, Escape) while every other type still exits. Sidebar gained WALL at 100, wired straight to EnterPlacement rather than QueueStructure and exempt from the ready-slot disable, because ADR-005 clause 3 gives a barrier no ready slot. Drag ghosts are pooled and hidden rather than churned; the run tints red per-segment past MaxBarriersPerPlayer and the HUD carries the running count and cost. Wall actors take a 1.0 contact blob (2.6 is sized for a 2x2) and skip the W2-05 rise tween, without which every mask rebuild would re-pop its neighbours. THE YAW TABLE, this ticket's flagged most-likely defect, and BOTH prior tables were wrong: I derived it from DEF-07's orientation contract and then VERIFIED IT AGAINST THE EXPORTED MESH BYTES rather than trusting any of the three candidate tables. Blender (x,y,z) -> glTF (x,z,-y) and the client maps sim Y to world Z, so Blender +X is EAST and Blender +Y is NORTH; the canonicals are therefore straight {E,W}=10, cap {E}=2, corner {N,E}=3, tee {N,E,S}=7, all at yaw 0. Godot's yaw sends +X to -Z (E->N->W->S), which is not a guess: the shipped hull-yaw line Mathf.Atan2(-to.X, -to.Z) is only correct under that sense and it visibly orients every unit in the game. Measured above the symmetric 0.95 footing, which would otherwise drown the signal: straight spans X 0.950 against Z 0.340 (already east-west at yaw 0), cap spans X[-0.210,+0.475] (arm east), corner reaches +X and -Z, tee reaches +X and both +/-Z but NOT -X. (1) DOC 22'S DRAFT TABLE IS WRONG ON TEN OF SIXTEEN ENTRIES, not four: it assumes a north-facing cap and the opposite rotation sense. Its variant table is correct and was kept. (2) DEF-07'S LEDGER CORRECTION IS ALSO WRONG: it proposes tee [7]=90/[11]=0/[13]=270/[14]=180, but the contract's tee omits -X = WEST, making mask 7 (N|E|S, omitting W) the canonical at yaw 0; its own corner row [3]=0 uses the same Blender+X=East mapping that forces this, so the two rows contradict each other. Correct row: [7]=0, [11]=90, [13]=180, [14]=270. (3) THE TICKET'S OWN ACCEPTANCE CRITERION IS WRONG and is knowingly not met: it asserts mask 10 (E|W) resolves at RotationDegrees.Y == 90, which would stand the straight ACROSS its own run; the mesh is already east-west at yaw 0, and the probe asserts 0. RECONCILED 2026-07-15: doc 22's criterion has since been corrected to assert 0, and it now names the perpendicular mask 5 (N|S) as the yaw-90 case and a run drawn along y as the case that catches a transposed table. This deviation is CLOSED - the probe and the spec agree, and nothing in the code moved to close it. Had the draft shipped, corner mask 6 would have been yaw 90 instead of 270, pointing its arms north and west and leaving visible gaps on the east and south sides. Clause 3 DEVIATION, and it is load-bearing: the spec's event-only _wallsDirty latch CANNOT work, because the interpolator runs eight ticks behind the sim (windowTicks: 8), so a StructurePlaced event arrives long before its segment surfaces in _view; the latch would be consumed against a view that lacks the wall and the mask would never be recomputed. The trigger is instead a cheap integer signature of the view's own barrier set (walls never move, so the live id set determines every mask exactly); the expensive rebuild still happens only on change, and the event hook is kept as a force. PROOF: all 24 golden lines BYTE-IDENTICAL, `git diff --stat sim/` empty. Verified live: a drag (20,20)->(25,20) emits exactly 6 PlaceStructure with AuxId 9 at y=20, x=20..25 in walk order; (20,20)->(23,26) emits 7 along x=20 with no diagonal; a real 12-segment run resolved middle=mask 10 straight at yaw 0, west end=mask 2 cap at yaw 0, east end=mask 8 cap at yaw 180, an isolated segment=mask 0 post; appending one segment rebuilt exactly 1 neighbour (the old cap 8 -> straight 10) and no others, asserted by count; the drag readout showed "1 WALL SEGMENTS 100 cr 1/80". A capture of a purpose-built showcase (trunk, two spurs, tee mask 11, cross mask 15, corners mask 6 and 12) shows one continuous barrier with both corners closing cleanly and no gaps or outward stubs. CAVEAT: the same-owner mask filter (two players' walls must not fuse) is code-reviewed but NOT exercised live, and the reason is structural: the two players' build radii never overlap on skirmish-01, so player 0 cannot legally place a segment adjacent to player 1's, and the AI builds no barriers until DEF-14. CAVEAT: DEF-07's own caveats are now confirmed in-game rather than only in Blender - the mid-run band does read as a dashed gold rail rather than punctuation (the doc 22 section 5 amendment blocked on Luke), and the segments render pale rather than field olive (the roster-wide drift C-05 fixes). NEEDS A HUMAN: whether drawing a line feels like drawing rather than clicking.
- [x] TICKET-P5-DEF-09 | owner: client-engineer | Box-select and mass sell or repair for barrier runs. The box-select filter admitted only mobiles, so structures were selectable by single click alone and the R and X hotkeys - which already loop over the selection and already filter to non-mobiles - could never receive more than one structure. Barriers now join the box, and BARRIERS ONLY, deliberately not all structures: box-dragging across your own base and catching the Construction Yard in a following X is a catastrophic misclick with no undo, and the reasoning is stated in the code. Mixed drags collect into two lists and prefer the mobile one, so a drag across the battlefield is an army selection and never quietly includes the fence behind the enemy, mirroring the existing single-click precedence. R and X needed no change and were verified by reading before being verified live. A mass sell past 8 structures now asks once and requires a second X within 2 seconds, because selling 40 walls for 2000 credits on a stray keypress is match-losing and the sim has no cancel. The multi-wall readout carries the two actions and the refund, which is the number the player actually needs. Box-selected walls get a ring via DEF-01's footprint-derived sizing, not just the click path. PROOF: all 24 golden lines BYTE-IDENTICAL, `git diff --stat sim/` empty. Verified live against a real 13-segment run: the box selects exactly 13 and the readout reads "13 WALL SEGMENTS   R repair  X sell   650 cr" (refund is Cost/2 per the sim's own SellStructure); R emits exactly 13 Repair with no confirmation; the first X emits ZERO sells and raises the toast, the second within 2 seconds emits exactly 13; a drag over 4 mobiles and 13 walls keeps only the 4 mobiles; a drag over the Construction Yard and the 13 walls selects exactly the 13 walls and never the yard. The confirmation toast is legible in the capture.
- [~] TICKET-P5-DEF-07 | owner: art-pipeline | Six auto-connecting barrier meshes: com_wall_post (mask 0), com_wall_straight (5/10), com_wall_cap (1/2/4/8), com_wall_corner (3/6/12/9), com_wall_tee (7/14/13/11), com_wall_cross (15), all com_ shared hardware per the CLAUDE.md prefix convention, built from the module's own box/cyl/join helpers and PAL keys only, registered in BUILDERS, baked and exported to game/assets/models/. Marked [~] not [x] because the ticket is BLOCKED ON LUKE by doc 22 section 5.3 and one acceptance criterion is deliberately unmet; the meshes are shippable and DEF-08 is unblocked. ORIENTATION CONTRACT, documented in a comment block above the six functions and load-bearing for DEF-08: a straight runs along +X, a cap's ARM points +X with its terminating block on the origin, a corner joins +X and +Y, a tee omits its -X arm; stated in Blender axes per the ticket, leaving DEF-08 to own the yaw table over the Blender +Y forward to glTF -Z conversion (this entry originally cited SkirmishLive.cs:717-719 for that conversion, a line reference that was found rotted when checked on 2026-07-15 and whose replacement rotted AGAIN within hours as SkirmishLive.cs grew by another wave, so the lesson is structural: cite that file by greppable anchor, never by line number. The durable anchors are the harvester-intake comment in SkirmishLive.cs, grep for Blender-Y-forward, and the axis-conversion step in ModelLibrary.cs's DEF-08 comment block above WallVariant, which states the mapping outright). Scale: 1x1 so pad() is NOT called; every segment spans 0.95 about its own origin. PROOF: all 23 existing golden hashes BYTE-IDENTICAL plus DEF-06's walls line unchanged, i.e. all 24 lines of golden-hashes.txt diff clean against a fresh `golden 2026` run; this ticket touches no sim file, and `git diff --stat sim/` is empty. Full battery green (match exit 0, determinism, selftest, replay, saveload). Bounds assertion passes per mesh: the largest AABB is 0.95 x 0.95 x 0.74 against the 1.0 x 1.0 x 0.8 limit. Godot reimports all six and writes .glb.import siblings; a load-all script instantiates all 27 models with 0 failures. Legal grep clean. THREE DELIBERATE DEVIATIONS, each a case where obeying the ticket literally would have broken something ratified or shipped. (1) TEAM MARK IS FERRITE GOLD, NOT SIGNAL ORANGE, so the ticket's `grep -n 'orange'` criterion fails by design. Doc 16's CURRENT text says "COMMON hardware: field olive with ferrite-gold marks", these are com_ meshes shared by both players, and the only other com_ team band in the roster (com_harvester) is already ferrite. Signal orange is the DIRECTORATE mark; baking it into a mesh both factions place would paint a Directorate stripe on a Sodality wall. (2) THE BAND IS ON com_wall_straight TOO, so the other half of that same grep also fails by design. The ticket's rule (mark only where the neighbour count is not 2) is the mechanical form of the doc 22 section 5 amendment, which is PROPOSED and blocked on Luke; section 5.3 says in terms that DEF-07's band rule changes if he rejects the barrier sub-clause and must not ship ahead of the decision. So the CURRENT one-place law governs ("team colour appears in exactly one place per silhouette, always") and every variant carries its one band. The whole difference is the single constant BARRIER_MARK_MIDRUN: flip it to False if the amendment is ratified. (3) EXPORTED VIA bake.py, NOT export_glb.py. The ticket says export_glb.py "needs no edit"; that is stale and running it would have been destructive. It still carries the container paths doc 17 line 25 flagged (/home/claude/b3d), and it iterates ALL of BUILDERS with a plain unbaked glTF export, so a naive path fix would have overwritten all 21 Wave 4 baked models with untextured ones. bake.py is the Wave 4 pipeline and takes a `--` name filter, which is what was used; `git status` confirms no existing model was touched. HONEST CAVEATS. (a) THE SIZE BUDGET IS UNMEETABLE AND THE TICKET'S PREMISE IS TWO ORDERS OF MAGNITUDE STALE: it demands each mesh be under 40 KB "since the 20 existing models total 620 KB", but the roster has been 144 MB since Wave 4 packed baked textures into the GLBs (com_factory alone is 23 MB). The six walls are 2.5 to 2.8 MB each at bake.py's default 1024px, which is exactly the roster's own W4-05 texel density for a 1x1 (1024 per cell, same as a 2x2 structure at 2048), so no bake.py edit was made. If ~16 MB for the barrier set is unwanted, adding the six names to a 512 class in size_for is a one-line change; flagged rather than decided. (b) THE MID-RUN BAND LOOKS WRONG AND THE RENDER PROVES THE AMENDMENT RIGHT: under the current law a 10-segment run wears a nearly continuous dashed gold rail along its whole length, reading as a stripe rather than as punctuation, which is precisely what section 5.2 predicted. art/3d/wall-contact.py therefore renders three frames, and the third (wall-contact-amendment.png) is the same view with BARRIER_MARK_MIDRUN False, so the blocking ruling can be made against two pictures rather than two paragraphs. It is a comparison render only; the exported meshes are the current-law ones. (c) DEVIATION, an improvement the ticket invited by calling its numbers "a floor, not a ceiling": the footing is 0.95, not the specified 0.85. At 0.85 the spans butt together continuously while the footings leave a 0.15 notch per cell, so the run stood on a dashed line of separate plates; 0.95 gives one unbroken plinth and still fits the cell. (d) FINDING FOR DEF-08, its single most likely defect, exactly as its own spec warns: the draft WallYaw table in doc 22 is WRONG on TEN of its sixteen entries. CORRECTED 2026-07-15, and the correction is spelled out rather than swapped in quietly because the first version of this finding went onto the same rock it was warning about. That version claimed the draft erred on its four tee entries alone and prescribed [7]=90, [11]=0, [13]=270, [14]=180; the prescription is WITHDRAWN. It accepted the draft's cap and corner rows because they are self-consistent under N->E->S->W, but self-consistency was never the test and both rows are consistently wrong: the draft reads this contract's "+X" as north and rotates the opposite way. Through the client's mapping (sim X to world X, sim Y to world Z, SkirmishLive.SyncActors) Blender +X is EAST and Blender +Y is NORTH, and Godot's yaw about +Y sends +X to -Z, i.e. E->N->W->S for increasing yaw, which is the sense the shipped hull-yaw line Mathf.Atan2(-to.X, -to.Z) already depends on. So the canonical cap is {E} = mask 2, the canonical corner is {N,E} = 3, and the canonical tee is mask 7, omitting WEST (the -X arm) rather than -Y. The derived table is { 0, 90, 0, 0, 270, 90, 270, 0, 180, 90, 0, 90, 180, 180, 270, 0 }, verified by measuring the exported vertex data above the symmetric footing rather than by re-reading the contract, and matching the shipped ModelLibrary.cs WallYaw byte for byte; the draft is wrong at masks 1, 2, 4, 5, 6, 8, 9, 10, 11 and 13. Its tee row is in fact its soundest, with [7] and [14] already right, and those two are exactly what the withdrawn prescription would have broken. Doc 22's DEF-08 spec now carries the derived table plus the derivation, and its acceptance criterion is fixed too: it asserted yaw 90 for mask 10, which is the straight's canonical orientation and takes no yaw, so as written it would have failed correct code. Derive from the builder.py contract comment; do not copy any table, this one included, and run `python3 art/3d/wall-yaw-gate.py`, which now machine-checks contract, bytes, code and both documents in one pass. (e) The meshes render pale rather than field olive, for the roster-wide palette drift doc 22 section 1 measures and C-05 fixes; they use the same PAL keys as every neighbour, so they are consistent with the shipped roster today and need no rework when C-05 lands. (f) The ticket title and this session's commit title both say "per faction", implying twelve; the spec body, the acceptance list and DEF-08's WallVariant table all name exactly six com_ meshes, which is what was built. (g) NEEDS A HUMAN: the contact sheet is rendered and inspected but Luke's sign-off is outstanding on the silhouette test and, critically, on the section 5 barrier sub-clause that gates this ticket. No client verification run: this ticket touches no client file.
- [x] TICKET-P5-DEF-06 | owner: qa | The walls gate: ScenarioWalls, its golden line, and the 200-structure perf gate. Nine phases, one per ADR-005 clause, all asserting: (A) upfront pay, a segment lands with nothing ready at the yard and charges exactly 100, with a negative control proving the upfront path did not leak into the sidebar flow (a real building with nothing ready is still refused); (B) 99 credits buy nothing and charge nothing; (C) the chain, with anchors chosen so each assertion has exactly ONE possible reason to pass - (8,14) is Chebyshev 6 from the yard so the yard carries it, (8,16) is Chebyshev 8 from the yard so ONLY the barrier chain at radius 2 can, (8,19) is 3 from the nearest segment and 11 from the yard so nothing can, a barrier refuses to anchor a power plant, and the yard still anchors a segment inside its own radius; (D) the cap bites at exactly 80 per player from a 120-cell grid crawl where every candidate is funded and chain-legal, so only MaxBarriersPerPlayer can stop it, and player 1's first segment still places; (E) an engineer bounces off a fence and a player left holding one wall is still eliminated with Winner resolving to the survivor; (F) auto-acquire ignores masonry at 2 cells with gun range 4 and sight 5, but an explicit order still bites; (G) the anti-turtle counter, GDD s6 line 53 made machine-checkable - a howitzer at range 8, outside the turret's range 5 and its own 3-cell dead zone, deals exactly 60 direct and exactly 30 to each orthogonal neighbour while segments 2 cells out take nothing (splash radius 1.5 proven real), breaches the line and takes nothing back; (H) the breach, a tank sealed out by one segment acquires it in under 30 ticks, destroys it and resumes its march unordered, with a negative control tank on a reachable objective that never glances at the wall (RouteExists short-circuits); (I) sell refunds exactly 50 and frees the cell. THE CENTRAL PROOF: all 23 existing golden hashes BYTE-IDENTICAL, evidenced by `git diff --numstat sim/golden-hashes.txt` returning `1 0` (exactly one added line, zero modified) and by diffing the runner's first 23 output lines against a baseline captured BEFORE any edit - both clean. One line appended, `walls 2026 0x3B29749C2B07A99B`, LF preserved. Perf gate: 600 units + 200 structures of which 160 are walls (80 per player, the DEF-04 cap), 1000 ticks, 1.719 ms/tick against the TDD s6 budget of 8, so the cap is affordable with 4.6x headroom. Full battery green (build, golden, determinism over all 24, selftest, match, replay, saveload, full default gate including lan/lanchaos/spectate/campaignsave/balance); purity grep clean; sim/ untouched by this ticket, which edits only Program.cs and golden-hashes.txt as contracted. HONEST CAVEATS. (1) The assertions were mutation-tested rather than trusted: six mutants (victory exclusion, auto-acquire exclusion, capture exclusion, the barrier-anchors-a-building exclusion, the breach branch, the cap check) were each introduced into World.cs and each was caught by the phase that claims to cover it, with the expected message; World.cs was then restored and verified identical to HEAD and the walls hash reproduced exactly. A scenario that passes first try proves nothing until you have watched it fail. (2) DEVIATION: Phase H steps 900, not the spec's 600. DEF-05's ledger entry pre-flagged this and the measurement confirms it - the tank needs about 30 ticks to close, about 510 to chew 500 hp at 15 dps, and about 118 to cross, arriving near tick 650, so 600 fails for the wrong reason. The tick cap is a measurement window, not a behavioural claim. (3) DEVIATION: phases E to H run in sub-worlds rather than the main world, because each needs a player eliminated or the map severed, which would poison the phases around it; this follows the ScenarioArtillery and ScenarioDepot precedent and every sub-world folds into the returned hash by XOR, so none can rot unnoticed. (4) THE PERF GATE WAS DISHONEST AT FIRST AND IS NOW POLICED: with real hit points the two armies annihilate each other and only 33 of 600 units survive to tick 1000, so the average silently reported the cost of a nearly empty world at a comfortable 0.986 ms/tick while claiming to have measured 600 plus 200. Hit points are now pinned so the full stated population stays on the clock, still firing and still scanning, for all 1000 ticks; the honest figure is 1.719, about 75 percent higher. The gate now fails rather than reporting a comfortable lie if the population it claims to measure is not alive at the end. It is a load rig, not a balance test. (5) Phase E pre-damages the yard via SetEntityForTest so the phase proves the victory rule rather than re-proving ballistics that DEF-04 owns. (6) The F, G and H rigs set ShortGameEnabled false: nobody in them owns a base, and stray eliminations would be noise. (7) Not covered here and left to their own tickets: the client-side placement and mesh work (DEF-07 and DEF-08) and the AI turtle doctrine (DEF-14). No client verification was run because this ticket touches no client file.
- [x] TICKET-P5-DEF-03 | owner: sim-engineer | Variable structure footprints (ADR-005 clause 1) with provably zero behaviour change. Footprint is now a per-type property: World.FootprintOf(structType) returns 1 for the barrier types (9 wall, 10 gate reserved) and 2 for everything else, and World.AnchorOf(centre, structType) replaces the CellOf(X) - 1 anchor-recovery idiom at all five sites (four in World.cs, one in SkirmishAI.cs). Block/UnblockFootprint and FootprintCentre take a size; the eight 2x2 spawners pass a literal 2. ValidPlacement gained a defaulted structType parameter, so all six existing callers keep their exact meaning without edit (FootprintOf(0) hits the 2x2 arm) and the client needed no change. The bit-identity the whole batch rests on is Fix64.FromFraction(2, 2) == Fix64.One, so FootprintCentre(a, 2) still yields exactly FromInt(a + 1); it is now asserted in the runner selftest rather than trusted, alongside FootprintOf and AnchorOf for both sizes. PROOF: all 23 existing golden hashes byte-identical, golden-hashes.txt unmodified. Caveat: the ticket's acceptance grep `CellOf(.*) - 1` now returns exactly one line, the AnchorOf definition itself, which is the canonical expression every call site was migrated to; the stricter grep `CellOf([a-z]*\.[XY]) - 1` returns nothing across sim/ and game/.
- [x] TICKET-P5-DEF-04 | owner: sim-engineer | Wall segment: the barrier entity (ADR-005 clauses 2 to 5). EntityKind.Wall = 11 APPENDED (never renumbered); struct type 9 = StructureTypeDef(100, Wall, 0), where BuildTicks 0 means the existing BuildStructure guard already refuses to queue it at a Construction Yard, exactly as it already refuses the MCV-deployed CY. Upfront-pay placement revives the pre-SIM-05 cost model: no ready slot, validate then charge then spawn. BarrierBuildRadius 2 and MaxBarriersPerPlayer 80 (derived from the TDD s6 ratified 200-structure budget, not guessed). IsStructure includes Wall so walls block, sell, repair and take damage; IsBarrier drives the three exclusions (victory test, engineer capture, combat auto-acquisition), each of which is a real break rather than a decoration. Splash and ApplyAreaDamage deliberately still hit walls: that is the anti-turtle counter. Sight = Fix64.Zero keeps 80 segments per player free in the fog pass. MapLoader can pre-place walls. No Entity field added; World.Serialization untouched. PROOF: all 23 goldens byte-identical (no existing scenario can reach a barrier branch). Caveat: the permanent scenario, its golden line and the 200-structure perf gate are DEF-06 and are NOT in this batch; behaviour was proven here by a throwaway scratchpad harness (21 assertions covering upfront pay, the 99-credit refusal, the 80-cap per player, chaining at Chebyshev 2 but not 3, a wall refusing to anchor a plant, the CY still anchoring a wall at 7, auto-acquire declining the wall while an explicit Attack chews it, elimination on a last wall, the engineer bouncing off, and sell refunding 50 and freeing the cell) which is deliberately not committed.
- [x] TICKET-P5-DEF-05 | owner: sim-engineer | Attack-move breach (ADR-005 consequences): shipped in the SAME COMMIT as DEF-04, because a wall without a breach makes the flow field return -1 map-wide for attackers and silently deletes attack-move for the AI and the human alike. RouteExists reads the deterministic flow cache; NearestEnemyBarrier scans entity order with a strict less-than tiebreak. DEVIATION FROM THE TICKET SNIPPET, and it is load-bearing: the spec's literal code sets `e.Moving = false` the moment the wall is within Sight, but this AMove block also runs on every tick where ExplicitTarget is already the wall and the wall is outside weapon RANGE (the pursuit branch leaves target < 0 and falls straight through), so halting on sight stomps the pursuit and parks the unit in the band between Sight 5 and Range 4 forever, never firing. That is the exact freeze the ticket exists to kill, reintroduced in a narrower band, and it reproduced: 0 segments destroyed in 600 ticks. Fixed by setting ExplicitTarget on sight (the fog rule is preserved verbatim: a unit never shoots a wall it cannot see) but continuing to close, letting the existing pursuit branch own the stop via its "in range: hold and fire". Verified after the fix: acquires within 30 ticks, breaches, and reaches the ordered point unaided. Architect must ratify this deviation and the clause-2 fog nuance. PROOF: all 23 goldens byte-identical (the branch is unreachable without barriers). Finding for DEF-06: its spec says "step 600" but a cannon tank needs ~30 ticks to close plus ~500 to chew 500 hp at 15 dps plus ~85 to cross, arriving at tick 622 measured, so that cap must be raised or the phase will fail for the wrong reason. Latent bug recorded per ticket clause 4, NOT fixed here: StepToward has no blocked-cell check, unreached today only because weapon range stops pursuit outside the footprint.
- [x] TICKET-P4-REV-01 | owner: design-review (new agent, charter at .claude/agents/design-review.md) | Multi-agent whole-game review: five parallel dimension reviewers (gameplay loop, UI/HUD, combat feedback, audio, content/modes) produced 85 findings, synthesised into docs/design/18-game-review-roadmap.md - a severity-graded gap register and five dependency-ordered phases (A live gameplay, B HUD/sidebar, C effects/audio, D menus/modes, E polish), each with machine-checkable acceptance criteria and an honest needs-a-human list.
- [x] TICKET-P4-GAME-01 | owner: client-engineer | Phase A - LIVE 3D GAMEPLAY. SkirmishLive binds World+SnapshotInterpolator+SkirmishAI into the 3D battlefield at exactly 15Hz (the Main.cs loopback pattern, ADR-001 respected): click/box-drag selection, context-sensitive right-click orders (move/attack/harvest), shift add+queue, mirrored starting forces. ViewEntity extended with UnitType/MaxHp (Presentation only; spectate + goldens green). Battlefield visuals extracted to shared BattlefieldView.
- [x] TICKET-P4-GAME-02 | owner: client-engineer + ux | Phase B core - the uplink sidebar: structure queue at the CY under pay-as-you-build, ready slot to PLACE button to grid-snapped ghost tinted by ValidPlacement, unit production at the Factory, credits/queue readouts. QueueContents read-only accessor added to World (M3 per doc 18, pure read, goldens byte-identical). Verified end to end through the real code paths: queue power plant (8000 to 7700 exact), ready, placement-scanner found (6,28), placed, plants=1 in world.
- [x] TICKET-P4-GAME-03 | owner: audio (agent) | Phase C audio - 13 SFX synthesised offline from pure stdlib (no assets downloaded, nothing evoking C&C/EVA per charter): weapons, explosions, UI, alerts, superweapon pair, seamless ambient loop; AudioDirector with 2D/3D voice pools wired to orders, builds, placements, and the event stream.
- [x] TICKET-P4-GAME-04 | owner: client-engineer (agent) | Phase C effects - CombatEffects consumes the per-tick GameEvent stream: muzzle flashes + tracers on Fired, death flash + smoke puff on Died, superweapon shockwave ring + column on Impact, production chime; pooled shared materials, self-freeing tweens.
- [x] TICKET-P4-GAME-05 | owner: tools (agent) | Skirmish-02 "The Cauldron": 180-degree point-symmetric map (provable fairness, equal 11.66-cell start-to-ferrite), validated by AI-vs-AI harness (500-tick treasuries byte-identical, 3000-tick full military activity).
- [x] TICKET-P4-GAME-06 | owner: client-engineer + ux | Phase D core - main menu in the uplink style (theatre picker enumerating data/maps, AI temperament, treasury options) flowing into the battle via MatchConfig; verified by pressing the real COMMENCE button headless and asserting the battle scene loaded with the chosen credits.
- [x] TICKET-P4-GAME-07 | owner: client-engineer | Phase B remainder DONE (except hotkey InputMap pass): fog of war as a shroud plane rebuilt per tick from the sim's public visibility bitgrids (near-black unexplored, dimmed explored, soft linear-filtered edges; enemy actors hidden unless their cell is visible); minimap compositing terrain + the same fog image + entity dots + camera marker with click/drag-to-fly; power readout summed client-side from own entities (no new sim accessor needed - PowerSupply/PowerDraw are public Entity fields) with a LOW state; ctrl+digit control groups; base-under-attack klaxon on a 12s cooldown. Ground plane trimmed to exact map bounds so the shroud covers every lit surface. Verified offscreen: a scouted vision corridor, both pools, the explored trail, and the minimap mirroring it. Remaining niggle: attack-move/stop hotkeys and an InputMap-based binding pass stay open under P4-GAME-08.
- [~] TICKET-P4-GAME-08 | owner: client-engineer | Phase D remainder. Campaign flow DONE: the menu gains a CAMPAIGN screen parsing data/campaign/campaign.txt, briefing screens render the story files, and SkirmishLive gains mission mode - the map places both forces, MissionRunner ticks beside the loop with its command output merged into the next tick, trigger messages surface as fading objective toasts, and scripted victories land through World.Winner polling (missions declare wins ahead of the short-game rule, so elimination events alone are not enough). Per-mission setup mirrors the gated scenarios (mission 1: 5000 credits + CY; mission 3: 4000 credits). Verified headless: menu > campaign > briefing > commence walked by pressing the real buttons, mission live with the exact scripted credit grant and the map-placed strike force. STILL OPEN: save/load UI, replay recording + browser, LAN host/join, settings scene, InputMap hotkey pass; Phase E polish thereafter per doc 18.
- [x] TICKET-P4-GFX-01 | owner: ux + art-pipeline | UI identity: all 15 sidebar build items carry sprite icons (art/png set downscaled to game/ui/icons), and Saira Condensed SemiBold is the project default font with IBM Plex Mono vendored beside it (SIL OFL, licence texts committed) per doc 18 Phase B. Campaign tech gating (manifest v2 allow-list columns) shipped the same day: mission 1 early tier, mission 2 nothing buildable, mission 3 adds depot/cannon/howitzer/engineer; hidden not greyed; queue paths guarded; verified 8 visible/7 hidden and a refused disallowed queue.
- [ ] TICKET-P4-GFX-02 | owner: art-pipeline + client-engineer | Turret tracking. Prerequisite: builder.py currently join()s each model into ONE fused mesh, so turrets cannot rotate. Change the tank/turret builders (cannon, bulwark, turret structure) to keep the turret assembly a SEPARATE object parented to the hull (glTF preserves object hierarchy when objects are not joined; name the node "turret"), re-run bake.py (bake per object or bake the two objects into one atlas), re-import in Godot (wipe .godot/imported if stale-cache symptoms appear - see session 18 notes), then in the client slew the "turret" child node yaw toward the entity's engagement target (drain from Fired events or ExplicitTarget once ViewEntity carries it) with LerpAngle at ~4 rad/s. Verify with the offscreen capture trick: two tanks ordered to attack in opposite directions must show hulls and turrets at different yaws.
- [ ] TICKET-P4-GFX-03 | owner: client-engineer | Damage states: smoke GPUParticles emitter attached at <50% Hp (ViewEntity has Hp and MaxHp already), flame at <25%, wreck on death (charred material swap under a recycling cap) with scorch decal - doc 18 Phase C list, acceptance criteria there.
- [x] TICKET-P4-SLICE-01 | owner: all agents | THE VERTICAL SLICE: dir_vanguard_car developed end to end through every pipeline layer in one pass - design YAML, catalogue type 12, its own VanguardGun weapon (id 7), balance matrix expectations, four forge iterations to a gate PASS (journal in docs/balance/2026-07-14-vanguard.md, including a genuine harness find: per-unit Attack orders could deadlock after a partial wipe; armies now AttackMove to muster), a wheeled model whose TURRET IS A SEPARATE CHILD NODE (bake.py learnt children; the glb carries the named hierarchy), sidebar entry with a rendered icon, and live client turret tracking (Fired events cache aim, the turret child slews toward it and returns to centre as the memory fades). Verified end to end in-game: factory built and placed through the sidebar, vanguard produced, attack-move hunt, FIRING capture with tracers at t=59s. Goldens byte-identical throughout - the proof that new content costs nothing until a match spawns it.
- [x] TICKET-P4-TER-01 | owner: tools + client-engineer | Terrain vocabulary: map format visual classes aliasing exactly to existing passability (w/h/r/f blocked, B bridge OPEN), client renders water/hills/ruins/fences/bridge decks, skirmish-03 "The Crossing" showcases a river with two bridge chokepoints (probe unit verified pathing across), mirrored hills, ruined village, fenced compounds. Goldens unchanged; existing maps parse byte-identically.
- [x] TICKET-P4-UX-01 | owner: ux + client-engineer | Structure interaction: click-select own structures (ring on demand), selection readout naming the selection with hp and context actions, factory rally points (right-click with factory selected, gold marker, fresh units PathMove there on ProductionComplete), R repair toggle and X sell exposing the sim commands, updated hint bar.
- [x] TICKET-P4-W3B | owner: ux + client-engineer | WAVE 3b UI JUICE, tickets W3-15 to W3-21 of doc 20 (the sidebar finally talks back). Sidebar build feedback: Refresh now takes full queue contents plus the head's build fraction (BuildProgress over BuildTicks x 100, pure post-Step reads), a ferrite-gold overlay sweeps across the queue head's button as it builds (measured 56 to 90 px between samples, back to zero at ready) and per-type count badges track the queue (x3 to x2 observed as the first rifle squad completed). Pressed and disabled button states replace the old hover-reuse (disabled reads clearly darker with dim text in captures) and the PLACE button pulses gold until the structure is placed (modulate observed animating, white and tween-free after placement). Order acknowledgement: right-clicks spawn a contracting emissive ground ring at the order point, bone for move, signal orange for attack (the ring sits on the target itself), ferrite gold for harvest and rally; one deviation from the spec's one-liner: FadeAndFree().Parallel() would have parked the contraction in the QueueFree callback's step after the fade, so the marker builds an explicit parallel tween instead (same visual grammar). Edge pan: the four binary 6 px tests became a graded 24 px quadratic band with a 0.35 s speed ramp from a 40 percent start and LimitLength preserving the analogue weights (deep-band displacement 9.3 units vs 0.3 shallow; ramp second window 6.4 vs 3.0 first). Production toasts: own completions slide and fade a gold flyout left of the sidebar (POWER PLANT READY and RIFLE SQUAD DEPLOYED both captured, hidden again by 3.5 s). Minimap: expiring alert pings (2.4 s life, cycling every 0.8 s) wired to base-under-attack and superweapon impact, and the camera dot became the true view-frustum trapezoid from the four ground-projected viewport corners. Two finds beyond the spec, both fixed: the minimap's own _Draw renders beneath its terrain and fog TextureRect children, so dots, pings and the in-rect frustum were invisible until the children got ShowBehindParent (the entity dots have plausibly been buried since the fog landed), and the frustum spills past the map edge at base viewpoints so the control now clips its contents. Sound variety: Play and PlayAt gained a pitch argument that always writes PitchScale (a pooled player reused after a 1.08 call read exactly 1.0 again), plus a Jitter helper (client-side System.Random, legal outside /sim) applied to select clicks, move orders, weapon fire and death explosions on the W3-01 weapon-class call; ui_confirm, alerts, ambient and superweapon signatures stay at pitch 1.0. Honest caveats: pitch variety is verified programmatically, not by ear (Dummy audio driver); the attack-ring colour was checked through the public OrderMarker call rather than a live enemy under the cursor; the base-under-attack and superweapon ping paths are code-wired but the capture exercised Ping directly at map centre. Verified by a temporary C# autoload driving the public API through the real build-place-produce loop, 27 of 27 programmatic checks PASS across two runs, fully torn down. Full battery green, golden 2026 hashes byte-identical, no sim file touched. MainMenu.cs untouched: the animated menu backdrop is W5-04, outside this wave.
- [x] TICKET-P4-W4C | owner: client-engineer | WAVE 4b GROUND PASS, tickets W4-10 to W4-18 of doc 20 (70 percent of every frame stops being one flat material). Heightfield ground: the PlaneMesh is now a generated ArrayMesh at 2 verts per cell displaced by a shared GroundHeight sampler (FastNoiseLite seed 7134, amplitude 0.075, max sampled 0.053), shores and bridge approaches held level by the flat-cell mask, actors in SkirmishLive and ReplayTheater ride the undulation (18 of 18 programmatic checks green, all mobiles within 0.045 of the sampler; stride bob now adds GroundHeight so bobbing infantry stay grounded on slopes). Three necessary deviations from the mechanical spec, all verified: Godot front faces wind clockwise so the winding is the spec's documented swap; the spec's vertex arrays carry no tangents, which NORMAL_MAP needs, so a flat plus-X tangent array is added; and water cells could not sit in the flat mask at 0 or the new ground sheet roofs the slab (top -0.07) and the river renders as dust, so GroundHeight returns a -0.22 basin under water (slab bottom -0.17, no actor ever stands there) and The Crossing reads as a proper recessed river again. Splat shader: game/shaders/ground_splat.gdshader exactly per spec, three layers blended by world-space masks; capture shows distinct cool dust, pale warm rock and darker gravel patches at the specified scale with fine grain at 200 percent zoom and no visible tiling; the six colour uniforms await the W5-02 biome retint. Rubble: the 220-node loop is one MultiMeshInstance3D at 2200 instances plus 255 talus instances folded in (exact-count list rather than 9-per-cell zero-scale, mechanically simpler); node count 584 to 816, well under the 1500 bar. Cliffs: rim cells carry base stratum, mid stratum and a dust-capped slab from the splat material over a per-cell deterministic rng, interiors raised; stepped strata and debris skirts confirmed in capture, though the interior-taller read is only weakly judgeable under fog and the strata read dim at vision edge. Shores: world-triplanar water kills the per-cell UV seam (confirmed across the revealed river), wet ramps slope the correct way (high edge at the ground lip) with foam lines; both read subtle at RTS height. Scatter: Tufts, Rocks and Plates MultiMeshes (900/350/250, zero-scaled rejects); honest caveat: the crossed-quad tufts read as small dark scrub marks from the top-down camera, not the pale clumps the acceptance imagined, and the tuft ramp's dark point needed alpha 0 or the alpha scissor would never cut (spec colour had alpha 1); plate and rock materials are constructed inside BuildScatter with the spec's GrainTex recipes to keep the spec'd signature. Tyre tracks: pooled fading decals behind vehicles, cap 96 held, hidden-cell movement prints nothing, infantry print none, with unitType 0 added to the exclusion because the mirrored starting squads spawn untyped (the W4B ledger find) and would otherwise drag tread trails from the opening seconds; one recorded knob iteration, texture alpha 0.38 to 0.62, because the spec value was invisible under the dusk AgX grade (the 0.65-alpha contact blobs bounded what reads); the trail is legible at close zoom, near subliminal at maximum height. Ferrite stain: cached radial amber decal with matching emission under every deposit in both scenes, confirmed pooling in captures; the mining shrink rides the existing Hp-driven parent scale mechanically but was not exercised live (the probe has no refinery, the W3 lesson). Bridge deck: plank streaking via anisotropic Uv1Scale plus piers, cross-beam and kerb posts per cell; streaking and posts read clearly in capture, the piers are mostly hidden from the RTS camera angle and read only as under-deck darkening. Frame time: matched A and B runs at the same viewpoint pin the same vsync cadence (60.0 to 60.0, and 115.8 in the one 120 Hz window against a 119.8 baseline), so no measurable regression, within the limits of cadence pinning. Verified via a temporary autoload driving SkirmishLive's public API on skirmish-01 and skirmish-03, fully torn down. Full battery green, golden 2026 hashes byte-identical, no sim file touched.
- [x] TICKET-P4-W4B | owner: art-pipeline | WAVE 4b model rebuilds, tickets W4-07 to W4-09 of doc 20 (the ferrite finally reads gold). ferrite_cluster rebuilt as seven faceted truncated five-sided shards in non-emissive gold ferrite with small emissive fhi tips (emit 2.4, the flagged Wave 5 taste knob) plus a cinder rubble ring; the old whole-emissive cones could never read as anything but clamped white. One necessary deviation beyond the doc's mechanical spec: raw primitive cones put every vertex on a sharp edge and the weathered material's pointiness chip mask interpolates grey across whole faces (bodies baked at R/B 1.15 against the palette's 1.42), so shards and rubble carry a bevel modifier plus subdivision to confine chips to edges (measured back at 1.43). Verified in the live client at maximum zoom: cluster body pixels R greater than G greater than B with an R minus B gap of 0.183 against the 0.15 bar, tips blooming, Hp-driven scale intact. Construction yard gained four gantry legs, four cross braces, a ferrite trolley stripe, a second drum and a pallet: the crane now visibly stands on legs instead of floating (capture confirmed). Power plant gained four wall pilaster ribs, two roof vents with an elbow pipe, a glow heat rim on the cooling tower, and louvres inset from 0.37 to 0.35 with a dark recess behind; ribs and inset louvres read clearly at game zoom. Infantry rebuilt with slimmer bodies, backpacks, cross-held rifles at per-man facings and a smaller base disc (heads lowered 0.025 to stay seated on the shorter bodies, a spec gap); honest caveat: at MinHeight 8 the men are roughly eight pixels tall so the rifle and pack read is marginal on screen, though squads byte-differ, read as varied rather than uniform pill-triplets, and the dot-cluster law holds. Harvester gained the intake drum with eight drum teeth built inside the de-merged intake child per the integration note (joined with the intake box active so the W2 churn hinge origin survives, confirmed at translation 0, 0.11, 0.56 in the glb), a glowing ore heap in the hopper and rear mud flaps; caveat: the heap core clamps white through the LDR emit map (the documented Wave 4a limitation), gold shows mainly in the bloom fringe, R above 0.6 with bloom confirmed across the hopper region. Six models rebaked at their class resolutions, 21/21 clean reimport after a cache wipe, no loose PNGs, import files still pin mode 3. One quiet find: the mirrored starting squads spawn with default unitType 0, so FindOwnUnitPos(2) cannot see them (worked around in the probe, may bite future scripted verification). Full battery green and golden 2026 hashes byte-identical (art plus a temporary, fully torn down verification autoload; no sim file touched).
- [x] TICKET-P4-W4 | owner: art-pipeline | WAVE 4 the material wave, bake-pipeline tickets W4-01 to W4-06 of doc 20 (sub-session 4a's re-bake half; the three model rebuild passes W4-07 to W4-09 and the 4b ground-surface tickets W4-10 to W4-18 remain open). Every model now exports a full PBR set instead of flat diffuse plus roughness: real per-object EMIT bakes with black-detection so non-emissive models carry no wasted texture (glow lands on exactly the models with lit parts, engineer/infantry/yard/turret/depot correctly stay dark), exported at Emission Strength 2.0 through KHR_materials_emissive_strength which Godot 4.7 imports; emissive detail parts across the roster in builder.py (glow and beacon palette entries, emit kwarg on box/cyl, Directorate glacis headlights, lit cab screens on harvester/MCV/vanguard, Sodality signature strips, refinery and superweapon cores, veil orb and rings, power plant window strip and stack beacon, factory chimney beacon and door lintels, and the one team-colour place is now self-lit); AO baked at 64 samples and multiplied into the diffuse in numpy at k 0.85; tangent normal maps baked from a bevel-shader plus brick-seam plus micro-grain chain added to materials2.wmat (every glb now carries normalTexture and panel seams read at close zoom); per-class resolution 2048 structures / 1024 vehicles / 512 infantry with bake margin scaling (all 8 structure glbs grew, all 3 infantry shrank); and true ORM packing with metallic baked through an emission value bridge, wired Green to Roughness and Blue to Metallic through one Separate Colour node so the exporter writes metallicRoughnessTexture, occlusion exported via the glTF Material Output group trick (occlusionTexture confirmed in every glb), and the flat Metallic 0.25 line deleted. One new Blender 5 trap for the doc 17 collection, found after the first test bake exported a pure black normal map: bake results live in the image's unsaved buffer and the NEXT object's bake silently drops any such buffer, so bake.py now packs every image immediately after baking. Also fixed dir_vanguard_car.glb.import still carrying embedded_image_handling 1 from the Wave 2 rebake, which was extracting loose texture PNGs next to the glbs on every import; it now pins mode 3 like the rest and the strays are gone. Full roster rebaked and reimported clean, 21/21 glbs load (the docs' habitual 20 undercounts the roster). Verified by before/after offscreen captures of an identical scripted skirmish opening plus Blender re-import renders. Honest verdict: the AO/crevice read is the visible win at RTS height (construction yard halls separate into slabs with contact shadow, harvester gains a dark track base and distinct hopper, power plant tower ribs and hall junctions shade properly); normal-map response reads at MinHeight zoom but is subtle at the default height; emissive windows and beacons render correctly but are sub-pixel at standard zoom in the dusk scene, and whole-emissive surfaces saturate toward white because the emit map is stored LDR (colour times strength clamps at 1.0 per channel), so ferrite_cluster still reads white-hot rather than gold; that read is exactly what the W4-07 rebuild and a Wave 5 taste pass are scoped to fix. Cost noted: structure glbs are now 8 to 23 MB each and the models directory is roughly 150 MB. Battery green and golden 2026 hashes byte-identical (no sim file touched).
- [x] TICKET-P4-W3 | owner: client-engineer | WAVE 3 the VFX wave, tickets W3-01 to W3-14 of doc 20 (3a combat density plus the camera half of 3b). Every weapon class now has its own effect family, keyed on the sim WeaponId through a weaponOf resolver added to CombatEffects.OnTickEvents (post-Step entity read, rally-code precedent, sim untouched): thin rifle tracers against a three-round autocannon burst, travelling emissive cannon shells for weapons 1/4/6, rockets on a shallow bezier trailing world-space smoke that survives the detonation via Reparent, and howitzer rounds on a 0.9s high arc ending in a dirt burst plus a ring sized to the sim's 1.5-cell splash. Impacts read on the receiving end too: additive spark fans on armour, dirt kicks on ground, and a 6 percent scale pop on the struck hull (meta-flagged against stacking, guarded against mid-rise structures). The battlefield keeps its history: procedural scorch decals in a 48-slot recycling ring buffer (oldest fades before repositioning), burning wrecks on big deaths (fire core with colour-over-life ramp, smoke column, warm light, capped at 10 concurrent, full teardown by 8.5s), two-tier damage smoke below 50 and 25 percent hp capped at 40 emitters with repair recovery, and a ferrite-gold mote stream at the harvester intake while loading. The superweapon got its full sequence: pulsing charge orb on the dish from launch to impact, a 40-unit sky beam, expanding flash, ballistic debris, an 8s dust pall and a six-unit scar (stage timing flagged for the Wave 5 taste pass). Camera juice landed as the canonical implementations: exponential position smoothing chasing a bounds-clamped target with FlyTo glides from the minimap, wheel zoom that slides along the cursor ray with exact height clamping, trauma-pool shake (distance falloff routed through a CombatEffects.Shake helper per integration rule 1) applied via H/VOffset so it never fights the smoothing, and billboard health bars above damaged or selected mobiles (left-anchored fill, green/amber/red by fraction, no team colour per the doc 16 one-place law). Muzzle omni lights cap at 8 alive; the billboard quad always spawns. Verified end to end by a temporary autoload driving a scripted mixed-weapon firefight, superweapon strike and harvest run: 40 programmatic checks PASS with zero engine errors (shells tracked moving between frames, howitzer arc height range confirmed, trail reparent survival seen, hit pop peaked at 1.059, every cap held, charge orb and beam and dust and gold motes all confirmed in captures). Full battery green and golden hashes byte-identical. Two probe lessons for the pattern book: the OS cursor parked in a screen corner edge-pans the camera and fights FlyTo probes (warp it to window centre first), and the sim's Harvest command is a silent no-op for a player with no refinery.
- [x] TICKET-P4-W2 | owner: client-engineer + art-pipeline | WAVE 2 milestone 1 (doc 20 motion): ActorRig scan infrastructure generalises the turret finder to named animation children (turret/wheel*/dish); turret RECOIL on Fired (0.055 kick along local backward, 180ms cubic spring home - verified live: RecoilActive(12) observed the frame the vanguard fired); wheels spin from travel speed; the scout dish idles. builder.py gained child_part() (bakes object rotation into the mesh so client spin axes are clean) and de-merged: vanguard wheels x4, cannon + bulwark turret assemblies, scout dish - all four rebaked, hierarchies verified in the glb JSON, clean reimport after a cache wipe. The cannon and bulwark now track and recoil with zero further code (the coverage-grows-automatically promise of W2-01 kept). Milestone 2 completed the wave: hull pitch spring from acceleration (critically damped, clamped 0.10 rad); structures placed mid-match RISE from the ground (Back.Out scale tween, initial map spawns exempt); dead mobiles tumble and sink as short-lived corpses while structures collapse downward (self-freeing tweens - the removal path now detaches rather than instantly frees); infantry stride-bob while moving (types 2/3/11, per-id phase); the harvester intake churns en route (de-merged child, agent-rebaked); factory doors slide open for 1.4s on ProductionComplete (de-merged door0/door1 children). Verified in one integrated run: placement rise, production door cycle, mixed-force hunt with pitch and bob, clean log. MCV unfold deferred: the Deployed event swaps entities, so the CY build-up rise already reads as the unfold.
- [x] TICKET-P4-W1 | owner: client-engineer | WAVE 1 of doc 20 SHIPPED (all 12 tickets): key/fill/rim light rig replacing the twin white suns (ReplayTheater gains directional light for the first time), SSAO+SSIL grounding, AgX tonemap with grading, procedural storm sky killing the black void, volumetric fog the muzzle lights play in, SSR with opaque scrolling water, 2-split soft shadows + 4x MSAA, glow retuned to tight halos (bloom 0 - the old muddy orange wash is gone), shroud rebalanced for night readability, tilt-shift far DOF with auto-exposure explicitly off, contact-shadow blobs under every actor, and camera-following ambient dust motes. Two knob iterations recorded per the doc's rules: fog density 0.03 to 0.022 (milky wash) and ambient inject 0.35 to 0.08 + sky affect 0.3 to 0.1 (the volumetric fog was scattering blue over the shroud - dark regions must stay dark). One binding correction: VolumetricFogGiInject does not exist in the 4.7 C# bindings, dropped (SDFGI-only anyway). Before/after: the same vanguard firefight viewpoint went from black-void diorama to storm-lit battlefield with halo tracers. Wave 2 (motion) next per doc 20.
- [x] TICKET-P4-PORT-00 | owner: architect | UE5 portability hardening: ADR-004 (engine strategy - Godot now, UE5 later, renderer-swap-never-rewrite, migration triggers and spike plan) ratified; docs/design/21 audits the commercial-studio spec against the build (verdict: the core demand has been law since ADR-001; zero engine references outside /game, now CI-ENFORCED by a second grep failing any 'using Godot' in sim/, tools/ or data/). Six portable-surface gap tickets filed (patrol/guard, aircraft study, sim tech tree, multi-resource, formations, faction recipe) - none are migration blockers.
- [x] TICKET-P3-VIS-06 | owner: client-engineer | Game-feel pass in the Godot client (presentation only, sim untouched, ADR-001 respected - golden hashes byte-identical after). Movement: mobile units now yaw into their direction of travel at a finite exponential turn rate instead of sliding sideways (model forward is -Z after the Blender-to-glTF axis conversion, so the desired yaw is atan2(-dx, -dz)). Readability: mobile units carry an emissive team-colour ground ring (Directorate signal orange vs Sodality corroded teal) - the one-place team-colour law applied at the presentation layer, and vital because both armies field identical common hardware. Landscape: noise-textured cinder ground with a colour-ramped palette band (raw FastNoiseLite spans black-to-white and reads as static; NoiseTexture2D.ColorRamp pins it to the world's materials), normal-mapped surface grain, Spine ridge cells with deterministic per-cell height/rotation jitter so the wall reads as geology, and 220 pieces of rubble scatter kept clear of blocked cells. Atmosphere: WorldEnvironment built in code (ACES tonemap, glow bloom that picks up the emissive ferrite for free, exponential distance fog with sky blend) plus shadow-enabled sun in Battle3D.tscn. The client is presentation-only so System.Random is fine there - the determinism laws bind /sim alone. Verified by offscreen captures of the running client: base with shadows, ringed units mid-march, harvester oriented toward its field, glowing ferrite. One process lesson recorded: a stale-path rm let a poll copy the previous run's capture and two "identical" renders sent the session chasing a phantom regression - byte-compare captures before diagnosing, and delete the exact artefact path the app writes.
- [x] TICKET-P5-ALERT-01 | owner: client-engineer | Harvester-under-attack alert (GDD s2:19, s7:85). Live play-test of skirmish-04 lost a harvester and three squads to nine AI waves in total silence: the only alert required a STRUCTURE to be hit. The alert now has its own cooldown so a harassed harvester can never swallow "base under attack", an amber ping against the base alert's red, a pitch-shifted cue (the audio pipeline still owes a distinct asset), and both alerts gained visible toasts - the old alert was audio plus a 1.5px ping and a watching tester missed nine waves. Verified live: HARVESTER UNDER ATTACK at tick 2668 with the harvester still alive (previously died 2711 unannounced), BASE UNDER ATTACK at tick 3095. Client-only; goldens byte-identical. The DEFEAT that prompted the session was proven NOT a bug by six independent harnesses plus ablation: the STANDARD AI legitimately beat an idle player at tick 4192; recorded in docs/tickets/P5-ALERT-and-balance-findings.md so it is never re-opened.
- [x] TICKET-P5-AUDIT-01 | owner: game-designer + architect | Doc 23, the base-systems roadmap (630 lines, 30 tickets, 7 waves), answering Luke's four play-test questions with a four-lens audit plus a four-sceptic adversarial pass. Headline finding nobody asked for: the shipped client NEVER LOADS /data - every loader caller is in the gate harness, so a live match runs off the compiled catalogue and a YAML edit changes nothing a player feels (ADR-006). The sceptics killed the obvious spawn fix before it shipped: measured, it drains the treasury at ~3000 cr/s and bricks the factory after eleven units, so rally lands BEFORE occupancy (doc 23 s7 records the inverted order). Also: units spawn stacked on one identical cell forever; a fully blocked spawn ring deletes the paid unit silently; power meets two of GDD s5's five rules; no radar exists anywhere; the Veil Projector was unbuildable by any human; prerequisites were parsed then discarded.
- [x] TICKET-P5-W1 | owner: client-engineer | WAVE 1 of doc 23 SHIPPED (all 15 tickets, single agent, 62+11 offscreen assertions): R stops lying (partitioned selection, chime only when a structure is repaired, mobile-only selections told to use a depot), hint bar reads "repair bldgs", per-structure stall readout (charging is entity-index order, so a global credit test would lie), structure health bars (footprint-wide, walls only when selected), amber repair rings, R routes damaged vehicles to the nearest powered depot with honest refusals (and found doc 23's own spec wrong: the 4-cell crowd-arrival radius equals the aura radius, so "2 cells out" strands combat units unhealed - fixed with a half-cell-past-centre aim plus per-order jitter), set-not-toggle multi-repair, wall-run repair guard (80 walls is 1200 cr/s, not "cheap"), DepotRepairRadiusCells named in World.cs (the wave's only sim edit, bit-identical), depot removed from Ralliable, rank pips (elite self-heal was invisible), truthful placement ghost (credits and wall cap), deploy-blocked toast, edge-triggered low-power alert at the 75% crossing, and the Veil Projector's first-ever sidebar button (faction-gated; note no shipped map declares factions, so only campaign grants can surface it - Q001's map-header path proven offscreen). Goldens byte-identical; CI green.
- [x] TICKET-P5-W2 | owner: sim-engineer | WAVE 2 of doc 23 SHIPPED (all 4 tickets): ComputePower factored with the PlayerId bounds guard, three call sites kept deliberately separate (collapsing them moves hashes - the doc's trap), third tally added in CombatSystem feeding nothing until PWR-04 (perf delta ~0.03 ms/tick against the 8 ms budget); AnchorOf corrected to centre-minus-half-footprint, bit-identical for 1 and 2, proven by the goldens, with a footprint-3/4 selftest round-trip - big buildings are now legal; MapLoader gained the missing ServiceDepot arm; catalogue loops bounded by MaxStructType and skipping the reserved gate type 10 (where ADR-005's reservation actually bites); the unit round-trip became a directory walk and immediately caught dir_vanguard_car.yaml naming a weapon the loader never knew - the exact failure mode the hand-enumerated check had been hiding; EntityKind 12-17 appended, struct types 11/12 compiled with com_barracks.yaml and com_radar_uplink.yaml authored, produced_at and prerequisites carried through schema and all twelve unit YAMLs, read and branched on by nothing. Goldens byte-identical; CI green both platforms.
- [x] TICKET-P5-SPAWN-03 | owner: client-engineer | Deploy control SHIPPED. Wave 1 verification found no client code ever issues CommandType.Deploy: a human could buy the 3000-credit MCV, drive it anywhere, and never unpack it - only the AI could. Now: a `deploy` InputMap action on D (the rebindable surface is 20 actions as of this ticket), a double-click path that falls through cleanly for non-MCVs, the single-MCV readout advertising the live binding, and the SPAWN-02 blocked toast inherited by both paths. 31 offscreen assertions including a full rebind-to-J round trip. Client-only; goldens untouched; CI green.
- [x] TICKET-P5-Q001 | owner: sim-engineer | Q001 RESOLVED: World.Save dropped _playerFaction, a field ComputeStateHash hashes, so any loaded save was silently all-Directorate and missed its own hash whenever a faction was non-zero - both gates passed by luck because their maps declare no factions. Save format bumped to v2 (faction array per player); v1 saves still load with their historical behaviour; the client's documented ResumeFromSave workaround removed; the saveload gate now round-trips a NON-ZERO faction and asserts hash equality, so the regression is impossible to miss again. Riders: the structure selection readout now prints live bindings instead of hard-coded "R"/"X" (would have lied after a rebind), and SnapshotInterpolator's comment stops promising newborns render a tick earlier than the code delivers. Goldens byte-identical; CI green.
- [x] TICKET-P5-DECIDE | owner: architect + game-designer + legal-review | The decision queue converted from prose owed in doc 23 s8 into ratifiable documents: ADR-006 (/data becomes the runtime source, lobby catalogue checksum priced in, recommendation stated), ADR-007 (rally into the wire and save format, both measured Wave-4 traps written into the ADR so no implementer can re-invent them), ADR-008 (power's teeth: turret gate reading CombatSystem's staged spans, refinery and yard draw for A11 co-sign, radar blackout on struct type 12, the walls-gate phase G assertion protected from rotting), ADR-009 (the barracks split, AI taught to build one so it cannot regress to rifles-only, airfield explicitly out until the air-layer ADR), all Proposed awaiting Luke; plus Q006-Q011 (tech centre vs radar uplink, engineer's producer, day-one radar blackout, depot stacking, the invented full-power cliff vs GDD's sell-to-sneak, and trademark scoping for internal documents - the product surfaces are clean). Docs only; each unblocks its wave on a one-word ratification.
- [x] TICKET-P5-AMOVE-01 | owner: sim-engineer + balance | ATTACK-MOVE PROSECUTES A BASE (ADR-010, RATIFIED; four goldens regenerated under it). The 2026-07-15 journal accused the stall-cancel (baseline World.cs:1488) of parking armies 7 cells from an intact yard; instrumenting all four AMove-clearing sites proved it fired ZERO times - the cascade was the completion branch (1203) ending the lead unit two cells short of SEEING the target (crowd radius 4 + ordered point 3 from the yard > rifle sight 5), then arrival contagion (1436) stripping the stance from the other ten units in five ticks. Fix, one principle at every site: AN ARRIVAL INFERENCE REQUIRES AN ARRIVAL. Completion and MoveTo exact-arrival judge all-clear from the ORDERED POINT via a lazy EnemyNearAMovePoint helper (adversarial review caught that an in-scan flag misses the ExplicitTarget/breach-pursuit path - closing that hole moved aisuper and mission02, so it fired in real scenarios); contagion seeds must have RELEASED the stance (!o.AMove); the stall-cancel gives up the walk but keeps the fight unless within 4 of the destination. PROOF: 24/24 determinism scenarios double-run identical, gate exit 0 end-to-end, replay/saveload/campaignsave hash-exact, lan 20/20 zero desyncs, lanchaos clean, balance gate exit 0, full battery 16/16 exit 0. Blast radius exactly skirmish/aisuper/mission/mission02 (all AI-driven; the attackmove golden is a field battle and is BLIND to the defect - its hash does not move). CONSEQUENCES BANKED, not hidden: (1) rifles now raze the fortified base at t=2046 retaining 73%, cannons t=1486 at 80% - DEF-17 negative controls stay reporting-only because promoting them would gate a claim that is now measurably FALSE; Balance + Game Designer decision recorded in ADR-010, tool prose updated to stop citing the dead diagnosis. (2) The howitzer-mirror UNRESOLVED row resolves (30% at 16.9s; the mutual contagion freeze is gone); five howitzer matrix rows shift, no winner flips. (3) ScenarioMission asserted a coincidence - victory arrives from VictorySystem elimination while a tagged camp UNIT lives, so the assertion is narrowed to the provable invariant (winner 0, every camp STRUCTURE razed) and the design fork is Q012 (producer + qa, decide-by 2026-07-31). (4) Howitzer explicit-Attack death is NOT the dead zone (closest approach 8.87 vs range 9): its standoff ring lands on the turrets with ExplicitTarget locked - own ticket owed. (5) ADR numbering law added to ADR-open-queue.md after this ADR was nearly drafted as 007 against doc 23 prose reservations.
- [x] TICKET-P5-ALERT-02 | owner: client-engineer + audio + art-pipeline | THE GDD s7:85 ALERT CLAUSE CLOSED, bar the radar. Four parts. (1) Distinct audio for real: two purpose-made cues in the synth pipeline's own idiom (art/audio/synth.py) - alert_harvester, a rising two-blip motif (335 ms, RMS -9.1 dBFS) against the klaxon's falling alternation, and alert_low_power, a sagging descent (900 ms, RMS -11.7 dBFS), both peak -3 dBFS; the whole set regenerated deterministically (all 13 existing WAVs byte-identical) and the 1.28/0.82 pitch-shift stopgap deleted from SkirmishLive. (2) The veil projector icon: game/ui/icons/sod_veil_projector.png, produced the way the other 15 icons were actually made - a sips 96-to-64 downscale of the art/png reference sprite (the method was trusted only after it reproduced the committed com_factory icon pixel-identically; a 3D render of the .glb would have matched nothing, the set is stylised 2D sprites) - and the Sidebar's Exists guard flipped on its own. (3) The jump-to-event key: InputMap action jump_to_event on Space (verified free in the [input] block), the 21st row of Settings.Bindable so the settings scene and the conflict detector inherit it; every alert site records the position it pinged through one RecordAlert helper, the key flies the camera there via RtsCamera.FlyTo (the minimap-jump idiom), the most recent alert wins, and the key is not consumed while no alert has fired. (4) The launch alert: SuperweaponLaunched by an enemy raises SUPERWEAPON LAUNCH DETECTED, the hard klaxon (base attack and launch detection share it deliberately: both mean drop everything), an orange ping at the LAUNCH SITE, and the jump record; the event is emitted identically to every client and names launcher and aim point, so a launch is global knowledge today - the classic-genre convention, said in a comment at the handler. PROOF: offscreen harness (temporary autoload, removed before commit) 16/16 PASS - both cues load and play through AudioDirector's directory loader, JUMP TO EVENT listed and rebound Space to Home to Space through the shipping _Input path, the bare key proved inert with no alert recorded, and a live skirmish-04 STANDARD match raised HARVESTER UNDER ATTACK by tick 2500 and the launch alert at tick 3225, after which the jump key flew the camera to the recorded launch site to within a hundredth of a cell; four captures banked in the session scratchpad. game/ builds Debug and Release; git diff sim/ empty; full battery exit 0; all 24 goldens byte-identical. Riders: the HUD hint now advertises the jump key from its live binding, and the import pass minted the six .cs.uid files (GameFiles, Lan, PauseMenu, Settings, SettingsScene, UplinkUi) that the repo convention tracks and these scripts never had. Remaining from s7:85: ONLY "radar goes dark" - ADR-008's blackout ticket owns that alert.
- [x] CHORE-P5-BURNDOWN-01 | owner: netcode + client-engineer + architect + docs-community | THE DECISION-FREE BURN-DOWN: five small independent items, one branch, none needing a ruling, which is the point of the batch. (1) README.md stops lying about a public repo: "pre-implementation ... no production code" replaced with the truth, every claim verified by counting (four skirmish maps, three missions, twelve unit YAMLs, ten building YAMLs plus the wall, with the barracks and radar uplink honestly flagged as catalogued but not yet buildable pending ADR-009), the layout section now matches an ls of the root, and /services is called the placeholder it still is. (2) Q002's mechanical half: Relay gains a bind address and LockstepClient an address to dial, both trailing optionals defaulting to loopback, so all three call sites (the runner's lan and lanchaos gates, LanSmoke) compile unchanged and the lan gate runs the identical soak; the LAN screen's HOST and JOIN buttons now name the ONE remaining blocker (the battle scene's frame loop: AdvanceTick blocks on a Monitor.Wait, real design work, still netcode's), doc 18 s1's over-claim is corrected to a loopback soak with two-machine play open, and a dated resolution-so-far note is appended to Q002, which stays open for the second half. (3) Doc 22's three numbering-conflict sites (its lines 1435, 1471 and 2062, the ones ADR-009 clause 11 supersedes) each carry a dated RESOLUTION note stating the shipped numbering from World.cs:46 and the compiled struct types, pointing at ADR-009; the surrounding prose is untouched because the doc is history. (4) ADR-011 drafted to Proposed: the skirmish starting hand moves into the sim's MapLoader layer per Q005, one skirmish-golden regeneration when ratified, the CellCentre convention decided IN the same change with Balance as co-signer for the measured 550-tick outcome shift, and Q005's do-not-bundle warning answered in the ADR rather than ignored; the open queue's ADR-011 reservation (Lua sandbox) is re-queued as ADR-013 in the same commit, per the queue's own ADR-004 precedent. (5) AudioDirector's two pre-existing nullable warnings fixed truthfully rather than suppressed: _ambientPlayer = null! (created in _Ready, the codebase idiom) and TryGetStream's out parameter annotated [NotNullWhen(true)] AudioStream? because it genuinely is null on the false path; for the record the second warning was CS8601 at the TryGetValue call, not a second CS8618. PROOF: full battery exit 0 before and after, git diff sim/golden-hashes.txt empty, game builds Debug AND Release at 0 warnings (was 2). FOUND IN PASSING, not taken: sim/README.md, game/README.md and tools/README.md still read "Placeholder. No code until ADR-001 and ADR-002 are ratified", the root README's old lie standing in three more places; the same class of decision-free fix, left for its own entry per the scope rule.
- [x] TICKET-P6-WAVE-A | owner: client-engineer + audio + ux | DOC 24 TIER 3 IN ONE WAVE, commit 343c482: the faction picker (P6-FACTION-01), the score (P6-MUSIC-01), the battlefield voice (P6-VO-01) and the contextual cursors (P6-CURSOR-01), all client-only and hash-neutral. FACTION: a FACTION row opens the skirmish menu; MatchSetup carries BOTH sides (two fields, because every pre-P6 sidecar means the legacy both-Directorate pairing and must keep decoding to it); BuildStartingWorld applies World.SetFaction for both players before any spawn; the sidebar's unit buttons gain faction-gated visibility mirroring the sim's Produce refusal exactly as the veil button mirrors BuildStructure, and the Sodality's shade raider and phantom tank get buttons at all for the first time. Replays did NOT carry faction (the .frep holds seed, setup, commands only) - fixed in the JSON sidecar, client-only, taken rather than filed; saves already carried it (format v2, Q001). MUSIC: two 64 s same-length beds from synth.py's own toolkit (calm root-and-fifth drone; additive war-drum layer), seam-checked seamless loops, on a new Music bus; AudioDirector gains the player pair with a smoothed crossfade driven by a combat-intensity signal (Fired involving player 0, ~5 s decay); MUSIC slider joins the settings scene; the fifteen existing WAVs regenerated byte-identical. VO: nine placeholder TTS clips (art/audio/make_vo.sh, one say line per clip, afconvert to the project wav shape) on the Ui bus ALONGSIDE every toast and klaxon, never instead, behind a per-line cooldown map so a massacre says "unit lost" once; legal-review check owed before any public build ships them (doc 24 caveat). CURSORS: eight 32x32 PNGs from a stdlib-only generator in doc 16 tokens; one CursorFor resolve per frame from state the scene already computes, applied only on change; menus keep the OS default and _ExitTree hands the pointer back. PROOF: offscreen harness (temporary autoload, removed before commit) 42/42 PASS through the REAL menu, battle, save, resume, replay and settings paths - a menu-driven Sodality skirmish built a shade raider, a hand-crafted wrong-faction Produce was refused by the sim, a Sodality save resumed Sodality, a Sodality replay re-simulated onto its recorded hash, the calm bed swelled to -11 dB under fire and decayed to silence, "construction complete" was found on a live UI player carrying the right stream, and the cursor resolver answered Attack, Harvest, Invalid and Select at the four required hovers; three captures banked (menu row, Sodality sidebar, attack cursor in frame). git diff sim/ EMPTY, goldens byte-identical, full battery exit 0, game builds Debug and Release at 0 warnings. Findings and owed items: docs/tickets/P6-wave-a-classic-parity.md (Sodality unit icons owed to art-pipeline; DirectorateMark/SodalityMark are slot colours, cosmetically misnamed now; a fresh Directorate skirmish now faces a Sodality AI per doc 24, with the mirror match filed as a possible OPPOSITION extension).
- [x] TICKET-P5-DATA-01 | owner: architect + client-engineer | P6 WAVE B1, commit 8e375ce: /DATA BECOMES THE RUNTIME SOURCE (ADR-006, ratified). BuildStartingWorld registers the /data catalogue before any spawn through GameFiles.RepoRoot; the runner's load path became CatalogueFiles in the sim so the client and the gate walk ONE implementation (sorted ordinal walk, duplicate claims refused, every compiled type demanded - the compiled catalogue is deliberately NOT the fallback for a broken /data); failures land as readable messages naming the file, the parser's line, or the compiled type a missing file should provide, behind a BATTLE REFUSED overlay on a fully usable menu. The desync surface is closed before it opens: World.CatalogueChecksum (FNV-1a over canonicalised registered defs, sim-computed, never file bytes) exchanged in the LAN hello (Wire Check/Start/Refuse; the LockstepClient constructor blocks for the verdict so a mismatch cannot reach tick 0 by construction), recorded in save format v3 (magic idiom; v1/v2 still load, absence means do-not-check never refuse) and in .frep v3 (catalogue line; v2 streams untouched), every refusal naming BOTH checksums. New catrefuse gate, additive (golden list untouched by construction), standalone and inside the battery: LAN refuse both-named no-desync, save and replay refuse, v2 byte-surgery compatibility proofs, the three readable error shapes, and the ADR's hash-impact argument ASSERTED - /data registers to exactly the compiled checksum (0xF7ED8ABE932D6C46 at HEAD). THE GATE CAUGHT A REAL BUG PRE-SHIP: the v3 loader's faction read was keyed on the v2 magic alone, misaligning every v3 stream; now keyed on anything-newer-than-v1. Riders forced by the wave: the sidebar's hardcoded price tables deleted (live catalogue delegates; a table would show compiled prices while the sim charged authored ones); the relay stands down quietly when a client dies mid-hello (reachable once a world factory can refuse over broken data); MapData.BuildWorld gained a pre-spawn configure hook because mission content reads the catalogue at spawn time. PROOF: offscreen harness (temporary autoload, removed before commit) 26/26 across two runs - copy-edited data (cannon 650, plant 350) priced on the real sidebar and CHARGED exactly 350 through the real queue path, then with data restored byte-identical the edited-era save and replay both refused naming both checksums, a malformed file refused naming com_rifle_squad.yaml line 1, a hidden file refused naming compiled unit type 7, and the menu survived everything and started a clean battle at the pristine 600. Full battery exit 0, 24/24 goldens byte-identical, spectate/replay/saveload/campaignsave/lanchaos/balance green, both client builds 0 warnings, git diff data/ empty. Findings and owed items: docs/tickets/P6-wave-b1-data-runtime.md (packaged-build /data pathing stays with the release ticket; Main.cs loopback scene still compiled-catalogue by choice).
- [x] TICKET-P5-SPAWN-03+04 | owner: sim-engineer + architect | P6 WAVE B2, commits 4cba971/dc49054/c5b2f90: RALLY INTO THE SIM, THEN SPAWN OCCUPANCY AND THE REFUND (ADR-007, ratified), in doc 23 Wave 4's load-bearing order, with the campaign's FIRST golden regeneration. RALLY: CommandType.SetRally = 16 on the ordinary command path; Entity gains RallyX/RallyY/HasRally/Departing appended after FieldCloaked, hashed and serialised in declaration order; SetRally accepts only an owned producing structure (IsRallyable: Factory or CY, the predicate ADR-009's IsProducer widens), clamps as Move does, AuxId -1 clears canonically. Produced units leave the mouth with movement fields written directly: toward the rally when set, else a deterministic default two steps further out along the chosen spawn offset, so the eleven-cell ring can never saturate; Departing suppresses the crowd-arrival shortcut until the unit leaves its spawn cell, killing SPAWN-D3 (a 2-cell rally moves the unit), and is deliberately set on the DEFAULT exit too (recorded on the ADR - the default lands inside the same 4-cell radius). Save format v4 - the ADR said v3, B1 took v3 for the catalogue checksum first; dated deviation note on ADR-007 - with v1/v2/v3 all loading rally-unset, proven by layout-aware downgrade surgery shared with catrefuse (whose old inline v2 surgery assumed the checksum was the only difference and had to be rebased). Client: `_rally` DELETED; the right-click issues SetRally (closing SPAWN-D9), markers written every frame from the sim's own RallyX/RallyY and reaped with their producers, rally-beats-auto-harvest kept via a sim HasRally read, rally persists across saves BY THE SIM (Q004's rally half resolved and appended there; the auto-resume half stays open by the ADR's own scoping). OCCUPANCY: the spawn ring tests standing entities via ValidPlacement's own predicate (CellOccupied; direct scan, stated in code, because SeparationSystem's buckets miss the broader predicate and same-tick spawns); every offset blocked = the unit is HELD at 100 per cent with the queue unpopped, so a paid unit can NEVER vanish (SPAWN-D2 dead) and the line stalls honestly. THE MEASURED 3000-CR/S TRAP: the BuildProgress/BuildPaid zeroing moved BELOW the hold decision, so BuildPaid stays intact while holding and the pay-as-you-build formula owes exactly zero every held tick by construction; ProductionComplete with C fires the tick a cell frees; the client raises EXIT BLOCKED through the W3-19 toast from a post-Step read. New spawngate (additive, standalone + battery): validation/clamp/clear, rally exit with C, 2-cell rally, v4 round-trip + v3/v2 downgrades, TEN units back to back on TEN DISTINCT CELLS, the walled-in hold spending EXACTLY ZERO over 100 ticks then releasing at exactly 400 credits for two rifles, the multi-unit close rally at distinct positions, SetRally through the .frep format. THE REGENERATION, once for the wave, every row explained BY EXPERIMENT: all 24 move because four fields joined the per-entity hash input; with those Add lines temporarily neutralised, EIGHTEEN scenarios reverted EXACTLY to the old goldens (proven bit-identical behaviour - they complete no factory production; mission03's Turtle never funds a factory inside its scripted horizon), and the SIX that stayed moved are exactly the production scenarios: production, skirmish, expansion, aisuper, mission, capture. PROOF: full battery exit 0 (24/24 determinism, all scenario assertions, defence load, catrefuse, spawngate, lan 5/5), saveload/replay/campaignsave/spectate/lanchaos/balance green standalone, purity and portability greps clean, goldens byte-identical to a fresh run with LF verified at byte level, both client builds 0 warnings, project.godot byte-identical. Offscreen harness (temporary autoload, removed before commit) 12/12 through the REAL menu, battle, right-click, sidebar, save-slot and resume paths: the real click became SetRally at the aimed point, rifles arrived at the marker, the walled-in hold toasted EXIT BLOCKED and spent zero, and the resumed battle carried the rally FROM THE SIM with the marker rebuilt from sim state; slots 1/2 and all pre-existing replays untouched. Findings and owed items: docs/tickets/P6-wave-b2-rally-and-spawn.md (CY rally affordance to PROD-05; Q004 auto-resume half to the economy batch; netcode inherits SetRally with nothing to retrofit).
- [x] TICKET-P5-PWR-03+04+05 | owner: sim-engineer + client-engineer + balance + game-designer | P6 WAVE B3, commits d18c8ab/0facf2a/dccb8d9/52004ee: POWER GETS TEETH (ADR-008, ratified) - the turret gate, the honest draws and the radar blackout, one wave because the ADR forbids splitting them, with the wave's ONE golden regeneration. DRAWS: refinery 0->40, yard 0->20, superweapon 100->150 in /data AND DefaultStructureType in lockstep (the selftest round-trip enforces the twins; catalogue checksum moved - ADR-006's designed replay-compat break); the zero-margin opening base (exactly 100 v 100) ships as the ADR accepts with the A11 co-sign in its deciders line; stale notes fixed in three YAMLs. GATE: one guard line in CombatSystem at the ADR's exact insertion point (above the cooldown decrement: a dead turret does not reload) reading the Wave 2 spans that fed nothing until now - PRE-combat snapshot exactly as pinned (plant dies tick N, turrets dark N+1; ProductionSystem keeps its post-combat tally per the per-system rule); turret kind only; divisionless AtLeast75, boundary inclusive and PROVEN AT THE EDGE in the walls battery's new PHASE J (0 supply withholds fire 60 ticks with cooldown untouched, 14/20 dark, 15/20 fires THE NEXT TICK, selling back below freezes the reload mid-cycle). THE WALLS PHASE G AMENDMENT IN THE SAME COMMIT AS THE GATE per the ADR: player 1 powered (100/20) and ASSERTED, so "took nothing back" is proven against a live out-ranged turret, not a dead one. Every SpawnTurret site re-read by hand (the ADR's acceptance): stealth and veil gained powering plants (report strings record supply v draw), bench rig verified at 1000/200, StDebug mirrored - and the re-read caught what the ADR's own clause 7 got wrong: the three MISSION .fmap files DO carry structures, and mission-02's camp turret drew 20 against supply ZERO (the walls-trap shape, hash-invisible); its compound gained a plant in the same change. Also found pre-existing: construction's phase E raider quietly kills the phase-B plant mid-scenario (hold-to-fire auto-acquire), recorded in code. ScenarioSuperweapon's boundary survived UNLOOSENED via the named escape (third plant supply:70 -> exactly 170 v 170). RADAR: struct 12 buildable end to end (SpawnRadarUplink, PlaceStructure arm, MapLoader arm, IsStructure - the silent killer - sidebar button, veil-projector interim model per doc 23 with the bespoke owed as TICKET-P6-ART-01); AI raises radar before superweapon (hasRadar rung at 1500cr; aisuper asserts the order, super lands 1051 v 766 before). BLACKOUT client-side at the minimap refresh, the documented rule exactly (living own uplink AND supply >= draw, the below-100 clause, NOT the 75): dark = cinder panel + bone RADAR OFFLINE, pings exempt deliberately, clicks swallowed; ALERT-02's LAST clause done - edge-triggered on the loss crossing from a flag starting false (radarless opening silent), toast + new synthesised alert_radar cue (carrier fracturing into static, distinct from the whole set) + vo_radar_offline + gold ping + jump-to-event; construction's new PHASE I pins the exact predicate in the battery (built, placed at draw 80, crossed dark on sells, relit on a rebuilt plant, died with the uplink at exactly 450). TURRET CLIENT FACE: per-owner brown-out drives an unshaded near-black MaterialOverlay wash on BOTH sides' turrets (seeing enemy guns die is the pillar's payoff) + readout OFFLINE - LOW POWER. Q008 handled in the open: mechanics shipped under the directive's scope, curation half stays with the Game Designer (status note on Q008; dated notes on the ADR). THE REGENERATION, every row by experiment: 13 of 24 moved, 11 byte-identical INCLUDING production and depot as clause 7 demands; neutralised scratch (gate + draws + AI rung reverted) reverted EIGHTEEN rows byte-exactly to pre-wave, the six still moved are exactly the six amended-world scenarios, and stealth/veil/mission02 hash IDENTICALLY in scratch and final (their plant is their whole movement; the gate never bites a powered turret); economy/victory move on the hashed PowerDraw literal alone; the AI five behaviourally; walls moves BECAUSE amended - unamended it provably would not have, which is the trap's whole point. PROOF: full battery exit 0, determinism 24/24, defence load 1.54ms/tick, saveload/replay/campaignsave/spectate/lanchaos/balance green, purity+portability greps clean, both client builds 0 warnings, project.godot byte-identical. Offscreen harness (temporary autoload, removed before commit) 13/13 through the REAL menu and sidebar paths with five screenshots: dark radarless open (no alert), still dark with power but no uplink, LIVE with the placed uplink, blackout + RADAR OFFLINE toast + exactly-one alert on selling the plants, relit on a rebuilt plant, and the turret wearing the wash with readout "TURRET 400/400 OFFLINE - LOW POWER"; harness recording deleted, 50 user replays and both save slots untouched. Findings and owed items: docs/tickets/P6-wave-b3-power-gets-teeth.md (Q008 curation to Game Designer; bespoke radar model to art-pipeline; B4 inherits IsProducer and real prereqs).
- [x] TICKET-P5-PROD-04+05+06 | owner: sim-engineer + client-engineer + ai-engineer + game-designer + balance | P6 WAVE B4, commits eaa90ba/23e071f/5d2c92f/2fbfcc0: THE BARRACKS SPLIT, THE TABBED SIDEBAR AND THE TECH TREE (ADR-009, ratified), with the TWO regenerations doc 23 s6 requires and explicitly forbids bundling. Ordered AI-before-gate (the ADR permits it) because a commander cannot route to a barracks that is not spawnable and the gate must not land on a commander that does not know the tree, so the battery is green at every commit boundary. BARRACKS (clause 5, hash-neutral, acceptance = the empty diff): SpawnBarracks in SpawnFactory's shape, PlaceStructure arm, MapLoader arm (mandatory - the depot shipping without one is PROD-D7's proof this gets skipped), IsStructure gains Barracks AND Airfield (free, and the omission is the failure mode), IsProducer at three of its four sites with the queue hash held for the regeneration it is bound to, the CY guarded out of Produce in the interim so a unit order could never reach a structure queue, and IsRallyable widened to IsProducer IN PLACE so the wire format never changed twice (B2's deferral answered). AI (clause 7, regeneration ONE): barracks in the entity scan and in the ladder between refinery and factory per doc 23 s4.3; produce routed at the COMMAND site by the chosen type's own ProducedAt (not the doctrine switch); queue-depth guard generalised to the ROUTED producer or a barracks queue runs unbounded and drains the treasury; Barracks/RadarUplink/Airfield added to wave-target kinds; expansionDesired left reading the factory ON PURPOSE with a comment binding it to Q006, because gating on the data is what stops the AI saving 3500 for an MCV it cannot buy. THE FINDING THAT JUSTIFIES THE TICKET, measured on the real map not reasoned: the barracks rung DEADLOCKED mission-01 permanently - 500 credits the opening never spent meant plant+refinery+barracks+factory+plant+turret and then 16 credits with ZERO harvesters, unable ever to afford the 1400 that funds everything; no income, no army, no wave, forever. Fixed with a STATED rule rather than restored luck: once the factory stands, want nothing more from the yard until something is mining (economy before defence; also covers losing every harvester mid-game). Mission-01 wins again at 3574 v 3440. Scope beyond clause 7's list, forced BY clause 7, recorded not absorbed. GATE (clauses 2/3/4, regeneration TWO): Produce gains e.StructType != def.ProducedAt (the split, one line) then HasPrereqs, using break so the epilogue runs; BuildStructure gains the same tree test; the tree authored across all ELEVEN /data/buildings files and mirrored in DefaultStructureType in lockstep (selftest round-trip enforces the twins; catalogue checksum moved to 0x5BF90D9D2F839D68 - ADR-006's designed replay-compat break); HasPrereqs is an entity-index scan, deterministic, per command not per tick; queue hash widened to every producer, shutting PROD-D5, whose real hazard was sharper than the audits said - factory and refinery are BOTH 2000cr/300 ticks, so two yards queueing one each were bit-identical every tick until ReadyStructure was written; clause 4's queueing-time semantic shipped as BEHAVIOUR (a queued harvester survives its refinery being sold; a fresh order is refused). Campaign edits in the same commit (mission-01 and mission-03 gain struct 11; header stops omitting yard and wall, documents barracks and radar, and says struct 11 = barracks while UNIT 11 = engineer). SCENARIO SURGERY decided per scenario as the ADR demands: production gains a barracks clear of its raider (timings untouched); CAPTURE is the case the ADR named - "the prize produced a rifle squad" at a captured FACTORY, refused under the split - rewritten to a cannon tank with the budget 200->400 because nobody there owns a plant and a 150-tick build at the half-rate floor takes 300; construction gains a DIRECT-spawned factory for phase I's radar prerequisite (direct spawns never run the gate, so the phase still tests the radar not the tree); spawngate's rifle producers all become barracks, which is the gate exercised end to end plus barracks rally proven. NEW PRODGATE mode (additive, golden list untouched): refusal both ways costing nothing, acceptance both ways with units on the ring, prerequisites bound to the OWNER's structures (an enemy refinery buys nothing), the queueing-time semantic, the whole tree walked rung by rung, the veil's faction gate proven ORTHOGONAL both directions, the barracks end to end at 500cr/100 ticks building all three infantry and rallying them, and PROD-D5 shut. TABS (clause 6, client-only, hash-neutral): BUILDINGS/DEFENCE/INFANTRY/VEHICLES; AIRCRAFT ABSENT because clause 10 keeps the airfield out and a tab over an empty list advertises a building the game cannot build; visibility becomes the AND of allow-list + Wave A faction gate (both per item, so intact per tab) + LIVE prerequisite + living producer, ABSENT not greyed per the ADR's ratified choice (the instruction floated greying; the ADR governs), with an empty-tab note carrying the honesty; membership from the live catalogue's produced_at not a second table; QueueUnit routes the same way; FindOwnStructureByType picks the shortest queue and fixes PROD-D6's inert second factory; Ralliable gains Barracks and NOT the CY (B2's affordance question settled: a yard's products are placed, not spawned). TWO CLIENT FINDINGS from the harness's own screenshots: at 190px the four tab titles overflowed and Godot silently collapsed INFANTRY and VEHICLES behind scroll arrows (a sidebar hiding half the game, invisible to every assertion) - panel widened to 250 and the toast's copied literal replaced with Sidebar.PanelWidth; and the screenshots themselves were STALE because the checks drive hundreds of ticks inside one frame, so captures now yield real frames first. THE REGENERATIONS, every row by experiment: ONE moved FOUR rows (skirmish/expansion/aisuper/mission - exactly the scenarios with a commander that has a yard; mission03 correctly did NOT move because its map places no CY so Act returns decapitated) with TWENTY byte-identical and neutralisation reverting all 24 to pre-wave; TWO moved SEVEN (adding production/construction/capture) with SEVENTEEN byte-identical, and the four AI rows are proven NOT gate-driven: reverting the queue-hash widening ALONE, gate and tree fully live, returned them EXACTLY to regeneration one, corroborated by an instrumented 6000-tick run counting ZERO refused Produce and ZERO refused BuildStructure on both sides. Q006/Q007 handled in the open like B3's Q008: the enforceable half ships (engineer's authored barracks placement now real; the four [com_factory] tautologies left AS AUTHORED and enforced honestly), the curation waits with dated status notes, and Q007's predicted cheap window has now closed. PROOF: full battery exit 0, determinism 24/24, defence load 1.575ms/tick with HasPrereqs in the command path, saveload/replay/campaignsave/spectate/lanchaos/balance green, purity+portability greps clean, both client builds 0 warnings, project.godot byte-identical. AI acceptance: both factions raised a barracks at tick 646 and fielded MIXED armies (Directorate 20 barracks-built + 6 factory-built, Sodality 21 + 4) with waves launching on both sides. Offscreen harness (temporary autoload, removed before commit) 51/51 across two live skirmishes with seven screenshots: four tabs navigating, membership by produced_at, the opening base offering ONLY the power plant with everything else correctly absent, the tree OPENING as a real plant is built and placed, the barracks built the same way, INFANTRY filling with rifle/rocket/engineer, a rifle order landing in the BARRACKS queue not the factory, a barracks rally set through the REAL right-click and honoured, and Sodality's veil absent at the plantless open BY THE TREE then appearing on its plant; 24 harness recordings deleted, 50 user replays and both save slots untouched. Findings and owed items: docs/tickets/P6-wave-b4-barracks-and-the-tree.md (Q006/Q007 to Game Designer, now at regeneration cost; A11 co-sign for the tier-2 move; bespoke barracks model to art-pipeline as TICKET-P6-ART-02; doc 23 PROD-06's repair half deliberately NOT bundled; tab hotkeys to UX; C3 inherits the per-producer seam and C5's air ADR owns the AIRCRAFT tab with the airfield).
- [x] TICKET-P6-VISUAL-V0-V1 | owner: client-engineer + tools | DOC 25 WAVES V0 AND V1, branch ticket/p6-visual-v0-v1: THE PROJECT CAN NOW SEE ITSELF, AND MORE THAN HALF OF EVERY PIXEL WAS FOG. V0 is a committed capture harness (game/scripts/LookDev.cs, game/scenes/LookDev.tscn, tools/lookdev/) rather than the throwaway autoload four earlier waves used: one committed reference save of skirmish-03 at tick 3600 with both bases, nineteen live units of both factions and all three shroud states in shot, three frozen cameras at the shipped -50 pitch (CAM-A at height 22, CAM-B at 42 with both bases in one frame, CAM-C at 9 on a bridge where a Directorate and a Sodality vehicle sit metres apart over water), captured through the SKIRMISH path so no capture is ever taken under ReplayTheater's five directionals. Determinism cost the most and is documented in full in the delivery notes: the sim is driven through AutoStep rather than the frame clock, the warm-up is counted in process frames rather than rendered frames because turret sway and track scroll accumulate the _Process delta, particles are seeded AFTER the first syncs create the damage smoke, the settle is 240 frames rather than doc 25's thirty because the fog's temporal reprojection has not converged at thirty, and the harness runs ONE PROCESS PER CAMERA because the fog history is a property of every frame the process drew (returning to CAM-A after a three-camera run and reshooting it moved 16,592 pixels with the scene frozen, which is what identified the cause). What remains is a driver-side floating-point floor of under twenty pixels of 1.44 million, provably not the harness: with every screen-space pass disabled six consecutive runs are byte-identical. LOOK-02's contact sheet prints its masks and their exact rectangles with every table, and Godot's --headless renders nothing so the roadmap's headless invocation is corrected to windowed with --audio-driver Dummy. THE DAY-ONE EXPERIMENTS SETTLED THE ARGUMENT: fog density 0 at CAM-A drops mean luminance 37.5/255 (the fog was ADDING that much uniform light), collapses mean HSV saturation from 0.488 to 0.064 and opens the frame's hue span from 18.5 to 222.7 degrees; hiding the shroud is worth 7.5/255 and no hue change at all. That also proves V1-01's own saturation criterion points the wrong way, because the frame's apparent saturation WAS the fog's blue cast. V1 then landed as one change: fog density 0.022 to 0.010 with a desaturated albedo and GIInject 0.6, doc 22's ratified C-08 shroud values unmodified, ambient source Color to Sky with the sky lifted to (0.060,0.080,0.130) top and (0.22,0.19,0.16) horizon at 1.6x, the ground shader's three samplers to filter_linear_mipmap_anisotropic, project.godot's 16x anisotropy plus debanding plus FXAA (TAA refused on contract grounds), SSR off. V1-07's single retune is TonemapExposure 1.35 to 2.9 and NOT an ambient cut: measured, AmbientLightEnergy is DEAD under AmbientSource.Sky at sky contribution 1.0 (0.4 to 1.0 moved open-ground luminance by 0.03/255), so the ticket's own candidate knob does not exist. Result at CAM-A: open-ground luminance 78.0 to 93.8, inside doc 22's ratified 90-135 band which the baseline never met; whole-frame hue span 18.5 to 53.7 degrees; frame range p95-p5 75.8 to 85.1. CAM-C hardware mean luminance 78.6 to 122.7. Frame time at CAM-B 10.27 ms to 9.86 ms, the project's first GPU-side number. Sim untouched, goldens unmoved, battery exit 0, Debug and Release clean. Two new visible defects UNCOVERED by the fog cut and owed onward: the one-texel-per-cell shroud boundary is now visibly blocky, and the top fifth of every frame is off-map void at a 75-degree FOV. Delivery notes: docs/tickets/P6-visual-v0-v1.md.
- [x] TICKET-P6-VISUAL-V2 | owner: art-pipeline + client-engineer | DOC 25 WAVE V2 (V2-01 + V2-02), THE UNCOVERED SHROUD, AND THE EXPOSURE IT CREATED, branch ticket/p6-visual-v2: ONE TEXTURE CHANNEL STORED THE NUMBER 0.2 A MILLION TIMES, AND OCCLUSION WAS APPLIED THREE TIMES. Four logical commits: shroud, bake-pipeline change, the 27 re-baked assets, then exposure and docs. PART 1 THE SHROUD (the ugliest thing left in frame after V1, fixed first because it is cheap): FogOfWar.cs painted one texel per map cell and bilinear upsampling interpolated a hard 0-to-0.30 alpha step across ~30 screen pixels, a staircase with diamond facets. Fixed by softening the FIELD not the filter or the resolution: per-cell opacity dilated 3x3 (an EROSION of the visible set), blurred by two separable 3-tap passes, then clamped back up to the truth so every texel ends AT LEAST as opaque as the sim says. It reveals nothing by two independent locks - the ramp lives entirely on the visible side of the boundary (feathers inward, never lifts shroud off hidden ground), and the plane hides nothing anyway (enemy units hidden by SkirmishLive.SyncActors reading World.IsVisible directly, minimap dots same gate); the truth image is still one-texel-per-cell and unsoftened and still what the minimap reads. Guarantee stated in a comment. Free: the rebuild moved off a per-cell SetPixel loop onto a byte buffer. PART 2 THE BAKE (all 27 models re-baked once): V2-01 metallic was the constant 0.200 at the 5th/50th/95th percentile alike across all 38 materials (99.96% in the invalid 0.10-0.85 band) because builder.mat floored at max(metal,0.2) and bake.py read the Principled default_value; removed the floor, defaulted mat/wmat to 0.0, baked the NODE OUTPUT, drove Metallic binary from the chip mask via a constant-interpolation ramp -> p5/p50 0.000, p95 1.000 on most materials, mid-band mean 7.3%. THE FIFTH ROADMAP ERROR, found here and corrected in doc 25 in place: V2-01 clause 3's "existing pointiness chip mask" does not isolate edges - pointiness is normalised over the mesh, so its flat-face value is 0.5725 on most models, 0.60-0.61 on infantry/wheels, 0.30-0.33 on the Sodality vehicles, and a flat face at 0.5725 sits 62% through the old 0.535-0.595 window, so the bare-metal chip colour was smeared across whole flat PANELS of most of the roster (a second, unfiled cause of the chalky read, and why the Sodality tanks measured DARKEST); replaced by the angle between the bevel-shaded normal and the true geometric normal, exactly 1.0 on any flat face whatever the topology, median 0.00-0.04 on every object, bevel at 32 samples so the threshold stays binary. V2-02 the triple AO: bake.py multiplied AO into the diffuse at k=0.85 while the same AO shipped in the ORM red channel and SSAO ran on top; DELETED the multiply outright (not zeroed), left use_pass_direct/indirect False and untouched per the roadmap (that fix is a verified no-op), pulled SsaoIntensity 2.5->1.6 and SsaoPower 1.8->1.4 -> diffuse-to-AO correlation 0.924->-0.133 (criterion <0.55), mean diffuse linear luminance 0.132->0.260 (criterion +25%, got +97%). One fix neither ticket named: the Cycles DIFFUSE pass returns the diffuse lobe which is BLACK at metallic 1.0, so the chipped-metal base colour was baking black; metallic is muted to 0 for the DIFFUSE bake only. PART 3 EXPOSURE 2.9->2.85: the bake did NOT brighten the whole frame (units brightened, ground is a runtime shader with no baked map so CAM-A whole-frame luminance FELL 59.7->57.1); exposure tracks the ground, its ratified constraint, and the new bake gives CAM-A ground 92.0 at 2.9, 90.0 at 2.8, 85.8 at 2.6, so below ~2.8 breaks doc 22 C-10's 90-135 floor; AgX compresses the hot units in its shoulder so exposure barely moves them (163->155 across 2.9->2.6), making it the wrong tool for them anyway; 2.85 lands CAM-A ground 91.0, safely in band. LOOKED AT ALL THREE CAMERAS: shroud staircase gone, replaced by a soft edge of vision; Sodality vehicle from a dark grey-olive chalky lump to a rust hull carrying its faction colour, Directorate from a chalky box to top-lit painted steel with track-edge specular; CAM-C unit chromatic fraction 0.14->0.42, unit saturation 0.108->0.156; ruins warmer legible tan. HONEST CAVEATS reported not hidden: the Sodality rust reads slightly high-key at the band-forced exposure (a SATURATION problem, C-05's job, must not be faked with exposure), and 5 structure materials retain >8% metallic mid-band (long mask boundaries at 2048px, cosmetically invisible at RTS distance). NOT IN THIS WAVE and still owed: V2-03 (roughness octaves) and V2-04 (doc 22 C-06 then C-05) need their own re-bake, now landing on the clean albedo V2-02 produced. A REAL TRAP FOUND: the capture harness does NOT trigger a Godot reimport, so a capture straight after a re-bake shows the OLD imported models (caught by checking the imported .scn timestamps; fix is one Godot --headless --import before capturing). Frame time CAM-B 9.863->9.766 ms at 117 draw calls. Sim untouched, git diff sim/ and golden-hashes.txt both empty, battery exit 0, Debug and Release 0 warnings, saves/replays/settings byte-identical (only Godot engine logs differ). Delivery notes: docs/tickets/P6-visual-v2.md.
- [x] TICKET-P6-VISUAL-TERRAIN | owner: client-engineer + art-pipeline | DOC 25 WAVE V-TERRAIN, THE NATURAL TERRAIN PASS THE OWNER ASKED FOR IN HIS OWN WORDS ("i want grass, trees, water, sand, rocks, proper texture"), branch ticket/p6-visual-terrain: THE GROUND WAS ONE ARID MATERIAL EVERYWHERE AND THE RIVER WAS A BLACK SLAB; NOW THERE IS A BIOME. Five logical commits: biome ground, water, grass, trees+rocks, docs. Subsumes and delivers the terrain half of doc 22 Wave A.5 (biome ground, ratified and never started) and the ground half of Wave C, deliberately NOT the roster-chroma tickets (C-01/C-05/etc, which are the units' colour and belong to the bake waves). THE BIOME MODEL: BuildBiomeMap generates an RGBA control map from the map alone with no RNG and no sim coupling - R grass, G sand, B rock, A wetness, one texel per cell, lightly blurred - and BiomeAt exposes the same field to placement, so the ground shader, grass, trees and rocks all agree where each biome is. Base biome is temperate if the map has water and arid if not (no new map metadata, no sim change): skirmish-03's river makes it the temperate showcase, the dry maps stay arid. Within a map, sand forms beaches in a band around water (distWater BFS) and dry patches by macro noise, rock forms on the feet of hills/ruins/ridges (distRock BFS) and by macro outcrop noise, grass fills the rest and recedes where sand or rock dominates; wetness is the shore land just outside the water. GROUND: ground_biome.gdshader replaces the single arid ground_splat (dust/rock/gravel, all dark grey-brown); blends the three biomes with multi-octave WORLD-SPACE noise on albedo AND roughness, a distance-faded detail normal (V3-03's anti-shimmer lesson) and a large-scale grass hue drift so a field is never flat green; sampled in world space (wpos.xz/map_size) not UV so the dust-capped mesa slabs pick up the same biome through the MultiMesh MODEL_MATRIX, the way the old shader relied on. WATER: water.gdshader replaces the StandardMaterial3D slab (metallic 0.85, roughness 0.08) that read as a hole; transparent-queue depth colour read from the scene depth behind the surface (shallow teal at the bank, deep blue-green in the channel, depth_scale stretching the shallow basin column into a full ramp), a fresnel edge, two world-space scrolling ripple normals, and a scrolling shore foam line; the per-cell 1.02-overlap slabs became ONE seamless BuildWaterSurface mesh because a transparent material double-blends coplanar overlapping tiles into a grid of dark seams (measured, the first water capture showed it) - the sunken basin the shader reads depth from is untouched, per the brief's "do not rebuild the basin unless you must". GRASS: a Grass MultiMesh of three-way crossed blade cards alpha-cut from a procedurally drawn blade texture, instanced only where the biome grass weight is high, thinned by density noise, stopped at sand/rock/water/blocked; three species tints jittered per instance; grass.gdshader sways the blades weighted by height; range-culled (VisibilityRangeEnd 40) so it is full at play height and gone by max zoom where a blade is sub-pixel and the ground's own green carries the grassland read (V3-04's rationale, and it keeps grass off the CAM-B budget). The dead-straw tufts are now gated off grassy ground so they dress the dry/sand/rock biome. TREES: a procedural low-poly tree (bark trunk plus a three-lobe canopy, two surfaces on one mesh), MultiMesh-instanced and CLUSTERED into copses seeded on the land cells bordering water and on the map edges so it reads as woodland along the river and the frame; foliage.gdshader gives the canopy noise-varied albedo/roughness and a gentle wind sway. ROCKS: larger irregular boulders (a low-poly blob warped by deterministic per-vertex noise for a real lumpy silhouette) on the rocky biome, cliff feet and water banks, noise-driven grey-brown albedo AND roughness with a detail normal and METALLIC 0 (the V2 lesson, no chalky specular); distinct from the sub-pixel debris scatter. PROCEDURAL, NOT BLENDER, deliberately: every terrain form in BattlefieldView is already a procedural Godot primitive (hills, ruins, fences, cliffs, scatter), the Blender pipeline is for the 27 unit models, so trees and rock formations join the procedural idiom - no .glb touched, no re-bake, no reimport. WIND: TickWater now advances one wind/wave phase from _Process (NOT shader TIME) and pushes it to the grass, canopy and water shaders, so it FREEZES under the harness pause and captures stay byte-identical, the same reason the water scroll was already _Process-driven. PASSABILITY: presentation only, guaranteed mechanically - every visual is built in BuildTerrain/BuildScatter which only READ blockedCells and the visual legend, no cell is added to or removed from the blocked set, decorative meshes are clustered onto already-blocked and edge cells to make a unit-walks-through-a-tree clash rare but never gate on the sim (the brief accepts the rare clash); git diff sim/ and golden-hashes.txt both empty. PERFORMANCE, MEASURED: CAM-B baseline 10.017 ms -> 10.55 ms after biome+water+grass (grass range-culled off CAM-B); tree/rock SHADOWS turned OFF after the double-sided canopies casting into the 4096 atlas collapsed the whole-map max-zoom view to ~1.5 fps, the exact "cost more than they show" case the brief names - at RTS distance the mesh form and SSAO contact darkening carry them. A REAL ENVIRONMENTAL TRAP hit at the end: the machine came under heavy remote-desktop (Jump Desktop ~75-110% CPU, WindowServer ~25%) load which saturates the WindowServer and starves the windowed offscreen render, so the heaviest render (CAM-B, full map) could not be timed cleanly with trees+rocks in frame; the final CAM-B figure is owed once the machine is free, and the grass field (the only heavy addition) is already culled off CAM-B. Full battery exit 0 (selftest, determinism, walls, LAN 5/5 no desync, defence load 1.548 ms/tick), git diff sim/ and golden-hashes.txt both empty, no /sim file opened for writing, Debug and Release 0 warnings, saves/replays/settings.cfg hashed before and after. Delivery notes: docs/tickets/P6-visual-terrain.md.
- [x] TICKET-P6-VISUAL-V3 | owner: client-engineer | DOC 25 WAVE V3, CAMERA, SCALE AND THE ALIASING, branch ticket/p6-visual-v3: THE OFF-MAP VOID IS GONE AND UNITS READ AS UNITS. Three logical commits: FOV/void, unit scale, aliasing. V3-01: Camera3D.Fov was never set so it ran at Godot's 75 default, and at the fixed -50 pitch the far edge of the frame was ~99 m away at play height and ~189 at max zoom, beyond any map, so the top fifth-to-third of every frame was empty off-map void (V0/V1's named biggest compositional flaw, re-handed by V1 and V2). Set Fov=50, the roadmap's ratified value. LOOKED at all three cameras: at CAM-A the black band is GONE and the frame is filled top-to-bottom with battlefield (the money shot); at CAM-B the map fills ~70% against ~45 before and the void is roughly halved, with a residual top band that is inherent and honestly reported (at height 42 the frustum overshoots this finite 96x64 map even at 50, and a narrower lens reads telephoto-flat at CAM-A); at CAM-C the top void band is essentially gone. Pixels-per-world-unit at CAM-A ~18->~27 exactly as specified; whole-frame CAM-A 5th-pct luminance 9.8->29.0 (the void floor lifted), mean 87.8->103.0, chromatic fraction 0.54->0.67. Gameplay-visible (less ground per height), needs Game Designer sign-off at a capture. Reconciliations checked: MAP-05 has NOT landed (DirectionalShadowMaxDistance is a constant 90 m, nothing to re-derive, the narrower frame only covers it better), and the minimap frustum tracks the lens automatically because SkirmishLive.GroundPoint projects through the camera's own ProjectRayNormal (stale ~37-degree comment corrected to 25; Minimap.cs NOT touched, a separate session holds it). V3-02: the far tilt-shift DOF is REMOVED not kept - at 75 its blur fell on the void for nothing, and with the void replaced by real battlefield, softening the far units the player must read is the near-field mistake V3-02 forbids; the dead DofBlurFarDistance=55 initialiser and its per-frame update go with it. UNIT SCALE: section 6 deferred oversizing behind the FOV and a measurement; the FOV alone took a vehicle from ~15 to ~27 px but a tank still read as a pale shape, so a modest 1.3 mobile-only Node3D.Scale on the presentation actor. PRESENTATION ONLY, kept true by construction: PickEntity's hard-coded world radii 0.8-1.4 are unchanged so clicks still land (1.3 is well below the dropped 1.6/2.2 that drew 3x the click target), World.FootprintOf/collision/passability are sim state and untouched so a unit still occupies its sim cell, the .glb meshes are untouched so no re-bake and no reimport, the scale is set once and persists because the per-frame actor update only writes Position and Rotation (never Basis/Transform) so interpolation/hull-yaw/pitch/bob/death-tumble leave it intact, and it is mobiles-only because structures rise-tween to Vector3.One and walls sit edge-to-edge (section 6's clip/interpenetrate warnings); health bars and rank pips are scene children so they keep their world sizes. LOOKED at CAM-C and CAM-A: a cannon tank now reads as a chunky piece with a legible turret and barrel, no clipping, no wreckage. V3-03: NO CODE NEEDED, delivered early by V-TERRAIN - ground_biome.gdshader (which replaced the ground_splat.gdshader the ticket still names) already carries the exact distance fade (1.0 - smoothstep(30,90,length(VERTEX)) into NORMAL_MAP_DEPTH, lines 105-107) at a low 0.06 detail-normal frequency, citing "V3-03's lesson"; clause 2's separate gravel normal is moot in the biome shader; the stale file reference is corrected in doc 25 in place (a case of V-TERRAIN overtaking the ticket, not a measured error like corrections 1-5). V3-04: the 2200 rubble flecks and 900 dead-straw tufts are half-to-two pixels at max zoom where they only shimmer; range-culled not cut (MAP-03 scaled the count up on purpose) - VisibilityRangeEnd 38 with a 6 m Self fade, verified against Godot 4.7 source that the range is measured to the AABB centre (renderer_scene_cull.cpp transformed_aabb.get_center()), which for a map-spanning MultiMesh is the map centre, so with cameras ~25 m from centre at CAM-A/C and ~47 at CAM-B the field shows at play height/close and hides at max zoom (the live grass's own pattern, End 40); minimum rubble scale 0.05->0.12 so nothing is sub-pixel even in range. LOOKED: scatter intact at CAM-C (guard against deletion), gone at CAM-B. MSAA CONSIDERED and left at 4x: an 8x probe (harness pin, reverted) measured 10.34 ms at CAM-B vs 4x's 10.37, effectively free, but that is this M4's tile renderer resolving MSAA for almost nothing and does NOT represent the GTX 1060-class min-spec where 8x is real cost, and 8x showed no visible edge gain; FXAA stays on, TAA stays refused on the interpolation/tween contract. Frame time CAM-B 10.800 ms/119 draws -> 10.370 ms/118 draws, net negative as section 5 predicted (narrower FOV culls geometry, V3-04 culls scatter at max zoom, DOF gone) - this is the clean CAM-B figure the terrain wave left owed. Capture determinism verify-determinism.sh PASS (all three cameras byte-identical across two runs, the 1.3 scale and visibility range are constants). Sim untouched: git diff sim/ and golden-hashes.txt both empty, battery exit 0 with the same LAN hashes as baseline, Debug and Release 0 warnings, saves/replays/settings.cfg byte-identical before and after. Before/after contact sheet at all three cameras. Delivery notes: docs/tickets/P6-visual-v3.md.
- [x] TICKET-P6-MAP-01 | owner: tools + game-designer | THE SKIRMISH MAP REDESIGN THE OWNER ASKED FOR IN HIS OWN WORDS ("rivers are windy not straight blocks, the land shouldnt just be fully open there should be design that pushes users to choose best place to set up their bases"), branch ticket/p6-map-redesign, authority ADR-013, standard doc 26: skirmish-01/02/04 rebuilt from committed Python generators, skirmish-03 PRESERVED (already to standard and the frozen look-dev reference whose camera constants must never move). skirmish-01 Serpentine Ford, the river-crossing map (a straight blocked-cell wall becomes a winding river forded three times, base alcoves in opposite corners, ridge spurs splitting the near bank into a lane and a flank, ruins/fences on the approaches, a safe 4-cell ferrite patch by each base and a contested 6-cell patch at the central ford; 548 blocked = 8.92%, 20 ferrite; starts moved to the rotation pair (9,9)/(86,54)). skirmish-02 Ironback Ridge, the ridge-and-passes map (scattered straight blocks become one winding ridgeline corner-to-corner pierced by three passes proved load-bearing; 594 blocked = 9.67%). skirmish-04 Tarnwater Crossing, the big 192x128 theatre (the ruler-straight water column now MEANDERS; three bridges, bank bluffs and the 60-cell economy kept; 9.72%). tools/mapgen.py holds the fairness invariant ONCE and proves it before emit: 180-degree rotation symmetry cell by cell, open 9x9 aprons, reachability from both starts to every field + the far start + every apron, closing every crossing disconnects the starts, identical Chebyshev ferrite profiles, 8-10% density. THE HARD GATE, non-negotiable: a full AI-vs-AI match on each redesigned map in BOTH faction matchups (both Directorate, then Directorate vs Sodality) PASSED - both sides build 8-9 structures, keep a harvester, produce, path across the crossings and fight (24-38 deaths per match); no crossing needed widening; skirmish-03 run as a control also passes. PROOF: full battery exit 0; golden output byte-matches golden-hashes.txt; golden diff is a SINGLE line (skirmish 0xB7514D5EEA6DFFFD -> 0x165DE5ADA957AC53), every other scenario including all three missions and capture byte-identical (skirmish-02/04 load no golden scenario; missions load data/missions/* which was not touched); git diff sim/ is golden-hashes.txt plus the skirmish-01 selftest fingerprint ONLY, no sim logic touched; game Debug and Release 0 warnings; new tools/lookdev/map_preview.py renders any .fmap top-down without Godot or the GPU, before/after captures taken and looked at (decisive transformations). NEEDS A HUMAN: Architect + Luke ratify ADR-013 (the skirmish golden move); art-pipeline + client-engineer taste pass on the running client (the previews prove layout, not lighting); note the doc-25 biome system now reads skirmish-01's new river so it renders temperate like skirmish-03. Delivery notes: docs/tickets/P6-map-redesign.md.
- [x] TICKET-P6-VISUAL-V4 | owner: art-pipeline + client-engineer | DOC 25 WAVE V4 (PART), FACTION IDENTITY AND THE BLOWN-TANK FIX, branch ticket/p6-visual-v4: THE BRIDGE VEHICLE WAS A WHITE-PINK BLOB AND THE SHARED STRUCTURES WERE THE SAME FOR BOTH PLAYERS; NOW THE TANK IS A FORMED COLOURED OBJECT AND THE FACTIONS READ APART. Four logical commits: the bake-pipeline material change, the 27 re-baked assets, the client team-colour edit, then docs. THE BLOWN-TANK FIX is doc 22 C-06 then C-05 in ONE re-bake, exactly what V2 deferred here as a SATURATION problem for C-05 and NOT an exposure one. C-06 the 8-bit emissive clamp: the EMIT bake target is an 8-bit buffer so every emissive over 1.0 clipped a channel and on a saturated colour that clip is a HUE SHIFT (superweapon core baked yellow, veil orb cyan-white, ferrite tips pure white); fixed by normalising every emissive material down by M=max(strength*max-channel) before the bake so nothing clips, then handing M back at export via Emission Strength=2.0*M so (colour/M)*2M reproduces the final colour exactly while the baked map keeps full chroma (superweapon emit_scale 1.775/emit_max 1.000; the veil orb no longer clips at all because C-05's teal peaks at 0.864). C-05 the PAL chroma pass: the old PAL held sRGB-looking fractions in scene-linear Base Color slots so every model rendered 30-40% LESS saturated than doc 16, and that desaturation under V2's brighter exposure IS the blown-tank read; the ratified linear table restores saturation while HOLDING luminance (gun steel #7C9EBC S0.34, rust #C4774D S0.61, teal #4FB8A8 S0.57), faction hues stay 187deg apart, the Directorate chip goes grey->cool (0.42,0.50,0.60), and it is NOT doc 16's hexes read as linear (doc 22 line 942 forbids that, rust would fall 62%). LOOKED CAM-C: the Directorate vehicle from a pale near-white blob to a formed cool-steel hull with panels/tracks/turret and a rich amber strip; the Sodality from a pale pastel-pink wedge to a saturated salmon-rust hull; the ferrite cluster from pure white to warm gold. NO exposure knob and NO runtime tint (verified ModelLibrary instantiates the .glb directly and no client code tints the unit body). C-07 team-colour placement: the bake makes the dir_/sod_ VEHICLES read apart by hull temperature but the shared com_ STRUCTURES are one model for both players (a Directorate refinery and a Sodality refinery were pixel-identical), so DressStructure adds a square orange/teal footprint strip around each structure and the unit ground ring widens 0.30/0.36->0.27/0.40 to survive the RTS band, marks held at EmissionEnergyMultiplier 0.9 under the glow HdrThreshold so no bloom, walls keep their AddContactBlob branch. LOOKED CAM-A/CAM-B: the Directorate base is now boldly outlined in orange and reads as its faction at a glance (symmetric code, player-1 reads teal; the Sodality base sits in explored-but-unseen fog in this reference state, so the two-faction image pairs the orange base with the Sodality unit). A BAKE-PIPELINE FINDING for the next person: the first full bake was externally KILLED under load at 13 of 27 models (a partial com_refinery mid-export and 12 models un-updated, which presented as a misleading 15-of-27 git status); a targeted re-bake of the remaining 13 completed the roster with a clean BAKE PIPELINE DONE and 27/27 modified - confirm the DONE marker and a 27/27 count, not the per-file git status, before trusting a full bake. The reimport (Godot --headless --import) ran clean, no loose textures, sidecars unchanged (the V2 trap honoured). SCOPE HELD: the bake alone was proven (stage-sat capture) to fix the tank and make the vehicles read apart, so the client change was kept to C-07; C-01/C-10/C-11 palette polish held out (C-10/C-11 carry re-brightening/taste risk, C-11 needs Luke's ramp sign-off), and V2-03 roughness plus V4-03 HUD/menu and V4-04 VFX remain - the colour and faction spine is done, the front-end presentation is the residual. Frame time CAM-B 10.345->10.340 ms (neutral), draw calls 118->214 (C-07 strips, no measurable cost). Sim untouched: git diff sim/ and golden-hashes.txt both empty, battery exit 0, Debug and Release 0 warnings, saves/replays/settings.cfg byte-identical (only Godot engine logs differ). Before/after contact sheet at all three cameras plus a blown-tank CAM-C close before/after and a two-faction image. Delivery notes: docs/tickets/P6-visual-v4.md.
- [x] TICKET-P6-ECON-REGROW (ADR-012) | owner: sim-engineer + architect + balance | P6 WAVE B6, commits 374c1a8/a8f20ac/d7ff34c: FERRITE FIELDS REGROW (ADR-012, ratified), formalised from doc 24's classic-parity finding that permanent depletion caps match length, with the wave's ONE golden regeneration. THE RULE IS THE ADR'S EXACTLY: a new RegrowthSystem ordered explicitly right after HarvestSystem (depletion then replenish), walking entities by INDEX not a dictionary, grows each ferrite field by the /data amount up to its spawn amount. The OWN-AMOUNT rule is load-bearing and is the ratified one, not the rejected spread-to-neighbour or unlimited-regrowth alternatives: a field regrows only while its OWN remaining amount is above zero, so a field stripped to zero (set not-alive at depletion) is dead ground forever and denial by exhaustion survives. THE SCHEDULE IS DERIVED FROM THE TICK, not a stored counter: it fires when the pre-increment tick is a positive multiple of the interval, so it resumes correctly across save/load because the tick is saved, and the first regrow lands at tick 75 (a full interval after tick 0). NUMBERS ARE /DATA WITH A COMPILED TWIN: fields are map-placed not catalogue-driven, so they get their own home data/fields/com_ferrite_field.yaml (regrow_amount 1, regrow_interval_ticks 75) validated by data/schema.field.json; World.DefaultRegrowAmount/DefaultRegrowIntervalTicks are the compiled reference twin the selftest walks /data/fields to prove, exactly as the unit and structure catalogues work, and the client loads the file through the new CatalogueFiles.RegisterFields on the same pre-tick-0 path so a Balance edit takes effect. THE CAP TRAVELS ON THE ENTITY, SERIALIZED BUT NOT HASHED: a field's spawn amount is per-instance (the map deposit or a scenario literal) so it is new per-entity state FerriteCap, set once at spawn and never mutated; like Sight it is serialized (save format v5, appended after the rally tail; a pre-v5 save defaults the cap to its stored amount, the sane resume) but deliberately NOT in ComputeStateHash, because it is immutable spawn-time provenance identical on every client and hashing it would move every golden including the no-ferrite scenarios the ADR requires stay still. FerriteAmount, which regrowth mutates, is already hashed, so desync coverage is complete. NEW REGROWTHGATE mode (additive, standalone + battery, never a golden scenario): a below-cap field recovers at EXACTLY the placeholder rate (13 over 1000 ticks proven differentially against a regrowth-disabled run with the harvest sequence identical), a field at cap never overflows, a field STRIPPED TO ZERO stays dead across 300 ticks and four regrow intervals, and a v5 save round-trips regrowth and the cap bit-exact with a v4 downgrade resuming hash-identically. SCENARIO SURGERY: ScenarioEconomy 4000 -> 4014, the two fields' full 4000 plus exactly 14 that regrew while alive and below cap before exhaustion; kept EXACT not a band because the harvest of these fixed fields is deterministic across every seed (verified 4014 at 2026/31337/424242/777). THE REGENERATION, every row by experiment: EXACTLY FIVE move (economy, skirmish, expansion, aisuper, mission - each a world whose ferrite is harvested below cap and runs long enough to reach a regrow tick), NINETEEN byte-identical including all sixteen no-ferrite scenarios, mission02 (map has zero ferrite), mission03 (its lone field never harvested below cap) and capture (no ferrite). Neutralisation decisive: because the cap is unhashed and regrowth is the only new mutation, disabling the single RegrowthSystem call on a scratch build reverts all 24 rows byte-for-byte to the pre-wave goldens, so regrowth is the SOLE cause of every move and nothing that must stay still moved. PROOF: full battery exit 0 (selftest with the ferrite twin round-trip, determinism 24/24 at 2026, every scenario assertion, defence load, catrefuse, spawngate, prodgate, regrowthgate, lan 5/5), saveload/campaignsave/replay/spectate/lanchaos/balance green standalone, purity+portability greps clean, goldens byte-identical to a fresh run with LF verified, both Godot client builds 0 warnings (Debug and ExportRelease), project.godot untouched, no user save/replay/settings written. AI-vs-AI resolves on a ferrite map: skirmish ran the full loop (bases built, harvesting live, 33 destroyed, treasuries 695/870, match ended), replay ran a 3000-tick AI match bit-exact. Q013 unaffected: the nightly-soak seed 900913 ScenarioPathing failure predates this wave and this change touches only ferrite fields. Findings and owed items: docs/tickets/P6-wave-b6-ferrite-regrowth.md (Balance owns the two numbers under A11, held to the twin by the round-trip; Architect may fold the regrow config into CatalogueChecksum if regrow numbers ever diverge across a version boundary, left out today consistent with the standard-field-amount constant; client may surface a field's cap and current amount now that a field is no longer a monotonic countdown).
- [x] TICKET-P6-START-HAND (ADR-011) | owner: sim-engineer + client-engineer + architect + balance | P6 WAVE B5, commits a9e622f/49b279f/e814e10: THE STARTING HAND ENTERS THE SIM (ADR-011, ratified), resolving Q005 and completing Phase B (B1 through B6 now all DONE), with the wave's ONE golden regeneration. THE LAYER IS THE ADR'S EXACTLY: the skirmish opening hand (start credits, a construction yard per side at the map's start cells, one harvester and three rifle squads per side mirrored) moves out of the client's SkirmishLive.BuildStartingWorld and into the sim as MapData.PlaceSkirmishStart, a MapLoader-layer builder taking the treasury as a parameter (clause 1); the runner's gated skirmish scenario calls the same builder with its unchanged 8000-credit treasury (clause 2), so the golden now covers the world players actually play rather than a bare two-yard world nobody plays; the mission branch is out of scope (clause 4) and untouched. THE CELLCENTRE CHOICE DECIDED IN THE SAME CHANGE (clause 3): the harvester and squads move from the client's Fix64.FromInt cell corners to Map.CellCentre, the convention of every other sim spawn, ending the corner-versus-centre split Q005 measured; carrying the corners into the sim would have silently enshrined them. It is outcome-shifting so BALANCE CO-SIGNS under A11: Q005's instrumented 550-tick earlier skirmish-04 defeat is expected and authorised, a report not a defect. The builder is FACTION-NEUTRAL common hardware, so it neither reads nor writes the sides: the runner leaves factions at default and the client sets the menu choice (FACTION-01) before the call, unchanged. OFFSCREEN CENSUS (throwaway console referencing only the sim, no engine, mirroring the client's build): each side got 1 yard, 1 harvester (700 hp) and 3 rifle squads (100 hp, weapon 2) over 20 fields, 30 entities, with the mobile units at the .500 cell centres (p0 harvester (12.500,11.500), squads at y 7.500; p1 the mirror) where the old corners read whole numbers, so the client still starts correctly and the battery's skirmish golden now exercises the real opening hand. THE REGENERATION, one row: skirmish 0x4F6252B168468346 -> 0x3CE6E400F07A3AA1, the single row the ADR authorises; the other 23 byte-identical including all four mission-family rows (mission/mission02/mission03/capture), which build their own forces outside the builder. MISSIONS DID NOT MOVE, PROVEN TWO WAYS: the golden diff shows them unmoved AND git diff data/missions is empty. NEUTRALISATION DECISIVE: a scratch build reverting BuildSkirmishWorld to the old no-starting-hand construction (credits + two yards only) regenerates all 24 rows byte-for-byte back to the pre-wave hashes, so the hand plus the centring is the SOLE cause of the one moved row and nothing that must stay still moved. PROOF: full battery exit 0 (selftest, determinism 24/24 at 2026 with skirmish now 0x3CE6..AA1, match with every assertion + defence load 1.673ms/tick, catrefuse, spawngate, prodgate, regrowthgate, lan 5/5), the exact CI golden check byte-identical, purity+portability greps clean, replay/saveload/campaignsave/spectate/lanchaos green standalone (replay and saveload report new hashes because BuildSkirmishWorld seeds them with the hand, each still bit-exact internally; campaignsave unchanged as a mission), balance gate VERDICT PASS (0-6 faction war is the pre-existing TICKET-AI-05 note, unchanged, confirming the balance tool builds its own worlds), both Godot client builds 0 warnings (Debug + ExportRelease), project.godot untouched, no user save/replay/settings written, FogOfWar.cs and Minimap.cs untouched (concurrent local session owns them). AI-vs-AI resolves on the shared world: skirmish ran the full loop (bases built, harvesting live, 37 destroyed, treasuries 2/1398, ended in bounded time), replay ran a 3000-tick AI match bit-exact, five lan games zero desyncs. Reports that shift (not gates): the two-client LanSmoke's printed hash (was 0x4EC4DA95C8D7F31A, corner-era), replay/saveload report hashes, balance-journal skirmish-04 tick numbers. Q013 unaffected: the nightly-soak seed 900913 ScenarioPathing failure predates this wave, is thrown before the run reaches skirmish, and this change touches only the skirmish builder ScenarioPathing never calls. Findings and owed items: docs/tickets/P6-wave-b5-starting-hand.md (Balance owns the 550-tick shift acceptance under A11; the LanSmoke recorded pass value in the P5-SET-01 prose is stale but a report, left as history; C1 inherits a sim-authored opening).
- [x] TICKET-Q013-SOAK-SETTLE (ADR-014) | owner: sim-engineer + architect | branch fix/q013-soak-pathing-settle: THE NIGHTLY SOAK IS FIXED (Q013 resolved, CASE B). Root cause found by MEASURING option 3 first (a scratch copy of ScenarioPathing, not committed): at seed 900913 exactly TWO of 500 units (ids 27, 151) NEVER settle even at 40000 ticks while 498 settle by tick 365, so it is a genuine convergence defect, not a too-tight budget. Unit 27 is a clean THREE-tick position orbit at cell (42,21) ~20.8 cells short of target with StallTicks stuck at 26/27/28; unit 151 orbits (41-42,23) with StallTicks 0/1/2; both keep the +2 blocked / -1 progressing leak perfectly balanced so the 60-tick stall-drop never fires (exactly the Q013 hypothesis, two independent orbits not a cell-swap). THE FIX (ADR-014, ratified): a monotone no-progress backstop in SeparationSystem, ADDITIVE to and independent of the untouched StallTicks net, benches a flow-pathing unit that has not bettered its NEAREST APPROACH to the destination for NoProgressDeadline = 14*TicksPerSecond (210 ticks / 14s). Nearest-approach not displacement because the orbiters keep moving but never get closer, so it catches an orbit of any period where the leak's balance point hides it. Deadline sized by measurement: the longest LEGITIMATE plateau among settlers across all five seeds is 132 ticks (seed 424242; 2026's worst 120), so 210 is a 59% margin and 3.5x the existing 4s net, and no healthy queueing unit is ever benched. Two new per-entity fields NearestApproachSq + NoProgressTicks, HASHED and SERIALIZED (save format v6, appended after the ADR-012 ferrite tail, ADR-007 rally precedent for mutable movement state); Raw-zero NearestApproachSq is the unseeded sentinel so a fresh order re-arms by zeroing it in the one command handler. Fixed serialization to list v6 in hasRallyFields/hasFerriteCap (the B1-era one-magic regression the code warns against, caught by catrefuse) and updated the runner's DowngradeSave layout helper to a v6 source stripping the 12-byte tail. THE REGENERATION under ADR-014: all 24 goldens move MECHANICALLY (a per-entity hashed field shifts every fingerprint, the rally pattern not ADR-012's selective one). NEUTRALISATION PROVED BY IDENTITY: deadline-disabled vs deadline-210 seed-2026 goldens are BYTE-IDENTICAL across all 24 scenarios, so the backstop never fires at 2026 and every golden move is the field-append alone with ZERO behavioural change at the golden seed; the only behavioural change anywhere is seed 900913, whose whole crowd now settles by tick 499. PROOF: full battery exit 0; the five-seed determinism suite (2026/31337/424242/777/900913, 900913 now settles instead of throwing) plus lan 20 all exit 0; the exact CI golden check byte-identical to the committed file; both Godot client builds 0 warnings (Debug + ExportRelease); saveload/campaignsave v6 round-trips green; project.godot and user save/replay/settings untouched; FogOfWar.cs and Minimap.cs untouched (concurrent local session owns them). The push gate exercises seed 2026 only, so its green does NOT by itself prove the soak; the LOCAL five-seed run is the proof and is pasted in the delivery.
- [x] TICKET-P6-C1a-STANCES (ADR-015) | owner: sim-engineer + client-engineer + architect (Balance under A11 for the leash) | P6 WAVE C1a, commits 0562aaa/be3fd76/a9d041a: THE UNIT COMMAND STANCES ENTER THE SIM (ADR-015, ratified), resolving Q003 and closing P4-PORT-01, with the wave's ONE golden regeneration; formations (P4-PORT-05) split to TICKET-P6-C1b, filed pending, exactly as the ADR scopes. BUILT ON THE WIP not redone: the interrupted checkpoint already carried the Stance enum, the six Entity fields, SetStance and its handler, CancelPositionalStance and its three call sites, and the one CombatSystem hold-fire guard, all matching ADR-015; it lacked only the StanceSystem body (so it did not build), the hash and the save v7 serialiser, and the WIP sim commit was AMENDED into one complete buildable commit. THE RULE IS THE ADR'S EXACTLY: Stance : byte { Aggressive = 0, HoldFire = 1, Guard = 2, Patrol = 3 }, zero the struct default and today's behaviour. HOLD-FIRE is the one combat change: the auto-acquire else-branch (taken when ExplicitTarget < 0) runs only when the stance is not HoldFire, so a HoldFire unit does not fire even with an enemy in range while an explicit Attack still fires, and it persists across a Move (Q003's engineer past the sentry); that guard is the load-bearing neutralisation point (always true for Aggressive). GUARD is a leashed hold: a new StanceSystem step ordered after OrderDispatchSystem and before MovementSystem finds the nearest targetable enemy within the unit's own SIGHT of its POST (leash, ties to lower id) and sets it as ExplicitTarget if armed, routing engagement through the explicit-attack close-and-fire path (immune to the crowd-arrival shortcut so short-range guards close), and returns to the post within the crowd radius when the leash is clear; the leash is the unit's own Sight, no schema change, no magic constant. PATROL is two-point cycling: SetStance kicks off the outbound attack-move leg toward endpoint B (ordered point, clamped as Move) with A pinned to live position, and StanceSystem flips PatrolOutbound and re-arms the no-progress backstop whenever AMove clears (reached the endpoint, area clear), engaging en route via the attack-move machinery whole. TRANSITIONS: SetStance = 17 is absolute not a toggle (idempotent, replay-robust), only a Unit accepts it, and a Move/PathMove/AttackMove/Stop/Attack order cancels Guard/Patrol back to Aggressive but PRESERVES HoldFire. STATE HASHED AND SAVED: the six fields (Stance, PostX/Y, PatrolX/Y, PatrolOutbound) are appended after the ADR-014 no-progress tail in declaration order, HASHED (rally precedent for mutable command state, unlike ADR-012's unhashed cap) and SERIALIZED (save v7, SaveMagicV7 0x534C4137); v1..v6 saves load Aggressive with zero geometry (exactly what they meant) so old saves and replays resume identically; DowngradeSave updated to read v7 and strip the 34-byte stance tail, keeping the v6/v5/v4 downgrade tests alive. THE CLIENT (presentation only, ADR-001 intact): three rebindable actions default H hold-fire / G guard / Q patrol, each a presentation-only issue of the one SetStance command with the live stance read back for the readout; hold-fire is an absolute toggle (all-held releases to aggressive), guard sets a leashed hold in place (no point, sim reads the post), patrol arms the attack-move two-step (key then far-point click, attack cursor and colour); actions added to project.godot [input] and Settings.Bindable (the single list the settings scene, conflict detector and ApplyBinds iterate) so they rebind and clash-check with no extra plumbing; an own combat unit's readout names its live stance and the three keys from their live bindings (SET-01), the MCV keeps deploy and is offered no stance. THE REGENERATION under ADR-015: all 24 goldens move MECHANICALLY (a per-entity hashed field shifts every fingerprint, the ADR-014 rally pattern not ADR-012's selective one). NEUTRALISATION PROVED TWICE BY IDENTITY on scratch builds reverted before the golden commit: (1) with the StanceSystem inert and the hold-fire guard forced off but the fields STILL HASHED, the seed-2026 goldens are BYTE-IDENTICAL to the wave build across all 24, proving the behaviour never fires at 2026; (2) additionally UN-HASHING the six fields recovers the PRE-WAVE goldens BYTE-IDENTICAL across all 24, proving the only mover is the field-append with zero behavioural change at the golden seed. NEW STANCEGATE (additive, standalone + battery, never a golden scenario, golden list stays 24): a hold-fire unit does NOT fire on an in-range enemy (10/10 hp) while an aggressive twin kills it; a guard leaves its post to kill an intruder nine cells out inside its leash and returns within the crowd radius holding no target; a patrol cycles its two waypoints (>= 3 flips over 600 ticks, near both ends); a v7 save round-trips all three stances bit-exact and a v6 downgrade of an aggressive world loads every unit Aggressive hash-identical. PROOF: full battery exit 0 at 2026 and 900913 (selftest, determinism 24/24, every scenario assertion, defence load 1.740ms/tick, catrefuse, spawngate, prodgate, regrowthgate, stancegate, lan); the exact CI golden check byte-identical to the committed file; the FIVE-SEED determinism suite ALL 24/24 double-run identical (2026, 31337, 424242, 777, 900913) with 900913 NOT regressed (ADR-014's backstop holds); the 20-game LAN soak zero desyncs; saveload (v7, 14601 bytes) and campaignsave (v7) bit-exact, replay reproduced a 3000-tick AI match bit-exact, spectate and lanchaos green; both Godot client builds 0 warnings (Debug and ExportRelease); project.godot [input] and other sections intact, no user save/replay/settings written, FogOfWar.cs and Minimap.cs untouched (concurrent local session owns them). AI-vs-AI resolves normally at 2026 and 900913 (AI does not use stances yet, game unbroken). Findings and owed items: docs/tickets/P6-wave-c1-stances.md and Q003 RESOLVED (Balance may promote the guard leash to a dedicated stat under A11 with no wire change; the AI-engineer may teach the AI to adopt stances through the same SetStance command with no format change; docs/game-designer may close doc 18's "H hold" row now that it binds a real behaviour; C1b formations filed pending in docs/tickets/P6-wave-c1b-formations.md). Details in docs/questions/Q013 RESOLVED section and ADR-014.
